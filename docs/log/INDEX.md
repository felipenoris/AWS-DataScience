# Log — index

**What was actually done in AWS, by hand, one file per stage.** The stage files are written **cooperatively
by the user and by Claude — and by Claude only when the user asks**, in that sitting. Plan narrative does
not belong here either way; it goes in [`docs/plan/history.md`](../plan/history.md).

**Claude never edits a stage log on its own initiative** — not to tidy it, not to correct it, not as a side
effect of some other task, and not because an entry looks incomplete. This is the half of the old
"never by Claude" rule that was load-bearing and it is unchanged: an unrequested edit to the project's
record of what happened is the one change nobody would think to review. *(Revised 2026-08-17, by the user.
Before that date Claude was barred from these files outright and wrote by named exception, which is why
every entry up to Stage 4 carries a provenance note in that older language.)*

**Provenance is what makes the change safe, so it is not optional.** A log is evidence, and evidence whose
author is unknown is worth less than evidence with a name on it. Every file's header states who wrote it;
where one file has entries from both hands, **each entry says which**, and so does anything inside an entry
that came from the other one — a measurement quoted verbatim, an explanation added on request. The
recurring failure this prevents is Claude writing what it *believes* happened over what was *measured*: a
reading pasted by the user is data and stays verbatim, while what Claude adds around it is analysis and is
marked as such.

**This index is Claude's to maintain without asking.** After reading a stage log, Claude brings that
stage's `Records` cell to what the file now holds.

One log file mirrors one stage file: `docs/log/log-stage-NN-*.md` ↔
[`docs/plan/stages/stage-NN-*.md`](../plan/stages/INDEX.md) — **the same slug, with a `log-` prefix**, so no
log file ever shares a filename with the plan file it mirrors. **Open the one row you need** — the `Records`
column below exists so that no stage ever requires reading another stage's log.

| Stage | Log | Records |
|---|---|---|
| Stage 0 — Baseline | [log-stage-00-baseline.md](log-stage-00-baseline.md) | Management account created by hand; `aws` CLI and `terraform` installed; the pre-existing `SUSPENDED` `Sandbox` account attached to the root, which predates the project and is left alone (`EXC-01`). |
| Stage 1a — Landing zone, accounts and OUs | [log-stage-01a-landing-zone.md](log-stage-01a-landing-zone.md) | Account-quota ticket (15); root MFA; the USD 50 budget; Control Tower enabled in `us-west-2`; every OU and account vend, **including the refused `Identity` vend under `Security`** that produced the `Identity` OU; the access portal; the `AWS Control Tower Admin` and `Infrastructure User` SSO users; the **break-glass chain** (`awsds-org-break-glass-alerts`, the `awsds-org-root-activity` metric filter, the `AWS Break Glass Alert` alarm) and its **2026-08-09 two-channel test**; centralized root access enabled and the per-account `Delete root credentials` check. |
| Stage 1b — Identity, policies, detective controls | [log-stage-01b-identity-and-controls.md](log-stage-01b-identity-and-controls.md) | **Complete — closed 2026-08-12.** Step 8.3's metric filter and its alarm; the Identity account registered as Identity Center delegated administrator (step 1); the five `sso-group-*` groups and their members (step 2); the `InfrastructureAccess` permission set, its tags and its assignments (step 3); the probe showing the delegated administrator reads everything but cannot change Management-targeted assignments (step 4); the six SSO profiles and the assumed-role ARN each one returns (step 5); the first `aws/list-identities.sh` snapshot and what it settles — step 3's owed tag check, step 4's read boundary across the whole read surface, and the root's enabled policy types; 5.1's retirement of the Account Factory direct assignments, including the second delegated-administrator boundary it uncovered — a permission set provisioned into Management cannot be altered from Identity, so the removals ran as CT Admin on Management, and verification (vi) left open by construction; step 6's AZ name→ID table, identical across every account that has a profile, with the names not in ID order; and **step 8.2**, where the organization IAM Access Analyzer was first created in the *wrong account* from the console wizard, deleted, and re-created inside **Audit** as `awsds-org-external-access` (`ORGANIZATION` zone of trust) with Audit registered as the `access-analyzer` delegated administrator. **And the 2026-08-15 close of that thread — the first authoritative read from inside Audit** (`aws/cloudshell/audit-iam-analyser.sh`, single-file upload): CHECK OK — one analyzer, ORGANIZATION, ACTIVE, five tags, no archive rules, scan current — and the 35-finding baseline, every one an `AWSReservedSSO_*` role through the directory's SAML trust, recorded as INV-16; report stored at `aws/output/cloudshell/audit-iam-analyser.txt`. |
| Stage 1c — Preventive policies | [log-stage-01c-preventive-policies.md](log-stage-01c-preventive-policies.md) | **COMPLETE — closed 2026-08-14 with 7.8.** 7.0's measurements: the organization's coordinates and the `Data` OU path the `datazone` carve-out is written from, the OU tree with ids, the Control Tower guardrail on each OU with its policy id and the reading of those documents — **Config already denied, CloudTrail denied nowhere, `Sandboxes` the one OU with no guardrail and the one whose control list came back *empty rather than erroring***, `Identity` registered after all; verifications (iii) and (x) answered; the policy quota measured as **not published**; BPA `NOT SET` everywhere beforehand. Then 7.2's three `enable-policy-type` calls, with AWS attaching `RCPFullAWSAccess` to every node by itself; 7.4 step 1's account-level BPA in **all nine accounts**; the 7.3 battery in three phases, with each probe's wording; and 7.5's two attachments to the root — `awsds-org-scp-baseline` (`p-1fp032g8`) and `awsds-org-scp-perimeter` (`p-4vs49ztw`) — including the **two statements attached but unexercised** (`ec2:ModifySnapshotAttribute`, whose id is validated before authorization, and `datazone:CreateDomain`, deferred to Stage 6 step 0), the decision-7 carve-out proven in both directions, and the canary emptied in the same sitting. Plus the two operational findings: the SSO token expiring mid-battery makes every probe read like a deny, and the baseline carries **no CloudTrail deny** by decision. **Then 7.6**: the four `awsds-org-scp-ou-*` documents created, parked together on `Policy Test`, probed, moved to `Workloads`/`Data`/`Interactive`/`Identity` and **re-probed from each OU's own account** — with the id of each policy, the probe that proved it, the two cross-checks (`glue:StartCrawler` denied in Data and allowed in Identity; `CreateSpace` and `datazone:ListDomains` still working in Development and Sandbox while `CreateNotebookInstance` is denied), the inheritance result in `Sandboxes`, and the two things left **untested** (`s3:DeleteBucket`, and the positive half of the D27 carve-out until Stage 5). The finding that outlives the sitting: **the denial message names the policy id, but only one when several deny the same call** (Lesson 20). **Then 7.5a and 7.6a**, the amendments a post-attachment review produced and the only place they are recorded as *executed*: the four EC2 launch doors and the D27 service guard in `Data`/`Identity`, and in the root baseline the GuardDuty vocabulary fix — it had named the deprecated `…FromMasterAccount` and not the live `…FromAdministratorAccount` — plus the new `DenyImageAndSnapshotExport`. Each document read back from Organizations before probing, then re-probed twice over: **phase 4b** in each OU's own account (`CreateFleet` and `RequestSpotInstances` denied in both, the crawler guard proven not to have inverted the carve-out) and **phases 1-3** on the canary for the root document (seven denies, all naming `p-1fp032g8`). Carries the second finding of the stage: **the validation-before-authorization wall is per *action*, not per service** — three different answers from EC2 in one run — which is Lesson 21 and the reason two probes recorded as untestable were neither. **Then 7.7**, in the order it happened: `CT.MULTISERVICE.PV.1` enabled on `Policy Test` first and probed alone — **exactly one row flipped**, which is what tells the intended control from the loose one — then **verification (vii) read from the attached document** rather than a console tab (86 `NotAction` entries, the four exempted Control Tower roles, and the correction that **`ecr-public:*` is exempt** while `ssm:GetParameter` is not), then the enablement on the five remaining OUs and the whole battery at **61/61**, with the denying policy id per account. **Verification (xi) closed in both halves**: `Sandboxes` accepted `enable-control` and carries its first policy ever, yet the deny that fires there names `Interactive`'s — Lesson 20 again, and the OU's policy list, not the probe, is what answered registration. Also the packing observation (two `aws-guardrails-*` per OU, one on `Sandboxes`, and `Policy Test`'s root controls folded into its *existing* document). Then the two root-user controls, and the one thing in the stage found by **reading rather than probing**: `AWS-GR_RESTRICT_ROOT_USER` enabled on `Policy Test` without `ExemptAssumeRoot`, the same-day re-enable that put `Null: aws:AssumedRoot=true` into `p-kve97k0o`, and the four remaining OUs enabled *with* it and read back one by one. Two findings close 7.7: **Control Tower's packing is inconsistent** — the root statements landed in the original guardrail on three OUs and in the *Region* document on `Identity` and `Data`, so a document's id says nothing about its contents — and **`Sandboxes` was emptied by decision**, the Region control disabled and its document gone, under the rule that nothing is attached or enabled there unless it differs from `Interactive`. **Then 7.8, which closes the stage**: the four documents written (decision 5 settled by measurement against the machine-readable service reference, which inverted the plan's own premise about S3 and could not measure `ec2:RunInstances` at all, leaving that action as the scope), the three new battery phases and the **defect in `readback.py`** that would have reported an attached RCP, tag or declarative policy as *"not in the organization"* both before and after — silent in the reassuring direction. The **two before-readings** that make the after-readings mean something: `./aws/declarative-ec2.sh` in five accounts, where only two of the four attributes actually move state, and four tag forms of `ec2:RunInstances --dry-run` all passing. Then the attachments, each with its policy id — the RCP staged on `Policy Test` first, then `awsds-org-declarative-ec2`, `awsds-org-scp-tag-enforcement` and `awsds-org-tag-policy` on the root — and **the outage, which is the stage's largest finding**: `awsds-org-rcp-perimeter`'s STS statement named `AssumeRoleWithSAML` and `TagSession`, which is *all* an `AWSReservedSSO_*` trust policy permits, so it **locked every SSO user out of all six member accounts at once** while the CT Admin console on Management stayed up (RCPs do not reach Management), and a 4 h credential cache made the lockout land minutes after the attach had appeared to pass. Detached, rescoped to `sts:AssumeRole` + `sts:SetContext` to match AWS's own `CT.STS.PV.1`, re-attached with the cache cleared first, and re-probed: **93 as expected, 0 unexpected, 0 untested**, read-back clean on all ten documents. Plus the two harness repairs the run produced — `classify` no longer reading a declarative policy's custom message *echoed by a successful call* as a deny, and `ensure_session` separating an expired token from a denied sign-in — which is **Lesson 24**. |
| Stage 1d — Audit trail, Config scope, org-wide enablement | [log-stage-01d-org-wide-enablement.md](log-stage-01d-org-wide-enablement.md) | **Complete — all four steps, and the landing zone with them.** **Step 9, 2026-08-15**: the stage's one permanent act. Preflight in Log Archive resolved the bucket **from the trail** rather than from a name, and a `get-bucket-logging` loop over every `aws-controltower-*` bucket confirmed the target appears only as a *source* — which also corrected two names the plan carried (the access-log bucket is `aws-controltower-cloudtrail-access-logs-*`, and there is **no** `config` bucket in Log Archive, confirming by inventory that it is in Audit). **Decision 9: borrow `AWSControlTowerExecution` from Management**, recorded as the only sanctioned by-hand use of that role; **decision 3: `COMPLIANCE` / 90 days**, whose cost is **zero** because the bucket's lifecycle already keeps every version 365 days — and which creates a standing floor, that lifecycle can never be shortened below 90 (INV-14, binding Stage 12 step 5). Read back `ObjectLockEnabled: Enabled`, `COMPLIANCE`, `Days: 90`; **decision 7 is now exercised rather than only measured**, the `CTS3PV8` exemption matching an assumed-role session. **The verification that counts was added on execution**: an object delivered at 03:12 *after* the write carries `COMPLIANCE` to 2026-11-13, with three deliveries that day — the bucket-level read proves configuration, this proves the default reaches CloudTrail and that the lock did not stop delivery. Finding it needed the trail's **`S3KeyPrefix`**, which is the organization id, so a key carries the org id **twice**. **Four attempts, and the failures are the record**: a borrowed session from an earlier sitting stayed exported in the shell, so `list-accounts` was denied as a management-account API called from Log Archive, the empty variable was carried into a malformed role ARN, and both errors read as authorization problems that did not exist (**Lesson 25**); then the name filter `Name=='Log Archive'` returned `None`, because the account is `Log Archive Account` — the id was derived from the trail's own bucket name instead. **Verification (iv)'s first half read `ACTIVE` / `IN_SYNC` / `version 4.0 = latest` — and the entry records why that is a *weak* negative**: Control Tower watches a closed list of things it owns, which very likely excludes an arbitrary bucket setting, so `IN_SYNC` confirms nothing was tripped rather than predicting the update. The same fact explains why 1c's ten attached documents never raised drift — a customer SCP is not Control Tower's. **The second half stays provisional, though narrowed**: Object Lock cannot be reverted by Control Tower either, so the failure mode is an update erroring, not reverting. **Step 12, 2026-08-14**: `CT.MULTISERVICE.PV.1` (allow `us-west-2`), `AWS-GR_RESTRICT_ROOT_USER` with `ExemptAssumeRoot` and `AWS-GR_RESTRICT_ROOT_USER_ACCESS_KEYS` enabled on the `Security` OU from the Control Catalog, then probed **by hand in CloudShell in each of Log Archive and Audit** — `ec2 create-key-pair --dry-run` denied in `us-east-1` with *"explicit deny in a service control policy"* naming `p-idgyiios`, `DryRunOperation` in `us-west-2`, which is verification (xiv)'s first half in both accounts. **Step 10's spend half, and the two things that had to happen first**: `AWS Control Tower Admin` was refused on the Cost Management console (`ce:DescribeReport`) although `AWSAdministratorAccess` carries `ce:*` — the cause was **IAM access to billing never activated**, which only root can change, so the user signed in as root twice (once to find Cost Explorer already enabled, this being an old account with months of history, once to activate the toggle). With that done the same reading works as CT Admin: Cost Explorer, current month, daily, `Service = Config` — **one usage type only, `USW2-ConfigurationItemRecorded`**, no rule-evaluation line at all; **USD 2.28 month-to-date of which USD 2.20 is a single first-day spike spread evenly across the accounts**, Audit's whole share USD 0.28, last activity the day before in `Policy Canary`. **Step 10's volume half, and both of its decisions, closed the same day.** Measured twice by two routes that agree exactly: a loop over the six `awsds-infra-*` profiles from the laptop (`select-resource-config`, **80-82 resources per account**, with a type breakdown showing ≈28 of the 82 are AWS service defaults recorded once and never changed) and the organization aggregator from Audit, which adds the two accounts no profile reaches — **Audit 86, Log Archive 79, eight accounts in all and Management absent.** The recorder's own shape, read from Development: `allSupported: true`, `includeGlobalResourceTypes: true`, `CONTINUOUS`, `PAID`. **Decision 4: leave it alone**, revisit at Stage 12 step 5 with EC2/ENI churn as the signal — the exclusion list is worth under a dollar org-wide and `recordingFrequency: DAILY` is the *expensive* mode at this change rate. **Decision 8: no** — Management has neither recorder nor delivery channel, so D16's `iam-root-access-key-check` meant five hand-made resources there; `AccountAccessKeysPresent` reads `0` and the alarm chain is live, so the instrument moved to **step 4 of the break-glass test** and the residual is written. The earlier 2026-08-14 entries, drafted by Claude at the user's request from laptop readings: the trail's **real** bucket name (`aws-controltower-cloudtrail-logs-*`, not the `aws-controltower-logs-*` the plan carried) and its log-file validation **already `true`**, both read from a *member* account; **`CTS3PV8`**, which denies `s3:PutBucketObjectLockConfiguration` on the log buckets to every principal but `AWSControlTowerExecution` — the wall in front of step 9, and the `NotAction` list that permits `s3:DeleteObjectVersion` to everyone, which is why the step is not redundant with it; Lake Formation reading `CROSS_ACCOUNT_VERSION = 4` with `SET_CONTEXT = TRUE` in an account with no lake, which answers verification (v) without acting and turns 11.2 into an instruction to Stage 5; `ram.amazonaws.com` **absent** from trusted access, so 11.1 has real work; and the `CT.MULTISERVICE.PV.1` exemption reading step 12 needs — 86 `NotAction` entries, four exempt Control Tower roles, `config:*` and `access-analyzer:*` exempt entirely while `guardduty`/`securityhub`/`macie2` are not. Then the user's own CloudShell readings as `AWS Control Tower Admin`, in three accounts and all still read-only: **9.1's before-state in Log Archive** — the CloudTrail log bucket has **no Object Lock** (`ObjectLockConfigurationNotFoundError`), versioning **`Enabled`**, and a lifecycle expiring current *and* noncurrent versions at **365 days**, which is the ceiling decision 3's retention has to sit under; **verification (xiii) answered in Management** — `describe-configuration-recorders` returns `[]`, so the landing zone does **not** record the management account and `docs/plan/cost-model.md`'s assumption holds, which is what makes decision 8 a real choice rather than a formality, plus `AccountAccessKeysPresent` reading **`0`** (D16's invariant holds today, by the free instrument 10.4 calls an intention); and **10.3's aggregator located in Audit** — `aws-controltower-ConfigAggregatorForOrganization`, created by `controltower.amazonaws.com` on 2026-08-09 with `AllAwsRegions: true`, the place the item counts are to be read from. **Then the stage's first write, and step 11 closes with it**: `ram enable-sharing-with-aws-organization` from Management returning `{"returnValue": true}`, the trusted-access list read back **eight principals with `ram.amazonaws.com` at 18:47:22Z** where the before-reading had seven, and `AWSServiceRoleForResourceAccessManager` created one second later — the service-linked role being the half that distinguishes this path from enabling trusted access on the Organizations side, which reads identically in the list and shares nothing. **INT-11's two organization-level halves are settled** and INV-09 restated. |
| Stage 2 — Terraform foundation | [log-stage-02-terraform-foundation.md](log-stage-02-terraform-foundation.md) | **Steps 5.0, 5.1, 1, 6, 9, 2 and 3 closed 2026-08-15; steps 5.1a and 5 closed 2026-08-16, `org-policies/` included — so step 5 is complete in both slices and what remains of the stage is its own close-out. Nineteen entries: five on the delegation, two on open question 11, two on the repository, two applies, the delegation's narrowing, the entitlement plane and the policy plane in two halves each — written, then imported and applied — then step 8's D11 lifecycle targets, the Validation with the stage's close-out, and verification (iii).** The delegation document as applied from Management (its three `Sid`s in full), and all four of 5.0's readings in order: the pre-delegation `ResourcePolicyNotFoundException`, the two `describe-policy` reads, the deliberate `update-policy` **write**, and the target-side duplicate-attach with the negative control that makes it evidence. **The answer is the good one** — a root-attached document is manageable, so `identity/org-policies/` holds all ten. Also the three measurements taken to write the document, `DEL-8`'s blind spot found and closed, and the one delegation entry that stays unexercised by construction. **Then the two `awsds-ctadmin-orgfull-*` CLI profiles under a second `sso-session`**, created because `AWSOrganizationsFullAccess` **has no CloudShell** — an implicit deny naming `cloudshell:CreateEnvironment`, no SCP involved. **And open question 11 measured**, as `AWS Control Tower Admin` through that permission set in two accounts with a single variable: in `Identity` eleven reads answer and the duplicate-attach returns `Duplicate…`; in `Development` **two** calls answer and everything else, the write included, returns `AccessDenied`. So the organization-wide reach belongs to the **account** — delegated administrator of Identity Center — and the write to **5.1's account-wide principal**, which reaches a second identity nobody chose. Four things the run corrected: **the denial wording does not classify** (Organizations returns the same text for management-account-only and for no-allow, so the discriminator was the controlled A/B and never the message); `DuplicatePolicyAttachmentException` carries `CreatePolicy` duplicate-name text; `describe-organization`'s `AvailablePolicyTypes` disagrees with `list-roots`; and the inline "re-read the pair before aiming at it" is impossible in the control leg, so its safety rested on the other leg's reading minutes earlier. **The eighth entry is steps 1 and 6, with no AWS call in it:** the version pin and why a pin is recorded in a log at all, the five `bootstrap/` slices and **why the rest of conventions §6's tree is deliberately not on disk**, the lock file generated once and copied because it is a function of the constraint and the platform list alone, and the three tools installed by two different routes — plus the fact that installing `tflint` is not the whole setup, since its rulesets are plugins that need `tflint --init` and a fresh clone hits that second step with no announcement. Also the `.gitignore` gap that mattered (`*.tfstate` was uncovered, which is exactly the bootstrap state of 2.2), **zero checkov suppressions and why that is a reason rather than a silence**, the backend generator and the three vocabularies its map exists for, and the `--tf-init-args=` spelling whose wrong form does not error — it turns the hook into a credentials prompt on every commit. **The ninth entry is step 9, local like the eighth:** the four checks behind two enforcement surfaces — the same scripts under `make check` and `pre-commit`, so the gate and the target cannot disagree and Stage 8 step 6 adds a third caller rather than a rewrite, with 9.3 kept out of the commit gate because it needs an SSO session (`make check-ou`); the `Makefile` created (`check`/`check-ou`/`check-docs`/`check-all`), the same file step 8 adds `up`/`down`/`status` to; the **authored OU→document map** `terraform-live/identity/org-policies/attachments.json` — names, never ids; one file, two consumers, 9.3's check and step 5's `for_each`; `check-plan-refs` deliberately left out of `make check` and given `make check-docs`, red today on prose predating the stage; **two silent defects found by testing 9.1 rather than reading it** — a scanner that failed to compile, matched nothing and reported `none` over a fixture holding all three violations (Lesson 13), and a line counter that accumulated across the file list; the gate **demonstrated failing** on staged fixtures — three hooks red at once — before being believed; and green afterwards: `make check` exit 0, the twelve-hook `pre-commit` chain passing `--all-files`, 1045 links resolving. **The tenth entry is step 2 — the project's first `terraform apply`, and the only AWS write in the stage so far:** the preflight that proved the name free before anything was created, then the two-phase bootstrap (local apply, backend uncommented, `-migrate-state`, local state deleted) that leaves one bucket and one KMS key in `Sandbox Account 1` with the state encrypted under that key and `BucketKeyEnabled: true`. **Decision 3 settled — noncurrent state versions expire at 90 days**, plus a 7-day abort rule, taken on day one because a lifecycle rule added later does not reach what has already accumulated. Two things the step owns that the plan had not named: **a second generated file per slice** (`terraform.auto.tfvars`, from the same table as `backend.hcl`, because `region`, the `<env>` token and the `Environment` tag value can be literals in neither a `.tf` nor a tracked file), and **the three checkov suppressions of 6.5 with the finding that produced them — a `# checkov:skip=` line written *above* the resource block is read as an ordinary comment and the check fails anyway**, with nothing saying the suppression was ignored. The verifications that go beyond the snapshot: a second `plan` from S3 reads `No changes`, and **two concurrent `plan` runs return one success and one `Lock Info … OperationTypePlan`**, which is D3's native S3 locking proven with no DynamoDB table and no lock object left behind. `./aws/tf-backends.py` closes at `BK-0`–`BK-5` all pass, 0 FAILED — so "no state bucket" in that account is a regression from here on. **The eleventh entry is step 3 — the same two-phase apply in the four remaining accounts, and now five state buckets exist**, `production/` planning seven resources rather than six (3.4's second key). What the entry records beyond the applies: the copy of the slice got an **instrument** — the two legitimate differences pushed into files of their own (`backend.tf` holding the backend block alone; `pki-key.tf` allow-listed by name, with a `precondition` on `var.env == "prod"` so a stray copy fails the plan) and `scripts/check-bootstrap-parity.py` comparing everything else byte for byte, **demonstrated failing on all four of its paths before being believed**. The read-backs: every second plan empty, each state object under its own account's key with `BucketKeyEnabled: true`, Production's state visibly larger (the two extra resources), rotation 365 days on all five keys, no local state anywhere, lock files untouched, `BK-7` pass on Production, and the `Environment` tag right in the three accounts where it is spelled differently from the folder. **Verification (i) half-closed by reading the applied bucket**: nothing in the bucket policy forces one key, so `pki/` keeps its own key inside the shared Production bucket; whether a Bucket Key applies to the override needs an object under the PKI key and stays open — D36's alarm unaffected either way. **The twelfth entry is step 5.1a — the delegation narrowed to one role, and the second and last Management action of the stage.** 5.1's `Principal` is the *account*, because a resource policy has no narrower one, so it reached a second identity nobody chose; the fix is an `ArnLike` on `aws:PrincipalArn` added to the two **write** statements and to neither read. Three things the entry carries beyond the paste: the amended document was **generated from the live one and diffed to prove nothing else changed** — 5.0's "its own content" discipline applied to an edit rather than a rewrite, with the read-back doubling as the rollback copy; **`DEL-10` was written and demonstrated red-then-green *before* the change**, so the condition arrived as a control rather than as an intention; and **the open question is answered — the document does accept a `Condition`**, though it still rejects `NotAction`/`NotResource`, so those are not one rule with two instances. **Verification (ix) in both halves, because either alone proves nothing:** the duplicate `AttachPolicy` turned `Duplicate…` → **`AccessDenied`** from `awsds-ctadmin-orgfull-identity` **and stayed `Duplicate…`** from `awsds-infra-identity` — the first row alone is indistinguishable from having broken the delegation outright. What it does not close is recorded as a cost: the condition is a **second place a principal is enumerated**, and a Stage 8 pipeline role will have to be added there. **The thirteenth entry is step 5's first half — the three decisions, and `terraform-live/identity/sso/` written.** **Decision 4: inline-only, the boundary *object* deferred to Stage 3** — a customer-managed reference would fail *provisioning* per account, so what is deferred is the container and not the content, and the two denies a boundary was wanted for are in the shared fragment now, because the carve-outs they defend are attached now. **Decision 5: `replace(file(…))`, `render.py` untouched** — it produced the bytes that are attached, and editing the generator in the same commit that first compares against them makes the reference and the comparison move together. **Decision 6: `terraform import` on the CLI** — decided by a measured fact rather than by taste, since an assignment's import id contains both an account id and the group GUID, the two values this design keeps out of git for two different reasons. **The slice as authored: six persona sets written and never typed into a console, one deny fragment composed into all six, and ten enumerated assignments.** **The line the slice draws:** every deny complete, every allow whose resource is the service or the account complete, and **no allow scoped to an object that does not exist yet** — each owed to a named stage, because writing them from the naming convention is guessing at an interface and an `awsds-*` wildcard in an S3 ARN means any bucket of that shape on earth. **Three gates answered rather than obeyed:** 9.2 failed on a *comment* naming the wildcard shape (it does not skip comments, deliberately); checkov `CKV_AWS_356` suppressed six times with the reason that one document serving N accounts cannot name the account in an ARN, with the `aws:ResourceAccount` narrowing deferred to Stage 6 where a sign-in can measure whether it silently denies; `tflint` turned one finding into the profile precondition the slice was missing. **The fourteenth entry is step 5's second half — `identity/sso/` imported and applied.** `InfrastructureAccess` was imported only after the live object had been read back first (PT4H, one managed policy, no inline policy, no boundary, `CostCenter=stage-01b`), which is why the import planned `0 to change`. **The for_each key was tested the way 5.5a(iii) demands** — one assignment imported, `plan`, then the rest — after `./aws/import-ids.py` was corrected to emit the address the configuration actually computes; with the old address the five would have imported under wrong keys and planned ten creates beside five orphans, with no error anywhere. **Read back from AWS rather than from state:** the next `plan` empty, zero provisioning failures, a group resolving from its GUID back to `sso-group-governance-managers`, the five mandatory tags on every set, and the provisioned-account counts matching 1b step 3.1 exactly — including the two absences 3.7 is about, with **only** the governance manager reaching Data Governance. Also: **Identity Center stores a compacted inline policy**, about a quarter smaller than the rendered document, so the size ceiling is measured on what the API receives. **The fifteenth entry is step 5.5's first half — `identity/org-policies/` written and gated, imported by nothing yet, and the only entry in the stage with no AWS write in it at all.** **One tracked document had to change, and a measurement is the reason**: `awsds-org-rcp-perimeter.json` wrote a single action as `["ecr:*"]` while Organizations holds `"ecr:*"`, and the provider compares `content` **structurally** rather than semantically — so nine of the ten documents round-tripped clean and this one would have stayed dirty forever. It was the only one-element action array in the folder against **seven of eight single-action statements already written as scalars**, so the fix moved the file toward the folder's own convention, and all ten now decode equal to what is attached. **`./aws/import-ids.py` was emitting an address the configuration does not compute, for the second time in the stage** — it keyed each attachment on the API's target `Name`, which is `Root`, while the slice keys on `attachments.json`, where it is `root`; six of the ten lines would have imported under a wrong key, which **does not error**. Corrected, and then the ten keys the `for_each` computes were **diffed against the ten the manifest emits before any import was run**. **Three things measured before a line was written, so the first plan gets read rather than approved:** all ten policies carry **zero tags**, so `default_tags` adds five to each — the first exercise of 5.1's `organizations:TagResource`; four descriptions are defective — one empty string, two absent outright, and one carrying its whole text in **literal double quotes** from a console paste; and neither `type` nor `description` lives in any tracked file, so the slice authors both and the apply repairs the four. **The 5.5 gate that applies here is therefore zero diff on `content` and `type` rather than an empty plan** — the mirror image of the exemption the written half of `sso/` needed. Two smaller findings the entry keeps: `tflint` turned its findings into the slice again (`aws_partition` deleted, since the ARNs live inside the tracked JSON and none is built in HCL; `aws_caller_identity` promoted to the profile precondition, which matters more here because the wrong account fails as an `AccessDenied` on `CreatePolicy` and reads like a broken delegation); and **9.1 reads string VALUES and skips full-line comments**, so a region literal in a `variable` description failed the gate while the same sentence in a comment above it passes — the mirror image of `sso/`, where 9.2 failed *on* a comment, deliberately. `prevent_destroy` on both resource types makes retiring a document or detaching one deliberately a two-commit operation from here on. Gates all green and `make check-docs` still red on pre-Stage-2 prose, with the limit stated rather than glossed: `validate` does not evaluate `locals`, so the OU name-to-id lookups, the `Data`-OU single-account assertion and the six preconditions stay unexercised until the first `plan`. **The sixteenth entry is step 5.5's second half — imported and applied, and it closes step 5.** Twenty objects imported, none created, every id emitted by `./aws/import-ids.py` and none typed. **The incremental import ran twice, and the two intermediate plans bought different proofs**: one policy → `plan` → the imported one moved to "1 to change" with **tags only** and no create beside it; one attachment → `plan` → the imported one **vanished from the plan entirely**, because an attachment has no mutable attribute and faithful therefore means absent, with `0 to destroy` as the no-orphan half. **The full plan matched the prediction line for line**: `0 to add, 10 to change, 0 to destroy`, not one `content` or `type` diff anywhere in it, five tags per policy, and `description` on exactly four — three `+` for the empty and absent ones, one `~` on the RCP where the literal double quotes come out — while the six already-correct descriptions produced no line at all, byte-equal with what `locals.tf` authors. **The apply ran from a saved plan file written outside the repository**, so what was read is what ran: `0 added, 10 changed, 0 destroyed`, the first exercise of the delegation's `organizations:TagResource` statement, and **the second plan reads `No changes`** — 5.5's gate met on the half it was written for. **Read back from AWS rather than from state**: `organizations list-policies` once per policy type, all four filters, ten ids identical to the state's; the four descriptions as authored; the five mandatory tags on all ten with `CostCenter=stage-01c`; `make check-ou` green at root 6 / four OUs 1 each / three empty as authored, and `make check` green. **And the ceiling was never rewritten, proven by the bytes rather than by the plan** — the live documents still hold 1c's console-pasted, order-preserving minification rather than `jsonencode`'s sorted form, so the apply changed descriptions and tags around documents it did not touch. **The slice-independence deliverable measured on both sides**: ten policies, ten attachments and zero `aws_ssoadmin_*` here; the only `aws_organizations_*` entry in `sso/`'s state is the data source. **The seventeenth entry is step 8 and the re-scope, with no AWS call in it.** It opens with the finding that produced the rest: **measured against the disk on 2026-08-16, this cell and `CLAUDE.md` both said the stage had nothing left but its status header, and three things were open** — `terraform-modules/` held one `README.md`, the `Makefile` said in its own header that `up`/`down`/`status` were unwritten, and no `[E]` slice had ever existed so the Validation had never run. **A stage is closed against its own file, never against a summary of it.** **Step 7 then left the stage entirely** for Stage 3 step 1.1a: the argument that moved it to the end on 2026-08-15 — nothing in Stage 2 consumes a module — does not expire when the stage does, and it un-blocks an input Stage 2 cannot settle, since a module is consumed **by git tag** in a monorepo whose host is GitHub today and GitLab from Stage 7 (`docs/plan/history.md`). What step 8 built: the layer table as a `dataclass` list whose **rank is not one of its fields** — read from a `RANKS` map keyed by slice name, so two copies of an order cannot disagree and an unknown name **raises** rather than defaulting to the end; the table **authored against a discovered tree**, failing in both directions, with the expensive one being a slice on disk with no row, since `make down` skips in silence what it has never heard of. **All four refusals demonstrated**, including refusal 3 against a **fixture row that deliberately claimed `production/pki` was `[E]`** — the only way to show the D36 exclusion is not merely shadowed by the layer filter. Two hooks that refuse to be reached silently: `status` prints the empty set **and why it is empty** rather than `USD 0.00/h` (Lesson 13), and `scripts/down-studio-apps.py` exits **1** if a SageMaker domain exists or if the call failed, so "write the hook and leave it empty" is obeyed about the body and not about the exit code. A fourth vocabulary — `PROFILES` — joined `backend.py`, keyed by account folder like the other three, with `AWS_PROFILE` per command and never exported (Lesson 25). **The eighteenth entry is the Validation and the close-out, and it is where the stage's last finding is.** `sandbox/scratch-test/` — one SSM parameter — applied, destroyed, rebuilt and deleted, with its orphan state object removed afterwards because a destroy empties a state rather than removing it. **The finding outlives the slice: `awsds` is a reserved prefix in SSM Parameter Store.** The first apply used `/awsds/…` and AWS refused with `AccessDeniedException: No access to reserved parameter name` — Parameter Store reserves every name beginning with `aws` or `ssm`, and the refusal reads like a policy problem while being a naming one. It is specific to Parameter Store *names*, which is why five state buckets, ten policies and seven permission sets never met it; `docs/plan/conventions.md` now carries the rule and `/datascience/<env>/…` as the answer. Two smaller readings: the parameter's **value is read back from AWS rather than exported from state**, because the provider marks it sensitive and because state agreeing with itself is not evidence; and **the rebuild is proven by a restarted `Version 1`, not by the ARN**, which is derived from the name and would have matched either way. The two claims the Validation exists to make were measured — the destroy plan read `0 to add, 0 to change, 1 to destroy` and named only the parameter while `bootstrap/` sat in the refused list, and `make status` took **both** of its branches for the first time. Verifications **(ii)**, **(iv)**, **(v)** and **(viii)** were answered and recorded, three of them by the step 5 applies: the pinned provider accepts `DECLARATIVE_POLICY_EC2`; the descendant-OU data source **does** recurse, proven by `Sandboxes` at depth 2 appearing in a seven-OU list, which the slice's own postconditions could not have shown; the `for_each` keys come from authored **names**, so a new OU moves none of them; and the `jsonencode(jsondecode(…))` round trip reproduces all four policy types. **The nineteenth entry closes the stage with (iii)**, the landing-zone drift read — a **Management** CloudShell run as `AWS Control Tower Admin`, the account no `awsds-infra-*` profile reaches, through a third script in `aws/cloudshell/`. **The answer is no drift**: `ACTIVE`, version `4.0` equal to `latestAvailableVersion`, `driftStatus: IN_SYNC`, `remediationTypes: INHERITANCE_DRIFT`, and the resource policy still `PRESENT` with 5.1a's condition on exactly two statements. **What makes the entry worth reading is the two limits it records rather than glosses.** The **manifest** holds six keys — `accessManagement`, `backup` (off), `centralizedLogging` and `config` (365-day retention each), `governedRegions: ["us-west-2"]`, `securityRoles` — and **nothing about an organization resource policy**, so `IN_SYNC` is *silence* about the delegation rather than approval of it. And `list-landing-zone-operations` returns **a single `CREATE`/`SUCCEEDED`**: the landing zone has never re-run, so the positive evidence the report was built to capture — an `UPDATE` or `RESET` after 2026-08-15 that the delegation survived — **does not exist yet**, and "has not disagreed" is not "has agreed". Two corrections the run forced on the instrument itself, both found by exercising it from the wrong account first: **from a member account both landing-zone calls succeed and return empty**, so sections 2-4 are now suppressed there and the run exits 2 rather than manufacturing "the landing zone has never run"; and a wrong-account run no longer **overwrites** a good report. A third correction is to a claim the first real run falsified — the manifest is **not** the inventory of what drift detection compares, since it carries no policy of any kind, not even the `aws-guardrails-*` SCPs Control Tower owns; it bounds the landing zone's *configuration*, which is the narrower and defensible claim. Recorded as **`INV-17`**, whose re-read triggers are a landing-zone update, a version bump (the two version fields are equal today) and the `Staging` vend. |
| Stage 3 — Networking | [log-stage-03-networking.md](log-stage-03-networking.md) | **Nine entries.** **First (no AWS call): the five execute-time decisions settled 2026-08-16, before the stage** — the supernet and where the allocation lives (`scripts/tfhygiene/backend.py`, through the generated tfvars — no new file); flow logs at 30 days with the S3 destination declined and why; `egress_mode = A`; the AWS-owned bucket allow-list as five families; step 0 as *remove all and turn creation off*. **Its finding outranks its decisions: a NAT does not bypass an endpoint policy** — the gateway endpoint's prefix-list route is more specific, so in-region S3 is judged by the policy either way, making the allow-list a **Stage 4 prerequisite** (the WireGuard host installs from `dnf`); also the entry the step was missing, **ECR pulling image layers from an AWS-owned S3 bucket** (fails after a successful login, error pointing at S3). **Second: two statements of the first entry corrected against the documentation** — the removal path is the **StackSet stack-instance deletion on Management**, not a per-account hand-deletion, and **AL2023's mirror list lives in the repository bucket itself**, dissolving the design-B caveat; verification (vii) answered by the Route 53 documentation on the way. **Third: step 0 executed 2026-08-16** — the console record field by field (accounts taken from the Stack instances tab, `us-west-2`, Retain stacks **off**; Account Factory's network configuration zeroed), with the merged 0.4: the read-only preflight (zero ENIs, one `CREATE_COMPLETE` stack per account, the template owning the log group with nothing retained), then **no VPC in any measured account**, every stack `DELETE_COMPLETE`, and **verification (vi) answered: nothing survives**, the flow-log log group included. The configuration half is proven at the `Staging` vend. **Fourth: pass 1 applied 2026-08-16** — `foundation/` in Sandbox, Development and Production (31, 30 and 32 resources, only-create, `No changes` on re-plan), read in chat before each apply. **The git order is the entry's lesson: a module consumed by tag cannot share a commit with its first caller** — the validate hook inits from origin, so the tags go *between* two commits. The post-apply reading found the instrument wrong rather than the infrastructure: **`NT-3`/`NT-4` flagged the mandatory public `0.0.0.0/0 → igw` route as an overlap**, and would have stayed red from the first VPC onwards; both now exclude exactly the internet-exit default. The four red `NT-8` rows are pass 2's pending associations. **Fifth: pass 2 applied the same day** — the two peerings and the four cross-account zone associations. **Its shape is the reusable part: minimal requesters in the originating accounts, then everything else in ONE ordered apply on the accepting side** (+1/+1/+32), because that is where "authorization before association" and "route only after the peering is ACTIVE" both point forward; the associations run *as* the VPC owners through provider aliases, and the peers' facts are read by tag rather than pasted. **Verification (iv) answered — additive** (0 changed, 0 destroyed, `No changes` on re-plan); **(vii)'s residual confirmed** (the authorizations are exactly the four Terraform holds); **(v) half-answered** (both ends pin the same AZ pair; placement is Stage 7's). **`networking.py` reads 0 FAILED**, so a red NT row is a finding from here. The entry also records the new runbook for by-hand Terraform changes. **Sixth: pass 3 applied the same day — `egress/`, and the first `make up` that is not a no-op**, one module (`vpc-egress-v0.1.0`) called once per account for 16/15/14 resources (endpoints 12/11/10, a NAT each, **USD 0.480/h**). **Two placements are the reusable part**: the private tier's default route lives in the `[E]` slice because it names the NAT's ephemeral id — which is why every `foundation/` re-plan still reads `No changes` after the applies — and `aws:ResourceOrgID` is deliberately absent from the interface-endpoint policy, because ECR layers and JumpStart artifacts are AWS-owned and org-less, leaving the resource axis to the S3 gateway allow-list that survives a `make down`. `egress.py` all checks passed; `networking.py` still 0 FAILED, the six new `0.0.0.0/0` routes being the first exercise of pass 1's `nat-` exclusion. **Its finding is that two instruments were wrong, both in Lesson 13's shape**: `EG-4` had no pattern for the **ECR layer bucket**, so it reported `pass` without reading the entry and would have kept doing so after a deletion; and `make status` counted a child module as one resource, reporting `2 resource(s)` for a slice holding 16 — the money right, the running-resources line an order of magnitude low. Also a `.gitignore` rule: **a module carries no lock file of its own**, four having acquired one in pass 1 with a single `h1:` hash against the slices' deliberate three. **Seventh: the D11 cycle, run the same day** — `make down` then `make up`, 16/15/14 destroyed and the same counts rebuilt, the burn reading 0.0000 USD/h between them. **The measurement is taken in both directions.** Every `foundation/` output set is **byte-identical** across the cycle — captured to a file *before* the destroy and `diff`ed against the same command after the rebuild, covering the VPC, all three subnet tiers, both gateway endpoint ids, all four route tables, the hosted zone and the security groups — with all three re-plans `No changes` at `-detailed-exitcode 0`; and **39 of 39 `[E]` ids are new, none surviving**, which is the pair INT-05 rests on. Read live from AWS mid-cycle with `egress/` gone: NAT 0, interface endpoints 0, EIPs 0, **gateway endpoints 2 in every account**. The private tier's default route came back onto the **new** NAT everywhere while the isolated tier still carries none — the tier verification (iii)'s `dnf` probe belongs in. **The NAT's public address changed in all three accounts**, so no partner allow-list or external control may ever be written against the address this lab egresses from. Two operational readings the entry keeps: Production needed a **`force-unlock`** after a tool timeout orphaned an apply lock, released only once three independent checks showed nothing in flight (no process, the state object's timestamp unmoved, all 14 resources intact) — the check that separates a safe unlock from a corrupted state, and not optional; and **a defect in Validation item 1's own wording**, which asks for a `diff` of two `./aws/networking.py` runs either side of the cycle while `aws/output/` is regenerated **in place**, so the "before" is overwritten by the "after" — the cycle closed only because the `terraform output -json` had been copied aside first, which is the stricter reading anyway, being byte equality of the anchor set rather than a text diff whose timestamp is expected to move. **Eighth: the probes, the same day — three `[E]` slices rather than a script**, because `aws/probes/` declares that nothing is created and these create instances, and because the expensive failure for a probe is an instance nobody turned off. `production/probes` is the target, `sandbox/probes` the perimeter and peering readings, `development/probes` INT-09; **no IAM principal in any of them**, which is load-bearing rather than tidy — the endpoint-policy statements under test carry no principal condition, so an anonymous request is judged by exactly the statement being measured. **The target is ONE host with TWO interfaces**, one process on `0.0.0.0`, one security group on both: two hosts would have left "nothing answered" indistinguishable from "nothing was listening there", and the sources reach it BY NAME through the peer's private zone so no address crosses an account boundary. **Perimeter** (isolated tier, four readings because the first three are worthless alone): the premise measured rather than assumed (`curl` to the internet timed out), the repo config printed — settling the second entry's claim, since the AL2023 mirror list comes from the repository bucket and `cdn.amazonlinux.com` was unreachable in the same run — then `dnf makecache` **SUCCEEDED**, then the pair, **200** on a real public object in the allow-listed bucket against **403 AccessDenied** on an equally public Amazon Linux 2 bucket the policy does not name. **Verification (iii) answered.** **Peering**, twice against the same host, one variable at a time: permitted **HTTP 200**, the same host's second interface in an unrouted tier **silent**, the permitted address on an unadmitted port **silent**. **INT-09 exercised for the first time** — the Deliverables measure Sandbox to Production and the Proves row is Development to Production, different connections with different routes on both sides — which also answers the DNS half a Sandbox host cannot; the Staging NXDOMAIN clause has no host to refuse until the vend and is recorded as an **absent negative control** rather than substituted. **Flow logs** close it in both directions, and the absence is the interesting half: ACCEPT, REJECT, and **zero** records naming the unrouted address from either source — silence that means something only because the REJECT in the same window proves the instrument records. **Its finding is that the instrument was wrong twice, and running it is what showed both.** The perimeter pair first used buckets that **do not exist** and returned **404/404**, which by its own criterion reads as "the perimeter is open": **S3 answers `NoSuchBucket` before it evaluates authorization** (Lesson 21), and the nonexistence chosen to keep the bucket's own policy out of the comparison had silently removed the policy under test with it. The second defect would have hidden the first — **a `user_data` change does not replace an instance by default**, so the corrected instrument would have run against a host still executing the old one; `user_data_replace_on_change` is now set on all three. Also: `Server.InsufficientInstanceCapacity` for `t4g.nano` in one AZ, answered by a `zone_index` on the source slices and deliberately not on the target (a secondary ENI cannot cross an AZ), with both route tables read first to confirm the readings are AZ-agnostic; the stuck apply ended with **SIGINT so Terraform released its own lock** rather than orphaning one; a security group rule description **cannot contain an apostrophe**; and a single ingress rule converted to `for_each` briefly races its own replacement. Burn with everything up **USD 0.4968/h**, the probes 0.0168 of it. **Ninth: the deferred teardown, run after the probes merged — `make down` on all three accounts, 59 resources destroyed** (4+16 Sandbox, 2+15 Development, 8+14 Production) and `make status` back to **USD 0.0000/h**. **The ORDER is the part worth recording**: in every account the probe slice went first and `egress/` second, because `probes` ranks 60 against `egress`'s 50 and `down` walks the table in reverse — the probes reach their target by a private-zone name over interface endpoints and a NAT, so tearing the network out first would have left Terraform destroying instances whose dependencies had already gone. **Nothing was arranged by hand**: the rank was declared once in `layers.py`. `bootstrap/` and `foundation/` refused by name in each account, reason printed (refusals 4 and 1). **The `[P]`/`[E]` claim measured a third time, and this is the cycle that matters most** — the first where `[E]` held instances, interfaces and DNS records rather than only endpoints: every `foundation/` output set **byte-identical**, all three re-plans `No changes` at `-detailed-exitcode 0`, and live AWS reading **zero** instances, NATs, EIPs and interface endpoints with the **two gateway endpoints still standing in each account**. **An earlier caveat resolved rather than left standing**: `sandbox/probes` had been applied with a non-default `zone_index` and `make down` passes no variables, so the destroy plan was read first — `0 to add, 0 to change, 4 to destroy`, because a destroy operates on **state** and not on what the configuration would compute. **And what the probes did NOT answer, stated precisely so Stage 4 does not inherit a half-claim:** Stage 3's verification (iii) is closed, **Stage 4's verification (i) is not** — it asks whether a host *finishes its user data* through the gateway endpoint alone, and `dnf makecache` fetches metadata without downloading a package, while the CloudWatch agent comes from a **different** allow-list entry `makecache` never touches. Stage 4's first boot closes it, which is why its own plan says to read the user-data output before debugging WireGuard. **Stage 3 is closed**; what remains is the `Staging` NXDOMAIN clause (no host to refuse until the vend) and verification (ii), which is Stage 6's. |
| Stage 4 — VPN access | [log-stage-04-vpn.md](log-stage-04-vpn.md) | **Thirteen entries, the last an ADDENDUM after the stage closed (2026-08-20): the host moved from arm64 to amd64 on the user's direction — one SSM parameter in the module (`wireguard-v0.3.0`), the `instance_type` list following it `t4g.*`→`t3.*`, and an apply that was a REPLACEMENT rather than a resize. It landed: `t3.medium`/`x86_64` running, the `[P]` Elastic IP re-associated so no client `.conf` moved, a new root volume, `./aws/vpn.py` 0 FAILED. Its findings that live nowhere else — the 36-second boot with the host's own `uname` as the architecture evidence, the key fetch again at 2s/zero retries (verification (viii) unexercisable, replicated on different hardware), the private IPv4 moving and why nothing broke, and `terraform-changes.md` §3's post-merge `orphaned` being conditional on a rebase merge. The twelve before it; the first three record no AWS call, the next four carry the stage's AWS writes, the eighth is pass 2's readings, the ninth closes pass 2 and applies pass 3, the tenth is the control-plane pair measured from the laptop, and the last three close the residuals — verification (iii)'s laptop half, the lifecycle cycle and the server-side MTU** (steps 2.3, then 4.3 + 1.4, then 4.1 + 4.2, then pass 2's target, all 2026-08-17; the residuals 2026-08-17/18) — so the tunnel endpoint runs, admits two devices that have both handshaked, and **the control-plane deny is live on the six persona sets**. All nine are written by Claude under an explicit authorisation, named in the file's own provenance note — **which the seventh made finer-grained: its readings are Claude's, its one apply was executed by the user** — and the ninth is the first written under the cooperative rule rather than as an exception to the old one. **First: step 1.3's two merged edits** — the `"vpn": 40` rank between `foundation` and `egress`, the `dormant()` body keyed on the Name tag derived from the row, and the `("sandbox", "vpn")` row added, failed on by `slices.py check` exactly as it should, and withdrawn to land with the slice. **Second: pass 1 authored** — `vpn-anchors.tf` (the `[P]` Elastic IP and the security group carrying step 3.1's single world-open rule), `terraform-modules/wireguard/`, `terraform-live/sandbox/vpn/` with the README for the hand-written `peers.auto.tfvars`, `peer_cidr` emitted to that slice alone, and the row back with its measured `0.0042` USD/h. **Its finding is that the first `[D]` row exposed three defects in the D11 machinery, all the same shape — the code did not implement its own contract**: `make down` would have run `terraform destroy` over the tunnel endpoint (now refusal 5, on the layer, refusing `up` too because an SSM-resolved AMI re-plans as a replacement); the `[D]` hook ran *before* the `[E]` loop in both directions, which is the lockout rank 40 exists to prevent (now first up, last down, and the host is left running when an `[E]` destroy fails); and `make status` counted state-file resources, so a stopped host would have reported a burn forever and Stage 3's USD 0.0000/h could never return (now the power state, read from EC2). **Decisions 2 and 4 taken** — the status-check alarm with `treat_missing_data = notBreaching` and no subscriber, and the host key generated on the laptop with peers as a map keyed by device so a revocation renumbers nobody. **Two things recorded as unverified rather than assumed**: decision 4's cost is wider than the decision named (the key lands in *user data* as well as state, in the account that will host notebook execution roles from Stage 6), and the `amazon-cloudwatch-agent-ctl` flags were never quoted — its page did not render on three attempts — so that step is non-fatal and reports its own exit. Three smaller findings: `wg show all dump` prints the interface's private key, so the sampler uses `latest-handshakes`; **checkov has no check for a world-open ingress on an arbitrary port**, so the one-rule invariant rests on `./aws/networking.py` §9 and `VP-3` alone; and `CKV2_AWS_5` fired on the `[P]` group because the attachment is a slice away, which is step 2.2 itself. **Third: the third design review, reviewed 2026-08-16 and propagated 2026-08-17 — decision 4 revised, the host private key moving out of the tfvars/user-data path into the `[P]` secret `awsds-<env>-vpn-host-key`**, a container with **no version resource** (Stage 7's `gitlab-secrets.json` idiom one stage early: the container is Terraform's, the value never is), enrolled by hand and fetched by the instance at first boot with its own role. What did **not** change is why the key is generated on a laptop at all. **The containment rides on the object** — one resource policy denying `GetSecretValue` to every principal but the instance role and `InfrastructureAccess`, reaching Stage 6's execution roles with no per-set fragment to forget, and scoped to the value read alone because denying `secretsmanager:*` would trap the container's own management. It **closes the previous entry's "cost wider than the decision named"**, and records why the two alternatives lost: a SecureString has no resource policy, and an S3 sibling's reads are data events CloudTrail does not record by default. Prices accepted by name: USD 0.40/month, a first boot that retries until the EIP association lands (each retry naming its cause), rotation as `put-secret-value` **plus** a deliberate `-replace`, and **automatic rotation forbidden forever** — `VP-9` fails on `RotationEnabled`, `CKV2_AWS_57` skipped with that reason rather than satisfied. **Two corrections made while writing it**: the runbook's premise inverted (reconstruction from user data → recovery from the secret, loss now five cases including `restore-secret` inside the 30-day window, the only forced rotation being a secret deleted past it), and a gate of Claude's own that proved nothing — `bash -n` run against still-JSON-escaped `terraform console` output, re-run on the decoded script (Lesson 13). **Fourth: step 2.3 applied 2026-08-17, the stage's first AWS write** — the `[P]` Elastic IP (allocated, **unassociated** until 1.4), the security group carrying the estate's only world-open rule, and the **empty** host-key secret container with its deny policy and rotation off. `4 to add, 0 to change, 0 to destroy`, re-plan `No changes`, `vpn.py` and `networking.py` both 0 FAILED (`VP-3` and `VP-9` passing, `VP-9`'s first real reading). **Its two findings are both in the instruments and the plan rather than in AWS**: `VP-2` emitted **no line at all** for an allocated-but-hostless address — the orphan allocation it exists to price would have billed in silence (Lesson 13, Stage 3's shape, found the same way: by running it) — now a note naming the address and the window in which it is legitimate; and all four resources inherited **`CostCenter = stage-03`** from the slice's `default_tags` while being Stage 4's, a slice-level default being unable to tell two stages apart inside one slice now that `foundation/` holds both. Fixed in the same sitting with a per-resource override as the whole of the difference (`0 to add, 3 to change, 0 to destroy`, in-place, read back **from AWS** rather than from Terraform). **Fifth: steps 4.3 and 1.4, 2026-08-17 — the key enrolled by the user (the one step in the entry Claude did not perform, confirmed only through the secret's metadata and CloudTrail) and the host applied**, 9 resources, the instance taking **11m13s across 13 `InsufficientInstanceCapacity` refusals** that the provider's own retry outlasted — `zone_index` was never needed. **Three verifications answered**: (i) YES in 35 seconds, so Stage 3's 9.3 allow-list is complete and Stage 3's (iii) closes with it; (viii) audit half confirmed (`GetSecretValue`, management event, the instance role named) but the fetch took **zero retries**, leaving the retry loop still unexercised; (iii)'s endpoint half YES over `ssmmessages`, its laptop half still step 3's. **Its findings are three, all outside AWS**: the confirmation plan wanted a **permanent replacement loop** — `associate_public_ip_address` read back from the address the `aws_eip_association` gave the host, `ForceNew`, so every apply would rebuild the tunnel (fixed with `ignore_changes`, measured on the cached module and the cache restored byte-identical); that plan was **first run through `| tail`, so `$?` was `tail`'s** and reported a clean 0 over 2-to-destroy (Lesson 13 from the other side); and the documented `user_data`-is-a-SHA-1 claim is **false on provider 6.60.0**, which stores the script in plaintext — the claim that mattered, *no key in it*, survives and is now the whole of it. **Sixth: steps 4.1 and 4.2 — `mbp` and `raspi` enrolled** (keys generated by the user on each device; only their public halves crossed, and the only possible check on them was structural), the roster written with authored `host` numbers, and the apply that publishes a peer: `2 to add, 1 to change, 2 to destroy`, this time with **no capacity refusals at all** against 13 four hours earlier. **Its finding is a confirmation rather than a defect, and it is the design's central one**: the rebuilt host came back with the **same interface key and the same address**, so `Endpoint` and the server's `PublicKey` — the two values every client config pins — did not move. Decision 4 exercised rather than argued. Also verified: `/32` per peer, and the handshake log already shipping to CloudWatch **with device names**, which closes verification (i)'s residual for step 7 and confirms the `peer=unknown` drift alarm from the other side. Same sitting: **the keys runbook gained procedure D** (a client rotating its own key, the path by which any public half reaches the server) with the `wg set` stopgap named as one, hand-editing `wg0.conf` added to Never, and §§4-6 renumbered — and **`./aws/vpn.py` gained `--on-host`**, an SSM Run Command reading of the running host (which peers `wg0` actually holds — the one question no `describe` answers). **Typed rather than default**, because every command it carries is a read but `ssm:SendCommand` is a write API: it is now the **second** fenced exception to the `aws/*` read-only rule, in `CLAUDE.md` and `aws/INDEX.md`. Its `wg show all dump` ban is a gate rather than a comment, and was proven to fire. **And the tunnel carried traffic for the first time**: `mbp`'s client config (full tunnel, both families) handshaked at 12:15:17Z, flipping the log line from `handshake=never` to `handshake_age_s=243` **under the device's name** — which exercises the observability chain end to end, link by link, for the first time. The age then tracked wall-clock across six samples with no reset; the user had brought the tunnel down after the test, and the entry records why that reading and *a live client whose packets stopped arriving* look identical here. Step 5's other two proofs — the laptop's public address becoming the EIP, and a VPC-resolver-only name resolving — are **not** recorded, and step 8 rests on the first of them. **Seventh: pass 2 opened — step 6.1 answered, and the reachability target back up without its egress.** `./aws/networking.py` **0 FAILED**: `NT-4` clean, and §9 showing **exactly one world-open ingress rule in the whole measured estate** (`awsds-sandbox-vpn`, UDP/51820, the five other measured accounts empty) — the shape `VP-3` enforces, and the first sitting in which the estate has anything at all to show. **Two readings from the same run are preconditions rather than the step, and were checked before the apply rather than discovered by a failed curl**: `NT-8` confirming `prod.internal` is associated with the **Sandbox** VPC, without which the pair is unrunnable; and the Sandbox **public** tier's route table — where the WireGuard host sits — carrying Production's two **private** /18s across the peering and **nothing** for the isolated /20s, the asymmetry that makes the forbidden address a control rather than a coincidence, and it lives in the route table, not in a security group. **The stage names `make up ENV=production`, and reading the plan changed the act**: `make up` has no per-slice filter, and `production/egress` plans **14 resources** (a NAT, its EIP, two default routes, ten interface endpoints) of which **none is in this reading's path** — `production/probes` reads only the `[P]` `production/foundation`, and its user data runs `mkdir`, `echo` and `nohup python3 -m http.server` with no `dnf` anywhere. So the slice was applied **alone, 8 resources**, and the deviation costs the machinery nothing: `make down ENV=production` destroys every `[E]` slice in the account however it went up, so the "nothing is left running" guarantee still holds. **Its provenance is split and the entry carries both halves' commands**: Recipe A followed as written, the plan saved outside the repository and read by Claude, **the apply executed by the user** after the authorisation had been given — applying the saved file rather than re-planning is what keeps it one act, and is the only reason a split operator is recordable. **The boot settled the deviation rather than leaving it argued**: cloud-init finished in **14.87 s in a private tier with no default route at all**, listener bound to `0.0.0.0:443` so one process answers on **both** addresses; the trailing SSM-credentials error is expected twice over (no instance profile **by design**, so the perimeter statements are judged anonymously, and no `ssm*` endpoint in Production — the console is the reading path the slice's own outputs prescribe). **The negative control was checked for being a control** (Lesson 26): both interfaces `in-use`/`attached` at device index 0 and 1 carrying the **same** security group, and the two A records resolving to exactly those addresses — same host, same process, the route the only variable. **No behavioural reading is in this entry**: the tunnel-down half, the three step-5 proofs, the pair and its second control (the admitted address on an unadmitted port, which fails in a different place) are all still owed; `raspi` still has no configuration, and the argument for closing that before 8.3 is 4.1's own — an enrolled device that has never handshaked is an enrolment, not a proven second way in. The `make down`/`make up` lifecycle pair is deferred to its own sitting because **`make up` has no `[D]`-only filter either**, so exercising the host's stop/start would drag `sandbox/egress` up with it. **Eighth: pass 2's readings — the tunnel routes, and the MTU that made it look like it did not.** The standing rule finally arriving: the readings, commands and fix are **the user's own**, from the laptop over phone tethering; Claude was asked in the text to explain step 4 and added that, the flow-log measurement behind it, and the finding. **Step 5 is complete and pass 3 is unblocked** — `52.89.212.1` from `checkip` (the one proof step 8 rests on, its `aws:SourceIp` matching that address or nothing), the `sandbox.internal` SOA answering, `probe.prod.internal` resolving to the target. **The Deliverables' tunnel pair is complete**: with the tunnel down the name dies at **DNS** rather than at the connection — the right failure, the zone being private — and with it up the listener answers. **Its finding is that a full tunnel with no MTU works until the path stops being Ethernet.** Nothing in the design set one — not the module's `wg0.conf`, not the client template — so `wg-quick` derived it from the local interface minus 80, and tethering presents 1500 while the cellular path carries less: the tunnel came up at **1420**, WireGuard sets DF, oversized packets vanished with no error at either end. **The symptom is graded by packet size, which is why it reads as "the VPN is broken"**: the 148-byte handshake succeeded, both DNS queries succeeded, TLS timed out — and **the two answered `dig`s exonerate the host on their own**, since the VPC resolver replies only to a packet that was forwarded *and* source-NATed into the VPC, so the natural first suspicion (a masquerade on the wrong uplink) was already excluded before anyone opened SSM. `MTU = 1280` on the client fixed it first try; it belongs on the client because the client's path is what varies. **Step 4's two silences are the entry's other measured claim.** Both arrive as `rc=28` and **the exit code cannot tell them apart** — nor can anything else on the laptop — so the claim that they die in different places was read from **Production's VPC flow log** instead: ACCEPT on 443, **REJECT on 8080** (the packet crossed the peering, reached the ENI, died at the security group), and **no record at all** for the isolated address (it never left the Sandbox account, falling through to `0.0.0.0/0 → igw` as an unroutable RFC1918 destination). The REJECT in the same window is what makes the absence mean anything (Lesson 26, Stage 3's argument reproduced from a laptop). **And the source column proves something nobody asked it to**: every record reads the WireGuard host's own address and **`10.90.0.2` appears nowhere in Production** — 1.2's NAT seen from the far side, and why `NT-4`'s "no route for 10.90.0.0/24" is a property of the design rather than a gap. Same sitting: the finding reached **`vpn-client.md`** — the `MTU` line in §1's *template* rather than in a troubleshooting note (Lesson 5), a sixth §0 row marking it the one value derived from the path instead of the design, and a §4 entry leading with the symptom that misleads plus the `ping -D` pair that turns "it was MTU" into a measurement. **Left open deliberately: whether the server should pin its own `wg0` MTU**, which trades against every client's path rather than one. Still owed: `raspi`'s configuration (before 8.3, per 4.1), `make down ENV=production`, and the deferred lifecycle sitting. **Ninth: pass 3 applied — the second device, and the control plane pinned to the tunnel.** *(Written before the apply was authorised and amended in the same sitting once it was, rather than left saying "not applied".)* Two acts in it are the user's and are marked as *reported* rather than observed: `make down ENV=production`, and `raspi`'s configuration and connection — the item 4.1 named as a **precondition** rather than tidiness, since after 8.3 a single-device estate whose one device must be revoked leaves break-glass as the only way back. Its config carried the previous entry's finding into first use: `MTU = 1280` came from the *template*, before the first connection rather than after a failure. **Steps 8.1/8.2/8.3 written, planned, read and applied**: `0 to add, 6 to change, 0 to destroy`, confirmed by address and in the negative (a grep for the Sid across the whole planned state returns those six and no seventh), then **applied from the saved plan file** so what was read is what was applied — `Apply complete! 0 added, 6 changed, 0 destroyed`. Read back **from AWS rather than from Terraform**: `VP-7` flipped from `note` to **`pass`, all six carry it**, with the seventh row still `(no inline policy)` and the check now saying so as its own note. Re-plan `No changes` at `-detailed-exitcode 0`, **the exit code read directly rather than through a pipe** — the fifth entry's near-miss was a confirmation plan run as `| tail`, where `$?` was `tail`'s over a plan holding two destroys. **Every persona set now denies every API call and every console action not arriving from `52.89.212.1`**, live in every account each set reaches; `InfrastructureAccess` is outside it by design and is the recovery path. The address is **written nowhere in the repository** — it arrives through the **first remote-state read in this tree that crosses an account boundary** (the apply is `awsds-infra-identity`, the state bucket is Sandbox), which is the one read that needs a `profile` in its config; since a profile literal may not sit in a `.tf`, it comes from a new `VPN_HOMES` table and a `vpn_homes` **map** — a list from day one, because D35 makes the VPN home a role an account plays. **Its finding corrects the stage file: 8.3's "separate, deliberate diff" is a create, not an update** — `InfrastructureAccess` has **no inline policy at all**, carrying `AWSAdministratorAccess` as a managed-policy attachment, which `./aws/vpn.py` §5 had been printing as `(no inline policy)` all along without anyone reading it as the answer to this question. **Two guards were added that the plan did not ask for**, both against the same silent shape: IAM does not validate an `aws:SourceIp` list, so an address that reads back null or renamed renders as `/32`, matches nothing, and denies every call from every network for all six personas — applied cleanly, reported green, discovered by someone who cannot sign in. A variable validation refuses an empty `vpn_homes`; a precondition refuses an address that is not one (Lesson 13, applied to a control instead of to a check). **Verification (vii) answered with room**: +304 bytes each, worst case `DeploymentManagerAccess` at **4867** against 10240, and the figure measured is the *rendered* document, about a quarter larger than what Identity Center stores. Both batteries re-run after the Production teardown, **0 FAILED each**: `VP-1`-`VP-6` and `VP-9` pass with `VP-7`/`VP-8` correctly reporting `note`, and the run was **copied aside to `aws/output/vpn-before-cycle.txt`** as Validation 2 requires; `NT-4` still clean over 64 routes and §9 still exactly one world-open rule. The two FAILED calls in each are the `awsds-ctadmin` session having no token — a different sign-in, needed only for pass 4. **Step 9.1 delivered as a pointer rather than a copy, deliberately**: the full procedure already lives in `vpn-client.md`, so `README.md` gained only what is genuinely its own — the three roles and which is unverified (INT-16), and why an instance rebuild changes no client config, which is a claim about the `[P]`/`[D]` split rather than a step. **The verification's own finding cost two applies, and the second changed the method.** `logs:GetLogGroupFields` was fixed by deriving the missing actions from AWS's documented console-permission list — three, not the one observed — and the console then failed on `logs:DescribeFieldIndexes`, **which is not in that list**: the method was sound and the source stale, so the conclusion is larger than the action — *enumerating the calls of a console surface is a race that cannot be won.* Replaced, by the user's decision, with the AWS managed policy **`CloudWatchLogsReadOnlyAccess`** on the four sets that run Insights — `4 to add, 4 to change`, each inline policy losing exactly one Sid and **shrinking**, every deny kept and `DenyControlPlaneOffVpn` verified intact at `52.89.212.1/32` in all four, re-plan `No changes` at `-detailed-exitcode 0`. Read **live** before attaching rather than from the doc page: **version 12**, whose latest revision added the `observabilityadmin:` namespace — the argument from both sides at once, since AWS both maintains it and changes it. **Three acceptances are written beside the resource so they are choices rather than side effects**: AWS authors the policy, so a future version reaches these personas with **no diff in this repository** (Lesson 11, taken deliberately — it can only add *reads*, and only *under* the denies); `logs:StartLiveTail` arrives and Live Tail is **billed per minute** with nothing measuring it (Lesson 6 acknowledged, not satisfied); and `cloudwatch:GenerateQuery` arrives, partly undoing the `cloudwatch:` deferral — but not `GetMetricData`, so the console's metrics panel still fails, by decision. `DevEnvStewardAccess` stays outside it (its log grant is a build pipeline, not a console surface) and never failed. **The console has not yet confirmed the second fix** — a clean plan is presence, not sufficiency. Still owed, and the first is owed **now rather than eventually**: **the control-plane pair** — the deny is live but nothing yet measures that it refuses the right thing and permits the right thing, so `VP-7`'s pass is presence and not sufficiency; run per persona set, reading the denial *wording* (Validation 3), and note the precondition the plan does not state — the four persona users exist in the directory but **none has a CLI profile**, so the pair runs from the console or from profiles created first. **Tenth: the control-plane pair complete — five readings, every exercisable set, three accounts, two services; every command and output the user's, pasted verbatim, with account ids and the persona e-mail inside each ARN elided and the elision declared; Claude wrote only the analysis.** **Its first finding is the user's, in review of a draft Claude got wrong**: one `sso-session` shared by five persona profiles would share an *identity*, because the CLI caches the token under the session NAME — switching `AWS_PROFILE` changes what is requested, not who asks — so the portal would refuse at credential-vending time, a **third** refusal in a reading built to tell two apart. `aws/AWS-CLI.md` already carried the rule ("two sessions, because they are two different people"); the layout applied is one session per **person**, with `awsds-scientist` shared by two profiles because that is one human holding two sets. Recorded as an addendum to **Lesson 25** rather than a 27th lesson — the rule existed, and what is new is the second entrance to it: not duration, but a cache keyed coarser than the switch being operated. A second setup finding: four `aws sso login` in a row all authorise the **same** person, because the browser stays signed into the portal — no flag fixes it, and each ARN is the evidence it worked. **The readings, 1-4**: `DataScientistAccess`, `DataScientistProdAccess`, `DeploymentManagerAccess` and `DevEnvStewardAccess` on `logs:DescribeLogGroups`, plus `GovernanceManagerAccess` on `lakeformation:GetDataLakeSettings` — its probe changed because `glue get-databases` succeeds **empty** with no database in the account, indistinguishable from a broken reading (Lesson 13), and a second service refusing under the same Sid in a third account is what proves the `Action: "*"` rather than quoting it. Tunnel up each returns data; tunnel down each returns `AccessDeniedException … with an explicit deny in an identity-based policy` — the wording that names *this* control and not "no policy allows" (the persona lacking the grant), an SCP or the RCP. **The probe was chosen so a refusal would mean something**: every probe is granted to its set, so what was measured is an explicit deny overriding an explicit allow; `sts:GetCallerIdentity` was used only to name the answering role, never as the probe, because AWS documents it succeeding under an explicit deny (Lesson 13). **Reading 5 is the negative control and it fired in the same conditions**: `InfrastructureAccess`, same account and action as a denied reading, tunnel still down — **succeeded**, which excludes the account-wide and network-wide explanations **twice, in two different accounts**, since an SCP or a routing artefact could not tell two permission sets in one account apart (Lesson 26). **Two things fell out that nobody asked for.** The tunnel-down halves **printed the ARN**, so the token stayed valid and `GetRoleCredentials` succeeded off-VPN: `aws:SourceIp` is evaluated **at the service call, not at vending time**, which answers *can a session be minted on the VPN and used off it* with a measured **no** — the mirror of Lesson 24's four-hour-cache trap, the same cache present and hiding nothing. And **`InfrastructureAccess` outside the deny stopped being an intention** (Lesson 5): the sign-in completed off-VPN and the call succeeded off-VPN, so the break-glass-free recovery path the Risks section promised is rehearsed before it is needed. **Verification (vi) answered: yes** — a persona sign-in completed off-VPN, and the chain is the point, all three links measured: the portal sign-in works, `GetRoleCredentials` works, using the credential is denied — so what the portal yields off-VPN is **reconnaissance and nothing actionable**; context for INT-16, not INT-16. **Verification (iv) exercised from the console, tunnel up — nothing broke because of the deny**, and the three things that did were attributed from the wording alone: the console opening in `us-east-2` hit the Region ceiling (`p-umksvu5a` — D37's own measurement arriving through a new path; the client instruction is "console on `us-west-2`"); `cloudwatch:GetMetricData` was **never granted** to any set (`logs:` is not `cloudwatch:` — left ungranted by decision, revisited at Stage 6 with a real workload in front); and **`logs:GetLogGroupFields` is a real grant defect** — four sets carry Logs Insights, one Sid literally named `ReadCloudWatchLogsIncludingInsights`, and **three** read actions are missing rather than one (`GetLogGroupFields`, `GetLogRecord`, `DescribeQueryDefinitions` — checked against the documented console list, not the error; Lesson 14 at the smallest scale) — applied in the same sitting, and then applied a second time by a different method, below. **The error's FORM is a retroactive criterion**: Logs Insights writes its own non-canonical text, so a wording-read pair must pick its probe *for producing the canonical wording* — `DescribeLogGroups` satisfied it by luck as much as judgement. **One unrelated reading, routed rather than argued**: Data Governance shows `DataLakeAdmins: []` and both create-default permissions at `ALL` for `IAM_ALLOWED_PRINCIPALS` — written into INT-11 and `AWS_STATE.md` in the same sitting, turning Stage 5's first LF apply into three ordered acts. **The pair is complete for every set that can carry it, and `VP-7`'s `pass` stopped being presence alone.** Still owed: **`DataScientistStagingAccess`, which cannot be measured at all** — it carries the deny but has no assignment, `Staging` being unvended, so five sets of six are exercisable and the sixth is an absent negative control until the vend. Then **the seventh set's create — DECLINED by decision (option a, open question 17, 2026-08-17)**: writing it surfaced the deadlock the Risks row predicted — a stopped `[D]` VPN host can only be started by a credential the deny would pin to the stopped host's own address, making break-glass routine — so the recovery path stays off-VPN, `./aws/vpn.py` `VP-7` now **fails** if the seventh set ever carries the Sid, and the trade has an `institutional-delta.md` row (the admin credential doubling as the fire escape, two jobs an institution separates). **The entry carries its gates**: `make check` OK, `ruff` and `terraform fmt` clean, the battery `0 FAILED` — with the caveat that its `some calls FAILED` banner counts *profiles without a token* (the two `awsds-ctadmin-*` and the five persona sessions the pair itself logged out of) and is never the verdict. Then the lifecycle cycle — whose trap is now named: `make down` stops `vpn` **last**, so with a full tunnel up the rest of that same command routes into a dead tunnel — and all of pass 4. **Eleventh, by the user: the plugin installed and verification (iii) closed on its laptop half**, a Session Manager shell opened **off-VPN** — which is the property the deadlock of open question 17 rests on, a shell that needed the tunnel would make the recovery path circular. Two findings from inside the host: **`wg show` matches the tracked roster key for key and address for address, with no third peer** — the read no describe call can produce, and the reason `--on-host` exists, obtained here without `ssm:SendCommand`; and **`wg0` sits at MTU 8921**, exactly the VPC's jumbo 9001 minus WireGuard's 80, *inherited rather than chosen* — so pass 2's pin lives only on the client, and the server side works by fragmentation. Owed: a decision on an explicit MTU, blocking nothing. **Twelfth: the lifecycle cycle, run by the user and verified by Claude — both validations answered, and the three `cp` copies the step asked for were never made.** What rescued it is that the cycle ends *down*: stopped-with-the-address-attached is the state the missing middle reading wanted, so it was read from the API instead. **CloudTrail reconstructed the whole timeline** — stop 22:38, start 22:40 (*same instance id*), readings, teardown — including the event that looked like the validation failing: a `DisassociateAddress` 65 seconds before the host stopped, settled by two fields as **the NAT's** address released by the provider, which incidentally makes `layers.py`'s rank order visible from outside the tool that implements it. Validation 2's diff is **one line, the instance state**; verification (ii)'s residual is closed in the present tense (`52.89.212.1` associated with a **stopped** host, and no `AssociateAddress` anywhere in the window); Validation 1 landed in its **stronger** form by accident of timing — `networking.py` ran six minutes before the teardown, with the NAT active and 66 routes against the previous 64, so *exactly one world-open SG* was counted with the `[E]` tier mounted. Lost: only the "before" file, whose content survives as **transcript rather than file** and is identical. **The lesson: a validation whose evidence is a local copy depends on somebody typing `cp`, while the audit trail cannot be overwritten in place** — and it answered two questions better, timestamping the transitions and naming the caller. ~USD 0.06, estate back to 0.0000/h. **Thirteenth: the server's `wg0` MTU pinned at 1280** — the question pass 2 left open and two later readings re-raised, closed by measurement. The apply carried **two** replacement causes and only one was ours: the SSM AMI had moved the evening before, so both ids are recorded, a boot failure otherwise having two candidates. Boot read from the console: **42 seconds**, and **Claude's prediction that this boot would finally exercise the key-fetch retry loop was wrong** — two seconds, zero retries, because step (3) sits behind a 36-second `dnf`, so the EIP association always wins that race and verification (viii)'s residual is **effectively unexercisable on the normal path** (a finding about the verification, not the code). The control is measured at both ends and locally, without the internet's cooperation: `ping -M do -s 1253` returns `message too long, mtu=1280` and `-s 1252` does not — one byte apart, the kernel printing `1252(1280)` itself — with the client's mirror image from the laptop; `ens5` at 9001 closes the arithmetic that **8921 was 9001−80**. **The negative control stayed inconclusive and its reason is now measured rather than guessed**: the local link is **1492** (PPPoE), not the 1500 first supposed, so an unpinned client is *eight bytes over budget on an ordinary home line* and survives only because the router returns `frag needed` — which reframes what the pin buys as **not depending on ICMP**, the likeliest thing pass 2 actually hit. `VP-2` confirms the address survived the replacement, which is what all six persona sets are pinned to. `vpn-client.md` updated in three places (the symptom should now be **one-sided**; both directions stalling is a finding about the host) and `terraform-changes.md` §6 gained the `-backend=false` credential trap. Left up at 0.0042 USD/h. **No close-out entry yet: the stage's 2026-08-18 close — the GuardDuty split — lives in the stage file's Status row, and the close-out log entry is the user's to write.** **The addendum gained a later section the same day (Claude, read-only):** the `CLAUDE.md` bullet describing the amd64 move was **stale in the commit that introduced it** — it said *in CODE, not yet in AWS*, *needs its two-commit tag*, and *a running `t4g.*` is that pending apply* while the same commit message said `APPLIED`. A bullet born stale is worse than one that goes stale, since nothing later disturbs it; corrected from a **re-measurement** (`t3.medium`, 64 GiB, `running`, `vpn.py` 0 FAILED) rather than from the commit message that produced the error. **The estate is still up**, so both baseline departures still understate the burn. |
| Stage 5 — Data foundation | [log-stage-05-data-foundation.md](log-stage-05-data-foundation.md) | **Thirty entries, 2026-08-18/20 — all Claude's on the user's request except the thirtieth's AWS acts, which are the user's own hand, with every merge and apply-commit the user's (the PR-18 merge in the eighth, the pass-2 commit in the tenth, PR #19's merge in the eleventh, PR #20's in the thirteenth, `c33eaa0` in the fourteenth): readings, all six decisions, pass 1 authored, APPLIED, merged and generalised, pass 2 authored AND APPLIED, pass 3 APPLIED, the findings propagated into the stages that had not run yet, pass 4a/4b APPLIED, pass 4c APPLIED, the security-zone withdrawal APPLIED, and the 4d amendment APPLIED with the four proofs it was blocking.** **First:** The stage opened with **INT-11's before-reading** (`DL-5`: `CROSS_ACCOUNT_VERSION=4, SET_CONTEXT=TRUE`, a third confirmation, taken while `data-governance/` still holds only `bootstrap/`) — and the same call carries the finding that **decides the order of the first apply**: `DataLakeAdmins` empty and both default-permission blocks granting `ALL` to `IAM_ALLOWED_PRINCIPALS`, which act *at creation time*, so the first `aws_lakeformation_data_lake_settings` owes three things at once — name the admins, carry `parameters` written from the reading, empty both defaults — **before any database exists**. Baseline for the rest: `DL-1`/`DL-4`/`DL-8`/`DL-11` pre-stage notes (Security Hub enabled nowhere is verification (ix)'s "before"), `DL-10` pass, no RAM share, `0 FAILED` — the `some calls FAILED` banner being seven tokenless profiles, entry ten's footprint again. **Also answered, by reading rather than by measurement: this stage does not need the Sandbox environment up** — passes 0-3 run in Data Governance, and the two Sandbox inputs (the `foundation/` gateway endpoint, the WireGuard EIP) are `[P]`, which is INT-05's rule paying off; only the persona-session proofs of pass 4 need the tunnel back, and `make up ENV=sandbox` costs ~USD 0.17/h against the tunnel's ~0.0042 because `slices.py` has no slice target. Estate at the time: only `sandbox/vpn` up. **Second: decision 4 taken, the user's — Iceberg maintenance is Glue automatic compaction** (the optimizer runs D27's carve-out names), Athena `OPTIMIZE`/`VACUUM` declined — so the 4.3 amendment putting `athena:StartQueryExecution` into `DenyUserCompute` is now owed via battery phase 4b, sequenced after any Athena-borne sample-table load; propagated to the stage file, `POLICIES.md`, Stage 1c's annotated bullet and `institutional-delta.md`, with Stage 11's rule already conditional on the outcome. **Third: decisions 1-3, the user's — the ontology renamed and extended (`zone`→`layer` gaining a `dropbox` value, `domain`→`businessunit`, new `security-zone` key) and [`docs/GOVERNANCE.md`](../GOVERNANCE.md) created as the one copy**, from the user's draft, in English per the repo rule: classification `public/internal/restricted/personal` with the default grant `classification ∈ {public, internal}` read-only; **`raw` default `internal` — fail-open, the user's call against the recommendation** (curated fail-closed by absence); **one `zn-lab` CMK for the whole lake, the drop-box sharing it** (the KMS-layer cost named; revision trigger a second zone); the two persistence gaps answered (`logs` = Stage 11's trails; `artifacts` has no writer yet); `AWS_STATE.md` seeded with the empty Lake Formation grant register. Hyphens standardized over underscores in the same sitting (`security-zone`, `zn-lab`). **Fourth: decision 5, the recommendation adopted whole** — admins = `InfrastructureAccess` only (the GM by specific grant, never admin — Lesson 9/D31; revision trigger: Stage 6's DataZone fulfilment principal), shares to the two named accounts (OU revisited at Stage 14), LF-TBAC default with 7.1's catalog-resource-policy prerequisite read before the first grant. **Fifth: decision 6, the grain reframed by the user — entitlement to roles/projects per the toolset's practice, per-user as mapped exploration rather than a target** (the objective met at that grain, `GOVERNANCE.md` §"The grain"; verification viii becomes the map; open question 13's Stage 5 half answered). **Pass 0 closed — all six decisions taken 2026-08-18.** **Sixth: pass 1 authored — `data-governance/data/`, eleven files, `58 to add, 0 to change, 0 to destroy`**, registered in the D11 machinery (18 declared, 18 on disk); the cross-account remote-state reads resolved (each consumer's `[P]` gateway endpoint, the VPN home's EIP) and the LF admin resolved by pattern. **Its finding changed how the stage applies:** the 5.2 obligation — empty both `IAM_ALLOWED_PRINCIPALS` defaults before any database exists — **cannot be stated in the plan** (both blocks Computed → `after_unknown`; `= []` refused; a `{}` block declares one entry, not zero), so omission is the only form and clears-vs-leaves-alone is unprovable → **the apply became two steps**, settings alone under `-target`, `DL-5`/`DL-6` read, revoke if needed, then the rest. Also: `s3-bucket` bumped to **v0.2.0** (`list(any)` cannot hold heterogeneous statements) — Recipe B, tag before caller commit. **Seventh: pass 1 APPLIED** — Recipe B run (tag confirmed on origin; commit 2 blocked once by a stale local module install, `Module source has changed`), then the apply in its two steps: settings alone (`1 added`), **the reading between them — `DbDefaults: []`, `TableDefaults: []`, `Parameters` still 4/TRUE — so omission CLEARS**, then the remainder (`56 added`), a crawler failure (`glue:GetSecurityConfiguration`, a role must read the security configuration it runs under) and its fix (`2 added, 1 changed`); re-plan **`No changes`**. **The claim verified per database, not inferred: `raw`, `curated`, `dropbox` carry NO `IAMAllowedPrincipals` grant — born governed.** Two gate findings folded in: `CKV_TF_1` skips per convention, and a **Glue security configuration** the stage text never had (a crawler samples object contents, so its logs encrypt under the lake key), with all three modes naming the key after `CKV_AWS_99` refused the half-disabled draft. `DL-1`–`DL-6` and `DL-10` pass. **Eighth: merged, synchronised, and the session's findings promoted.** `main` at the merge commit, the branch deleted, `s3-bucket-v0.2.0` re-confirmed on origin at the hash it was pushed with, and a post-merge `plan` reading **`No changes`** — the check a fast-forward does not give, since the applied branch and the merged branch are only the same object until somebody rebases one. **[Lesson 27](../plan/lessons.md) added**: a plan is silent about the values the provider owns, so the setting that must be right *before anything else exists* is the one Terraform will not promise — Lesson 13's failure moved into the artifact that authorises the apply. It names the exposed class (**account-level settings singletons**, which create nothing and overwrite state AWS initialised) and keeps the read-back, omission-clears being one provider version's measured behaviour. **Writing it corrected the discriminator**: `terraform providers schema -json` marks `computed` on attributes and **never on `block_types`**, so the instrument is the plan's `after_unknown`, and only on a create-or-update plan (a `no-op` returns `{}` — verified against the post-merge plan). **The terraform-changes runbook gained Recipe D, the staged apply** — `-target` as a measurement, with the two preconditions, the check, and step 1 being *write down what reading would make you stop*; §8's flat "never `-target`" reworded to name Recipe D as its only sanctioned use, so the two pages stop disagreeing. Recipe B gained the local-`source` authoring step and its **revert *and* re-init** pair (only the second touches `.terraform/modules`), with a matching blocked-commit row; the `checkov` row now says an accepted gate finding **re-enters the recipe**. Two findings deliberately **not** promoted, criterion stated: `glue:GetSecurityConfiguration` (self-announcing — the error names the action) and the bucket-policy `Principal` validation (anticipated in authoring, not learned by failing). **The stage itself did not advance** — passes 2/3/4/6 untouched, 4.3's amendment owed, crawlers still never run. **Ninth (2026-08-19): pass 2 authored, `9 to add, 0 to change, 0 to destroy` — NOTHING APPLIED.** `governance.tf`, the first grants to a **human persona** and decision 5's second half delivered: `ASSOCIATE` on the three LF-Tag keys, `DESCRIBE` on the three databases and on their tables by wildcard — **no `SELECT`, no grant option** — implementing the permission set's own unread one-line description, *"The catalog, never the rows."* Principal resolved by pattern after confirming the role is provisioned here. **6.2's reading comes back in the strongest form the question has: every `s3:` mention across the six persona sets is inside a `Deny`; no `s3:Get*` Allow exists anywhere** — so D13's premise holds **by absence rather than by exclusion**, which is what D22's account split bought (the drop-box `PutObject` is a bucket-policy grant, correctly absent here). **The finding: the IAM half already existed and granted the persona nothing** — `AdministerLakeFormation` permits the API *call*, an LF `ASSOCIATE` decides whether it *succeeds*, the halves sit in different accounts/slices/stages, and a missing LF grant shows up as an **empty catalog listing** rather than an error (the reverse direction has no symptom at all) → **[Lesson 28](../plan/lessons.md)**, with the mitigation shipped as a comment at *each* end. **Verification (viii) answered as decision 6's map** (`GOVERNANCE.md` §"The grain"): LF filters attach to the role, TIP is the only per-person surface and costs remote access (reachable, not adopted), `${aws:userid}` prefixes govern copies — *expressed* answered, *observed* not claimed. **Deliberately unasserted**: what a non-admin must hold to *grant* data permissions via an LF-Tag expression — the LF pages returned no body to an automated fetch (one attempt answered from memory and was discarded), so `REFERENCES.md` carries the provenance and Stage 6 settles it **by measurement**; if granting requires holding-with-grant-option, D31's separation lives in the IAM deny, not in Lake Formation. Repository: **the slice README rewritten as an index of controls in `POLICIES.md`'s discipline** (one row per `Sid`/tag assignment/grant — settings, ontology, the four key-policy statements, the perimeter's two `Sid`s with three branches and two carve-outs, the drop-box's four, both service roles, pass 2's grants), the code-vs-deployed split named so a staler second answer cannot grow. Register **unchanged** — rows are written in the sitting that grants, not the one that authors. No behavioural proof: it is a claim about the pair and needs a GM session, hence the tunnel. **APPLIED the same day: `9 added`, re-plan `No changes`, `./aws/datalake.py` `0 check(s) FAILED`** (the commit was the user's, `6440c6a`, so no branch or PR from Claude; a repeat apply was refused as a stale plan, the runbook's guard working). **`DL-5` re-read after the apply and the INT-11 bracket holds** — this pass applied into the slice that owns the settings resource, whose failure mode is a silent parameter reset. **The claim verified against the API rather than the code:** `list-permissions` on the principal returns exactly nine rows and nothing else — `grant_option=NONE` on every one, and **no `SELECT` anywhere, established by exhaustion**, so Stage 2's one-line description of the set (*"The catalog, never the rows"*) is now true of the Lake Formation half too. Register: **3 rows covering 9 triples**, and its note corrected (the first consumer grants arrive at pass 3, not 2). Still owed and structurally so: whether the persona can in fact tag — `awsds-governance-data` is one of the seven tokenless profiles and the set carries `DenyControlPlaneOffVpn`, so the proof needs a sign-in as that persona with the tunnel up. **Eleventh — pass 3 APPLIED:** the two cross-account shares, `4 added`, re-plan `No changes`, four `LakeFormation-V4-*` shares `ACTIVE` and held by both consumers with **zero invitations** (INT-11 closed). **The entry's substance is what changed before the apply:** AWS's Lake Formation pages read fine through a *rendering* browser, which retired the previous entry's provenance caveat and answered pass 2's deferred question (the persona **tags and does not grant**) - and two things about the decided form were wrong, the grant option being mandatory on every cross-account grant, and the default expression needing a `layer` gate or it shared the **drop-box**. Also: the 7.1 catalog-policy prerequisite measured **conditional and not applicable** (no Glue resource policy exists), `DL-7` rebuilt to separate two opposite causes it used to report identically, and the finding that **each consumer account needs its own data lake administrator** before a share is visible there - which is now step 8's opening prerequisite. Lessons **29** and **30**, open question **18**. The entry also closes the previous sitting: **PR #19 (pass 2's records) is the user's merge**, synchronised locally, records-only so no post-merge re-plan was owed. **Twelfth — the propagation, and the only entry here with NO AWS call in it**: where each of passes 1-3's four findings lands in the stages that have not started. Two of the landings were **defects rather than additions** — Stage 9's post-grant reading expected a catalog that cannot answer yet (Production has no data lake administrator until a later pass), and three owed acts in this stage's own pass table had **no owning pass** (the crawler pair, the drop-box halves, 4.3's amendment, now a numbered debt list on pass 4 with the amendment last). Plus one finding that had been sitting unread in the applied state: **`sample_trades` has no rows**, so verification (x) answers by column list and **Stage 11's row-filter proof now depends on Stage 9 having written real rows**. And one instrument gap written down rather than fixed: **`DL-6` is Data-Governance-only**, so nothing would read the create-defaults in the consumer accounts pass 4 gives them. Records across both stage indexes, five stage files, `GOVERNANCE.md`, `GLOSSARY.md`, `cost-model.md` (the KMS count settled at one lake CMK, 9 → 10), Recipe D's forward schedule, `terraform-live/README.md` and `aws/INDEX.md`. **Thirteenth — pass 4a/4b APPLIED, and the sitting's finding arrived during the *preparation*, before a line of Terraform.** The before-reading of both consumer accounts found `DataLakeAdmins: []` **and** `CROSS_ACCOUNT_VERSION=4 / SET_CONTEXT=TRUE` **and** `IAM_ALLOWED_PRINCIPALS` on both create-defaults — so **INT-11's reset hazard is symmetric**, not the producer's peculiarity, which nothing in the plan had said. Worse, **`DL-6` was reporting `pass` while two accounts sat in exactly the state it exists to fail** (it read `DATA_PROFILE` alone) — the debt pass 3 wrote down, paid here: both checks now report per account, and in a consumer `DL-6` deliberately carries **no *databases exist* guard**, because the defaults act at creation time and the first local object is the resource link. Built: `terraform-modules/consumer-data/` **v0.1.0** (the tree's first slice-pair that is one module applied twice, and its first **nested** module-by-tag) plus `s3-bucket` **v0.3.0** (it could expire only *noncurrent* versions, which reaches nothing never overwritten — `DL-9` needs the other one), through a **Recipe B chain of three commits**. Applied per account in Recipe D's two steps — `1 added`, the reading between, `15 added`, re-plan `No changes` — with the precondition re-measured (`after_unknown: true` on both default blocks, as at pass 1) and **the reading failing correctly once**: `DL-6` reported FAILED for Development while its turn had not come. Three design points settled in the authoring: **`scratch` is a PREFIX, not a bucket** (D13's own wording is the origin — *"non-registered prefixes (scratch, artifacts, model outputs)"*; the three files calling it a bucket all credit D19, which never mentions it), **the CMK is per (zone × account)** — the user's call, `alias/awsds-<env>-zn-lab`, with the lake's own key declined across the boundary because `AllowProductionPickupDecryptViaS3` carries no bucket scoping — and **the key policy delegates administration to root and no cryptographic action**, so no IAM policy in the account can grant `Decrypt` behind D31. Measured after: `0 check(s) FAILED`, `DL-7` at *4 shares out, 4 resource links, no pending invitation* (**verification (v) closed in both halves**), four re-grants per account verified by `list-permissions` with **no grant option anywhere** on the persona, and one applied triple nobody wrote (each `InfrastructureAccess` holds `ALL` on the links it created — now a register note). **Verification (x)'s exclusion half answered one layer earlier than the plan expected**: `counterparty` shows in Data Governance and **not** in either consumer, read as each account's own administrator, with the negative control in the same reading — the `restricted` column never crossed the account line. Register **13 rows / 25 triples**. Owed and named: **4c** (the persona grants, deliberately sequenced after the slices so the ARNs can be enumerated from their state instead of wildcarded), **4d** (every behavioural proof, all needing the tunnel), **4e** (the SCP amendment, last), pass 6. **A second review pass followed the first propagation and found seven misses, two of them in the rows that OWN the changed claim** — INT-11 still described the `Parameters` reset as a Data Governance property, INT-03 still said the consumers had no administrator, `aws/INDEX.md`'s `DL-5` row still scoped the reading to `data-governance/data/`, Recipe D's schedule still listed all four accounts as pending, `GLOSSARY.md` had no **resource link** entry, D19 carried no revision note, and `terraform-modules/README.md` still opened with *"Empty on purpose today"* beside seven modules. **Lessons 31 and 32** came out of that re-reading rather than out of the apply: a check inherits the scope of the account it was written in (deliberately *not* Lesson 13 — this one discriminates perfectly and looks like evidence), and two spellings of one object survive while nothing has to build it, the tie-break being *follow the citation*. Branch `claude/stage-05-pass-4`, four commits, both tags on origin; **[PR #20](https://github.com/felipenoris/AWS-DataScience/pull/20) merged by the user**, synchronised, and the post-merge re-plan run: **`No changes` in all three slices** (both consumers plus `data-governance/data/`, since a module-touching merge could have moved the lake too) and the instrument all-pass. **The merge was a REBASE, which is exactly what pass 1's warning was about**: every hash was rewritten, so both tags now point at commits that are **not ancestors of `main`**. Nothing is broken and doing nothing is correct — but that was *measured*, not assumed: each module's **tree hash at its tag is byte-identical to `main`'s**, and `terraform init` resolves content rather than position. **Recipe B gained the check**, because it recurs on every merge and the reflex is counter-intuitive — expect `orphaned`, verify the tree hash, and never move a tag. **Fourteenth — pass 4c APPLIED, and it turned a scoped step into a defect report.** Scoped as *Athena + the derived zone*, the authoring produced a third statement family and a correction: **the drop-box write is cross-account, so pass 1's bucket policy was only ever half of the permission** — and step 6.2 had asserted **in writing** that the missing identity half was *"correct rather than missing"*. The reading behind that sentence was accurate; the inference was not, and its failure mode is an `AccessDenied` **naming the half that is right**. Applied as seven statements in `DataScientistAccess` — the Athena run family on the two workgroup ARNs (`primary` absent, and that absence is the denial), the derived zone's read/write/delete scoping (write per-`${aws:userid}`, read at the persona grain, **delete in `scratch/` only** — `results/` and `derived/` are deleted by the 30-day lifecycle and nothing else), and the drop-box's identity half with its KMS pair. `1 changed`, re-plan **`No changes`**, and the reading that matters taken in **both consumer accounts** rather than in Identity: a set becomes a role in every account it reaches, and `get-role-policy` returns all 18 statements with the `${aws:userid}` policy variable intact. One Stage 2 ledger line **corrected rather than delivered** — *"s3:GetObject on the governed lake through the share"* would be D13's own bypass. **Lesson 28 AMENDED rather than duplicated**: the intersection has a second trigger that is not a service at all — the **account boundary**, where one IAM's evaluation rule goes from OR to AND, which is why a lesson that already listed *"an S3 bucket policy"* did not fire. **`DL-12` added**, reading the identity half off the provisioned role, so the question that got past a review, a plan and a gate is now asked mechanically (pass in both accounts, `0 FAILED`). **The propagation sweep came back closed**: four foreign-principal statements exist in the estate, the writer's half applied here and the Production pickup's already correctly specified at Stage 9 step 3.1 — the defect correlates with the **distance** between the two halves, not with the concept. INT-10 amended (it described only the resource half); `CLAUDE.md`'s Stage 5 bullets consolidated to its own budget rule in the same sitting. Committed by the user as `c33eaa0`. **Fifteenth — the `security-zone` dimension WITHDRAWN and applied away, one day old, and the decision arrived through a conversation rather than a review:** the user worked through the bucket/CMK model as verification questions until the premise broke — *"achei que a chave CMK estava associada a uma LF tag"* — and **no such mechanism exists** (a tag gates TBAC on catalog objects; a CMK is bound to a bucket by Terraform; the link was the shared `zn-lab` spelling, and the tag appeared in **no TBAC expression at all**). Replaced by **one data CMK per account** (`GOVERNANCE.md` §Encryption, new, the one copy; §`security-zone` deleted). **What survived the wrong premise was checked rather than assumed:** the framing died, the *measurement* that declined the lake's key across the account line (`AllowProductionPickupDecryptViaS3`, unscoped) stands verbatim, so the applied shape never moved and D31 is untouched. **Four applies, all re-planning `No changes`** — Data Governance `4/3/6`, both consumers `1/1/1`, Identity `0/1/0` — and the mechanic was chosen, not discovered: `moved {}` blocks per module address made the rename an **alias replacement with the key `updated in-place`**, so no object was re-encrypted and the KMS cost never moved. Destroyed: the LF-Tag and the GM's `ASSOCIATE` on it (**the grant register's first removal**, 25 → 24 triples, annotated on its row rather than rewritten). `DL-12` **failed correctly in the before-reading** — the instrument was edited to the new Sid before the apply existed, pass 4a/4b's shape again — then passed in both accounts, `0 FAILED`. Recipe B's `Module source has changed` fired once, from the **commit gate** rather than an apply. Then a **four-lens adversarial review** (13 agents) found **six misses, three of them in rows that OWN the changed claim** — `CLAUDE.md`'s routing table, `AWS_STATE.md`'s "nine grants" against its own register, and INT-10 naming a `Sid` that no longer exists, **which no grep would have caught: it contains neither `zn-lab` nor `security-zone`**. Plus `GOVERNANCE.md`'s new §Encryption arguing against a key "that also served state and **logs**" while the lake's own `logs` bucket sits under it. **Sixteenth — pass 4d opened (evening): the host's first-ever start, the user's hand, met `InsufficientInstanceCapacity`** — verbatim error kept; the discriminating read (`t4g.nano` IS offered in the AZ → transient, not configuration) and the successful retry minutes later, `./aws/vpn.py` 0 FAILED with `VP-2`'s EIP reassociation confirmed. The sitting's decision, measured against `layers.py` and both dry-runs: **host-only start, not `make up`** — no 4d proof runs inside a VPC, so the `[E]` slices (0.168/h) stayed down against the host's 0.0042/h. Three topology readings (public subnet → IGW; the route table's two `[P]` gateway endpoints splitting tunnel traffic into `aws:SourceVpce` for S3 and `aws:SourceIp` for the rest, mirrored exactly by the drop-box policy; `source_dest_check` deliberately on) — **and one 4d probe corrected**: the perimeter's `aws:PrincipalAccount` branch means the carve-out pair's negative half must run as a principal from a *different* account. Repository: `vpn-keys.md` + `vpn-client.md` **unified into `runbooks/vpn.md`** (Parts K/C kept whole, new Part S: components, topology, §S5 start/stop with the Name-tag lookup and the capacity ladder), references updated across seven files; the missing `awsds-scientist-dev` CLI profile written down as the next sitting's prerequisite. Every 4d proof still owed — the host was left `running`, waiting for the tunnel. **Seventeenth — 4d's first proof lands, and it is the carve-out pair**, every command the user's and pasted verbatim: `awsds-scientist-dev` added to `~/.aws/config` (local file, no AWS change), then **one `s3api get-object` on `awsds-data-curated` run twice from `<Sandbox Account 1>`'s infrastructure role** — off the tunnel *"with an explicit deny in a resource-based policy"*, on it *"because no resource-based policy allows"*. **Two wordings from one command is what makes it a verification** (Lesson 13): the first is `DenyOutsideTrustedNetworks` with all five ANDed tests holding — including a **negated operator on an absent `aws:SourceVpce` evaluating true**, the mechanism the branch rests on — and the second is the perimeter standing down, leaving Lesson 28's missing resource-side half. `s3:ListBucket` rather than `s3:GetObject` in both, because S3 evaluates it on the bucket to choose 404-vs-403; a `NoSuchKey` would have been the bad reading. Tunnel confirmed by two different claims: `checkip` → the `[P]` Elastic IP (the full tunnel), and the `sandbox.internal` SOA answering (the VPC resolver, so DNS is inside it too). **One thing deliberately not claimed**: the message names no branch, so *which* of `aws:SourceVpce` and `aws:SourceIp` admitted the on-tunnel call is settled by the previous entry's route-table read, not by this one — an end-to-end `vpcEndpointId` proof would need a flow log or an S3 data event, and neither was read. Every **persona** proof of 4d still owed, in both accounts. **Eighteenth — 4d group A, in Sandbox: the persona works, and one designed path is broken.** Every command the user's, verbatim. **Verification (ii) CLOSED IN FULL and INT-11's vending half exercised for the first time**: the first persona Athena query `SUCCEEDED` through the resource link, output at `s3://awsds-sandbox-derived/results/<id>.csv` (D19 practice (i) holding), `ResultSetMetadata.ColumnInfo` carrying **five** names — so **verification (x)'s persona half is answered and the column filter holds at the ENGINE, not only in the catalog**. Two plan predictions corrected by measurement: `EnforceWorkGroupConfiguration` **overrides** the client's result location rather than failing the query (the hijack landed in `results/`, and the target was chosen writable so a failure of enforcement would have shown as a wrong-place write, not a permission error); and `glue:StartCrawler` from a persona is refused by the **identity** policy's implicit deny, **not** by an OU policy — the `Data` OU's SCP cannot reach a persona in `Interactive`, so the D27 carve-out stays unexercised in both directions. **THE FINDING: `DenyControlPlaneOffVpn` fires on every direct S3 call from the tunnel**, so the drop-box `PutObject` — pass 4c's own deliverable — is denied *with an explicit deny in an identity-based policy*. Diagnosed by elimination over the five deny statements, then by control: the same session gets an **implicit** deny from Glue and an **explicit identity** deny from S3, and only the network path differs — the public subnet's gateway endpoint means an S3 request never presents the Elastic IP in `aws:SourceIp`, while Glue and Athena do. Athena's own write to `results/` survives because a forward access session carries `aws:ViaAWSService=true`, the statement's carve-out. **The two halves of the design disagree about a split only one knows about**: the bucket policy has three branches, its identity twin has one. Closed-not-open, but it breaks D18/D25's ingestion path and probably the derived zone's usability — the fix (a third `aws:SourceVpce` condition) binds six permission sets and was deliberately **not** applied. Also recorded: Athena's `primary`-workgroup denial names no policy, so it is a **weaker reading** than the S3 ones and is written up as such. Group B (Development) untouched, and it gains the probe that decides the finding's width: the persona reading its **own** derived bucket. **Nineteenth — group A re-run by Claude's hand** (read-only under the standing rule; the five writes only after the user authorized them by name), **and it settles that width**: the persona is denied `s3:ListBucket` on `awsds-sandbox-derived` — a bucket with no network perimeter of its own, where `UseDerivedZoneBuckets` grants that very action — so **a `Deny` is overriding an explicit `Allow` and the finding is about the S3 PATH, not about the lake's buckets**. The user-facing shape: *the scientist runs the query and cannot fetch the CSV*, since Athena writes the result under `aws:ViaAWSService` and the person reading it does not. Nine read-only readings reproduce word for word (A3's and A4's original execution ids still answer, so those were re-read rather than re-derived); the three denied writes reproduce exactly; and the two that create something were re-run as **fresh executions** — the hijack overrode the client's location a second time, in an execution sharing nothing with the first. Cost: two more CSVs under the 30-day lifecycle; no probe object anywhere, since the drop-box `PutObject` was denied. **Two controls named as NOT taken**: `InfrastructureAccess` on the same bucket (the clean isolation, since 8.3 applied `DenyControlPlaneOffVpn` to the six personas only) and CloudTrail's `vpcEndpointId` beside the Glue event — both blocked on `Token for awsds does not exist`, the portal being signed in as the persona (Stage 4's finding). So the diagnosis rests on three measurements plus an elimination: enough to act on, **not** enough to call measured. **Twentieth — group B, in Development: every one of the ten readings mirrors Sandbox**, writes included (user authorized the group by name). **The row that earns the repetition is the identity**: the provisioned role is a *different* one, so one permission set, two accounts, two distinct IAM roles, both failing identically — which moves `DenyControlPlaneOffVpn`'s defect from "something is wrong in Sandbox" to **a property of the DOCUMENT**, reaching `Staging` at its vend and every D35 Sandbox unit with nobody having acted. The same two-role evidence upgrades the proofs that *worked*: workgroup enforcement and the engine-level column filter now hold from two roles rather than one. **Verifications (ii) and (x)'s exclusion half close on the consumer side entirely.** And one caveat recorded rather than glossed: the pandas half is "answered" but **D13's own mechanism is still unmeasured** — the S3 denial arrives as the VPN deny, which would refuse the call whatever Lake Formation had granted, so it masks the implicit deny D13 argues from and stays owed until the statement is amended. Cost: two more CSVs on the 30-day lifecycle; the drop-box gained no object in either account, so `incoming/2026/08/19/` does not exist. **The persona session has now done everything it can do.** **Twenty-first — the two controls taken as the infrastructure user, and the diagnosis stops being deduced.** The tunnel was confirmed up *first*, so the control varied identity and not route. **Control 1**: `InfrastructureAccess` lists both derived buckets over the same endpoint without error — bucket policy, account CMK, gateway endpoint and network all exonerated, the deny isolated to the persona fragment — and the listing shows **the persona's own query results sitting in the bucket** (8 objects in Sandbox, 4 in Development), which states the cost the way a person meets it rather than as a denied API call. **Control 2 could not be taken as it was named**: the persona's denied `ListObjectsV2` is an S3 *data* event and the trail carries management events only, so no such record exists — the path was re-created instead by a management call of the right kind. S3 over the tunnel presents **`10.20.160.254`** (the host's own ENI, inside `10.20.0.0/16`) and **`vpce-0cc3e139c1167ca83`**; the same session's denied Glue call presents **`52.89.212.1`** and no endpoint; Athena's staging write is recorded with `athena.amazonaws.com` as its origin under the persona's own `sessionIssuer` — the `ViaAWSService` carve-out **measured rather than argued**. So the mechanism is precise where it had been a disjunction (**present, carrying a private address** — not absent), and the elimination over the `Deny` statements was re-done by exhaustion over the documents instead of carried forward. `permission-sets.tf`'s existing `precondition` turns out to predict this exact symptom for the **wrong cause** — it guards a malformed list, never an irrelevant key. **Second finding: the proposed amendment was aimed at the wrong list.** `vpn_homes` holds one row, so every tunnel call in *either* consumer leaves through **Sandbox's** endpoint, and Development's own is not on that path until Stage 6 puts compute in its VPC; the lake bucket policy's `consumer_vpce_ids` therefore works **by luck**, carrying Sandbox's id because Sandbox is a *consumer* rather than because it is the *VPN home* (Lessons 10 and 29 arriving together). Fix shape settled: `StringNotEqualsIfExists` on `aws:SourceVpce` over the **VPN homes'** `s3_gateway_endpoint_id` — no new output, no new state read, `IfExists` holding the polarity — with two consequences stated rather than discovered (it widens the carve-out to anything reaching Sandbox's endpoint, and it covers S3 alone) and **still not applied**. **The regression to carry forward**: D13's own mechanism is not merely deferred but *overwritten* — every attempt is intercepted by the explicit deny, so the implicit deny D13 argues from becomes measurable only after the statement is amended. **Twenty-second — 4d's two authorized acts, and one of them cannot be performed by anybody.** **Act 1**: `glue:StartCrawler` as `InfrastructureAccess` in Data Governance — an account in the `Data` OU — is refused *with an explicit deny in a **service control policy***, so `DenyCatalogMaintenanceRunsExceptMaintenanceRole` **fires for the first time since 1c attached it** (group A had found the persona could not reach it at all, being refused earlier in `Interactive`). But the **positive** half is **unreachable rather than owed**, closed by three readings taken before the write: the SCP admits only the maintenance role *or a service principal*; that role's trust policy carries one statement, `GlueServiceOnly` (`Principal.Service = glue.amazonaws.com`), so **no human and no other service's role can assume it** — it is an *execution* role, never an identity anyone becomes; and both crawlers read `Schedule: null` with zero triggers and zero workflows. **Nothing in the estate can start them**, which is Lesson 22 exactly. **The consequence reaches past the deliverable**: D18/D25's ingestion is `PutObject → crawler catalogues` and **both halves are now broken for unrelated causes**, so amending `DenyControlPlaneOffVpn` fixes half a path — and neither document is at fault, the crawlers' **`Schedule` is simply a decision nobody took**, now open. **Act 2**: a `classification=restricted AND layer IN (curated, raw)` TABLE grant to the **Sandbox account only** took its `sample_trades` column list to **six with `counterparty` present**, while **Development, read in the same minute, stayed at five**; revoked, both back to five. **Granting in one account rather than the two the stage budgeted is what makes it a measurement** — Development is a live negative control excluding time, catalog caching and a stale session, and the two dropped writes were the ones that would have destroyed it. **Closes the classification pair's second half and verification (x)'s explicit-grant half**, at the same account/administrator grain the absent half was measured at; **the persona grain is explicitly NOT measured** (it needs a second, consumer-side re-grant, untouched). Revert verified three ways — column lists at five, `LFTagPolicy` count back at 4, **zero** permissions carrying `restricted` anywhere — and `./aws/datalake.py` reads **0 check(s) FAILED**; its per-profile `FAILED` lines are absent SSO sessions, not lake findings. The `AWS_STATE.md` grant register is unchanged and correctly so. **4d now owes only 4e.** **Twenty-third (2026-08-20) — the sample rows: the one-way door was walled shut all along.** The user chose rows-before-4e (against the recommendation, both costs in view) and the first governed write ever attempted was **DENIED** — the vended `AWSLF-…` session of `awsds-data-lf-registration` naming `kms:GenerateDataKey`; nothing left behind, verified (metadata still `00000-…`, no orphan files). The reconnaissance found **three files with three spellings of one capability**: the role's live policy read-only; the `.tf` comment deferring the write to "Stage 9's step 2"; **Stage 9's file silent** — the promise existed only at the promising end; and Stage 5's item 3 asserting the door open. Nothing had ever exercised the path, so all three survived — **Lesson 34** (Lesson 20's mirror: an unexercised allow-path is exactly as unmeasured as an unexercised deny), and the mechanism side was the true one again (Lesson 32). The rows-now decision surfaced it with one account, one role, one key instead of inside Stage 9's 2.4 cross-account job. **Fix applied 2026-08-20 (authorized)**: a second inline policy `registered-locations-write` — pure addition — `PutObject`+`DeleteObject` on the two registered prefixes (Delete reasoned, not measured: failure-cleanup and VACUUM), `KmsGenerateDataKey` the measured action; plan `1 add / 1 change` (the change read in full: the description string alone), re-plan `No changes`, `GetDataLakeSettings` read back intact (the DL-5/INT-11 hazard did not fire), slice README rows added in the same sitting. **The ceiling framing is the point**: every governed access to the registered locations, from any account, is a session of this role — so this is Stage 9's 2.4 prerequisite delivered early, and its file now carries the dated callout where the amendment would have been owed. **The load**: the identical INSERT `SUCCEEDED` (1.9 s), `count(*)` **12** / 4 counterparties, metadata `00000→00001` (a real Iceberg commit), one parquet + snapshot avros, `./aws/datalake.py` **0 FAILED**. **Stage 11's row-proof dependency on Stage 9 is LIFTED** (synthetic-shape caveat named). 4e is now honest — there is finally an in-account Athena door to close — then pass 6. **Twenty-fourth (2026-08-20) — the review sitting: zero AWS writes, and it paid four ways.** (1) **Three log corrections**: group B's header said *eight* readings over a ten-row table; entry 23's leftover line said one object where the listing says three; and entry 22's "a decision nobody took" was **half wrong — the code knew better**: `maintenance.tf` rejects a standing schedule *on cost* (DPU-hour, 10-minute minimum; `DL-3` checks it) and names the trigger "on-demand" — so 4d's real measurement is that **on-demand has no demander**, and the live candidate was already in the stage as verification (iv)'s event shape (OQ 19, the stage bullet and `AWS_STATE.md` refined to match). (2) **A drift finding**: the first lake plan read `1 to change` — the INSERT's Iceberg commit stamped `iceberg.field.*` onto the live table's columns and Terraform wanted to strip them (**Lesson 23**); `ignore_changes = [storage_descriptor[0].columns]` authored with the argument, slice back to `No changes`. (3) **The 4d amendments AUTHORED and deliberately not applied**: the third `DenyControlPlaneOffVpn` condition (`StringNotEqualsIfExists aws:SourceVpce` over the **VPN homes'** two gateway endpoints each, `IfExists` holding the polarity) plus a second plan-time guard — the existing one predicted the right symptom for the wrong cause — with the plan read in full: **`0 to add, 6 to change, 0 to destroy`**, exactly the six persona documents, both preconditions passing; and the lake's `trusted_vpce_ids` rebuilt on the home axis, **rendering identical today** (`No changes`) so the coincidence stops being load-bearing. The apply is its own sitting: apply → `VP-7` → the two unblocked proofs → then 4e, still last. **No Terraform for the crawler demander, deliberately** — OQ 19 is an undecided input. (4) **The session's AWS CLI written up as a debugging reference** — one table, every command family with its intent and its mechanics: the denial-wording taxonomy, `get-query-execution`'s `StateChangeReason` carrying the denied principal verbatim (the read that moved the diagnosis to the vending ceiling), CloudTrail's management-only scope, the trust-policy read that closes Lesson 22, the DL-5 bracket, and the plan→show→apply→re-plan discipline. **Twenty-fifth (2026-08-20) — the amendment APPLIED and the four proofs taken; three authorized writes.** The saved `0/6/0` plan applied, a second invocation **refused as stale** (Recipe D's guard working), re-plan `No changes`, and the read-back taken on **both provisioned roles** — the defect was the shared document's, so the fix is only proven where it lands; `./aws/vpn.py` all-pass and **`VP-7` both halves** (all six sets carry it, `InfrastructureAccess` does not). Then, as the persona in both accounts: **the exact call that diagnosed the defect flipped explicit → implicit** (`ListBuckets`); a **contrast pair** — one action, two buckets, one session — separating "the network is blocked" from "this bucket is not granted"; **D13's mechanism closed**, the direct S3 path refused *because nothing grants it*, identical in both accounts (while the explicit deny sat on top this reading and a broken-share reading were the same output — **Lesson 13 live**); and the **drop-box**, `PutObject` landing at last (**`AllowInteractiveWriterPutOnly` attached → exercised**, Lesson 20 discharged) with `GetObject`, `ListObjectsV2` and `DeleteObject` all implicit-denied — **D18's refusal of an exchange bucket is now a construction, not a convention**. **The finding: on an encrypted write path Lesson 28's intersection has THREE terms** — identity, resource, **and the key policy** — and the third is invisible in the failure taxonomy (a missing key policy surfaces as a *KMS* error, yesterday's INSERT shape) because it is only legible in the **success** response's `SSEKMSKeyId`; the lesson's wording is owed an amendment. **Residue declared**: the probe object is uncollectable until Stage 9's `awsds-prod-job-exec` exists — `AWS_STATE.md` owes an `EXC` row. **Twenty-sixth (2026-08-20) — the propagation, and four findings the writing itself produced.** (1) **`VP-7` had been reporting `pass` over the defect for three days**: it greps for the *Sid*, and the defect was presence-with-the-wrong-conditions — **Lesson 31**. Amended to read `aws:SourceVpce` too, with a third state `yes, IP only` whose failure text carries the diagnosis; `ruff` clean, **the new branch is unrun** (the infra session had expired). (2) **Debt item 4 said one document and means two** — `DenyUserCompute` lives identically in `Data` **and** `Identity`, and `check-index.py` compares `Sid`s, not action lists, so a half-amendment passes every gate (**Lesson 14**, in the shape `POLICIES.md` itself predicted). (3) **INT-05 was proven by a call nobody nominated as its proof** — the drop-box *write*, not a lake read: every lake bucket carries `DenyOutsideTrustedNetworks`, so the `PutObject` had to satisfy the `aws:SourceVpce` branch and did, through Sandbox's own `[P]` gateway endpoint; the row now also warns that the identity and bucket lists are on **different axes**, coinciding only until Stage 14's second VPN home. (4) **A stale verification row found by reading, not by a gate** — (x)'s explicit-grant half had been answered on 2026-08-19 in the body and not in the row. Also recorded, because it argues against its own decision: **open question 17's exemption is the negative control for the deny it exempts** — `InfrastructureAccess` succeeding on the same bucket over the same tunnel is what isolated the 4d cause, so narrowing that set later removes a diagnostic as well as a bypass. Propagated into `lessons.md` (28 amended, **33 closed** with its proof shape), `AWS_STATE.md` (+`EXC-02`), stages 4 and 5, `integrations.md`, `open-questions.md` 17/19, `GOVERNANCE.md` §Drop-box, `SMUS.md` (the `s3` interface-endpoint row becomes a measurement Stage 6 owes), `POLICIES.md`, and four Terraform files + both lake READMEs. **4e prepared** as a five-step sequence with its cost stated first: after it, nothing in Data Governance can run an Athena query, `InfrastructureAccess` included, so the `count(*)` that proved the 12 rows stops being reproducible there. `make check: OK`; `check-ou` not run (no session). **Twenty-seventh:** the two owed checks, and the sign-in that blocked them. `aws sso login` reported success while every `awsds-*` profile failed `ForbiddenException`/`GetRoleCredentials` on a token cached one minute earlier: **the browser re-approved the persona's live portal session**, and the cache is keyed by **sso-session name, never by user**, so the wrong human's token *occupies* the right one's slot and every later login finds it valid and does nothing — the remedy is the logout. Three instruments were pointing at the inert command; the third is the one that mattered: **`scp-battery.py` records that exact wording as a `NO-CREDENTIALS` floor breach, its most serious finding, and is right to** — on 2026-08-14 it *was* the ceiling (`awsds-org-rcp-perimeter` denying the SAML flow's STS actions in all six accounts). One wording, two opposite causes, so **filtering by the text would have suppressed the real breach with the operator's click — Lesson 24 arriving from the other side, in the function whose docstring cites it.** The discriminator asks a different *system* instead: **IdC's own listing of what the token is assigned, a path that never traverses STS**, with `True`→keep the finding, `False`→stop, **`None`→keep the finding** (hiding a real breach is the expensive direction). Exercised in the wrong-identity state by loading the functions directly — **the battery never ran, no probe fired** — six correct `STOP (operator)` classifications, `dev`/`prod`/`sandbox1` included, where the token *does* hold the account but not the role; without it this sitting writes six phantom floor breaches (**Lesson 30**). Then, as the infrastructure user: **`./aws/vpn.py` all checks passed**, `VP-7` reading "all six carry it, **each testing `aws:SourceVpce` as well as the address**" — the amended branch on the amended policy, still explicitly not sufficiency; and **`make check-ou` OK** — root plus seven OUs, all four sections clean. Written down as an **amendment to Lesson 24, not a new lesson**: the parent's wording fork is necessary and not sufficient, a second text rule would repeat the lesson in reverse, and the separating evidence must come from **a channel the tested mechanism cannot influence** — the rule the parent's own instruments already followed without naming it. Corollary carried: **before a harness records a breach in every account at once, ask what single local thing could produce that uniformity.** `make check: OK`, `ruff` clean on the three amended scripts; nothing committed, six files modified. **Twenty-eighth: 4e APPLIED — pass 4 is closed — and two assumptions broke on the way.** (1) **The item's own procedure was stale by a stage**: "amend both JSONs, then `update-policy` in place" came from `POLICIES.md` and the battery runbook, which describe the **Stage 1c** console-paste world, while **Stage 2 step 5.5 adopted all ten documents into Terraform**. The hand path would have been drift *and* would have skipped the guards — concretely, `awsds-org-scp-ou-data.json` carries `<ACCOUNT_ID_DATA>` inside the **D27 crawler carve-out**, so uploading the tracked file raw leaves an `ArnNotEquals` matching the literal placeholder: the carve-out becomes decoration with **no error at upload and none at evaluation** (**Lesson 11** — Stage 2 changed who authors these documents, invalidating every procedure written about them). Corrected at the step that carried it; the real route is **Recipe A**. Applied `0 to add, 3 to change, 0 to destroy` — the two documents (`p-gl01bcdm` 1028→1057, `p-mmfc17ac` 494→523) plus a **rider declared in advance** by commit `6a5bf33` (the RCP `description`, no `Statement` touched), carried on purpose because excluding it needs `-target`, which the runbook allows only in Recipe D. Re-plan `No differences`, both read back, `./aws/org-policies.py` all checks passed with **`CHK-3` still passing** — the substitution proving itself on the very statement the hand path would have wrecked. (2) **The probe's fork resolved a third way.** Both accounts answered `AccessDeniedException` — so *not* untested, and **Athena authorizes before it validates** for this action — but the message **names no policy**, and the battery's `classify()` has assumed since Stage 1c that a denied call names its document, filing a policy-less `AccessDenied` as `DENY-NOT-SCP`, an IAM deny. Right in general, wrong here, and unfixable by wording. **Attribution moved into a contrast probe**: the same call from `Sandbox 1` (`Interactive`, which the amendment does not reach) got past authorization and died on `WorkGroup is not found`, so the two denials can only be the per-OU documents. `probes.py` gained **three** entries, not two, the third an `allow`; the two denies will read `note` and never `ok`, which is the honest reading — 93 → **96**. *Must still succeed* trio OK in both accounts. Cost paid as predicted: **no Athena in Data Governance at all now**, `InfrastructureAccess` included. Paperwork same sitting — both `DenyUserCompute` rows, and the non-coverage bullet **kept as a blockquote rather than deleted**, because an allow whose justification had been withdrawn for two days is what that list exists to make visible. **Stage 6's 1.6 inherits both measurements** (probe `DenyAthenaSparkStartSession` as a contrast pair; re-measure authorize-before-validate, which is per-action) plus one check to run: the installed CLI has no `update-session` while 1.6 names `athena:UpdateSession`. `make check: OK`, `check-index.py` clean, `terraform fmt` and `ruff` clean. **Then the review in the same sitting**, pass 4 closing being the re-trim trigger: `CLAUDE.md`'s current position was **over budget at 10859 bytes → 8080**, the four sub-passes collapsed into one state bullet plus **three things Stage 5 leaves standing**. Both findings were followed to the files that could repeat them — **`scp-battery.md` still carried the stale `update-policy` sentence** (corrected with the reasoning, since the danger is that the hand path *succeeds*); **`terraform-changes.md` §8 gained the paired rule**, including the adopting side's obligation to **grep the prose for a mutating command when importing**; **the org-policies README's placeholder list omitted `<ACCOUNT_ID_DATA>`** — the very token that would have caused the damage, found by a reader and not by a gate; **`stage-01c`'s dated copy deliberately left alone**, history recording what happened while runbooks say what to do. **`AWS_STATE.md` `EXC-03`** so the two permanent `note` rows are not investigated, with an explicit "do not fix by loosening the classifier". **Lesson 35 added** (adoption invalidates procedures; beside Lesson 11, not inside it — claims vs instructions, and the opposite failure shape) and **Lesson 24 widened hours after being amended** by an instance that did not fit it: one cause, no attribution, same remedy, so the rule was restated on locus — *ambiguous **or** silent*. **No decision needed changing** (checked: D13 untouched, no decision names the battery procedure); **open question 14 gained a pointer, not a copy**. **Twenty-ninth — pass 6 READ BEFORE IT WAS RUN, and the step described a mechanism that does not exist (Claude's hand; NO AWS write, pass 6 still unexecuted; the one decision in it is the user's).** Before-reading: **six profiled accounts `not enabled`**, delegated administrator **`(none registered)`**, `securityhub` in no policy of this organization, `DL-11` on its standing note. The step said *auto-enable for existing and future accounts*; **auto-enable reaches NEW accounts only, current Region only — on this organization, zero of them**. Replaced by **central configuration**: one policy on the **root**, which also reshapes 13.3, because a centrally managed account cannot run `BatchUpdateStandardsControlAssociations` — the first disable converts the recommended policy into a **custom** one. **Two products wear one name** (`describe-hub` beside `describe-security-hub-v2`): only **CSPM** runs FSBP, and **13.0 now refuses the v2 product by decision** because enabling both makes Security Hub abandon Control Tower's `aws-controltower-BaselineConfigRecorder` for a service-linked one — Lesson 17 exactly, and the plan's Stage-12 recorder deferral voided in silence. **`DL-11` extended to fail on the v2 product's ARRIVAL**, the one Security Hub reading that is not *has the stage run yet*; report gained a `V2 PRODUCT` column, all six `absent`. Three smaller corrections: **the console pushes toward a linked Region the `us-west-2` ceiling denies**, so CloudShell in Audit is preferred with the collision as the reason (the CLI has `NO_REGIONS`); **the Config prerequisite was MEASURED** — every profiled governed account carries Control Tower's recorder `allSupported`+globals, `CONTINUOUS`, `SUCCESS`, which is also why 13.0 leaves it alone; and **13.4's `INV-09` count was stale from the 2026-08-18 split** — Security Hub is the **ninth** principal and **fourth** delegation, not the tenth. **The user decided Management is SELF-MANAGED** until Stage 12, after the first framing was corrected: **enabling CSPM does not record an account**, so including it buys a bill and a `WARNING` dashboard, not coverage. That reason unlocked a cross-stage finding — **Stage 1d decision 8's revision trigger names this very step as its candidate, and the candidate DOES NOT FIRE**; decision 8 corrected in place, nothing waits for it before Stage 12. **Two self-corrections:** a stage assertion that the repository already contradicted (`REFERENCES.md` held the *delegating is enabling* answer for CSPM all along — the new lesson's own failure, arriving while the lesson was written), and **`check-docs` caught an account enumeration whose rewrite exposed a wrong number** (prose said four accounts from four manual calls; the instrument had read six). **Lesson 36 added** — *auto-enable is a word each service defines for itself, and a cross-service finding written in the stage that hit it stays there*: three services, three answers, the repair was a different **mechanism** rather than a corrected parameter, and the Macie instance had already been written down in Stage 11's Status row where Stage 5 would never read it. **`PRICING.md` gained Security Hub's own 30-day trial**, a second window from GuardDuty's, opening at a different stage — neither covering the Config cost underneath. `make check: OK`; `datalake.py` **0 FAILED**; `check-docs` red only on the pre-existing `stage-03`. **`REFERENCES.md` was edited by both hands this sitting; both sets of entries survive.** **Thirtieth — pass 6 leg 1, and the first AWS write of the pass:** `AWS Control Tower Admin` → Management → `AWSAdministratorAccess`, CloudShell. The root id, Management's id and **`Audit Account`'s** id were collected *there* **because Audit cannot go back for them** — member account, no Organizations view, `list-roots` refused — which is the ordering 13.1d gained before the leg ran. `list-accounts` filtered on the exact vended name **and** `ACTIVE` returned **exactly one line**, so the fail-loudly condition never fired and no wrong id reached the write. `enable-organization-admin-account` was accepted with no denial, the expected shape for a service that appears in **no policy of this organization**. **Leg 2, in Audit, RAN in the same sitting** — `describe-hub`, the `NO_REGIONS` aggregator, `ConfigurationType: CENTRAL` (read back `ENABLED`), the FSBP ARN **read** past its `azure-` near-namesake, the `awsds-fsbp-only` policy, and the two associations. **`describe-hub` turned a documented sentence into a measurement**: the hub was `SubscribedAt` the moment of leg 1's designation, with no enabling call ever made in Audit — *“delegating is enabling”* is now **observed** for CSPM rather than inherited from GuardDuty. **The census closes exactly and produces a finding**: eighteen association rows — seven OUs, one root and **ten** accounts, where the organization has nine live ones. The tenth is **`EXC-01`**, the SUSPENDED `Sandbox` hanging off the root — **the first control ever to reach it**, because a root association walks the tree while an SCP arrives through an OU it sits in none of; `EXC-01` amended in `AWS_STATE.md`, its row expected to **never** reach `SUCCESS`. **At ninety seconds only `<Management>`/`SELF_MANAGED_SECURITY_HUB` read `SUCCESS`; the RE-READ then settled sixteen of eighteen** — all seven OUs and all nine live accounts — so the association half is done and CSPM is on under `awsds-fsbp-only` everywhere governed. **Two rows left where one was predicted:** the suspended account **and the `ROOT` above it**, the likeliest reading being a parent summarising a descendant that can never succeed. The cost is an invariant: *every row `SUCCESS`* is **unavailable to this organization**, and `EXC-01` now carries the narrower one — every OU and every live account `SUCCESS`, those two exempt by name. **CLOSED THE SAME SITTING by two instrument readings:** `datalake.py` **0 FAILED** with `DL-11` six `pass` — `enabled` + `FSBP on` in every profiled account and **`V2 PRODUCT` `absent` in all six**, which is the only reading here that is not *has the stage run yet* (13.0 refused v2 so Control Tower keeps its Config recorder; `DL-11` fails on that product's **arrival**); and `org-trusted-access-services.py` restating **INV-09 to nine principals and four delegations**, `securityhub` → `Audit Account` `ACTIVE` — the count the 2026-08-18 split had left saying *ten* in the sub-step. **Nobody enabled trusted access for `securityhub`: the designation did**, the shape GuardDuty and Macie will repeat. **Left of pass 6: only 13.3's triage**, which needs a first report that does not exist yet — and under central configuration a disable is a **policy edit**, so expect `awsds-fsbp-only` to become a custom policy. |
| Stage 6 — SageMaker Unified Studio | [log-stage-06-unified-studio.md](log-stage-06-unified-studio.md) | **Thirty entries, 2026-08-19/26 — twenty-nine Claude's on the user's request, and EIGHT of them written in TWO HANDS — the eleventh, the thirteenth, the fifteenth, the sixteenth, the eighteenth, the nineteenth, the twenty-third and the thirtieth (the user's console, browser and shell output verbatim, Claude's measurements under them). The first SEVEN are doc-and-decision sittings and the file existed before the stage did; the eighth is the build, the ninth the plan review that followed it, the tenth the image build code with its buildbox, the eleventh step 1.3, the twelfth steps 1.4 and 1.5, the fourteenth step 5.0's BUILD half, the fifteenth its PUSH half — **step 5.0 CLOSED** — the sixteenth **step 1.7's missing leg, and it closes it**: off VPN, in the portal's own sitting, the console refused `logs:DescribeLogGroups` *with an explicit deny in an identity-based policy* and was clean with the tunnel up; the message named the persona role and `us-west-2` **itself**, and the same probe's Stage 4 CLI reading of five days earlier matches byte for byte, which also settles that stage's residual about choosing an action for its wording — **the console wraps but does not rewrite** — and the thirteenth **step 1.7 plus the gap it uncovered** — the portal opened with the tunnel down and behaved identically with it up, so **INT-16 is answered as its fallback (ii)** (VPN-only APIs and console, not a VPN-only portal; the positive control arrived in the sixteenth), while the same click returned `CreateProject` denied on BOTH networks and located it: **zero policy grants** on the root domain unit, whose only owner was the domain-creating role, so the two project profiles were templates nobody could instantiate. Closed the same day by `grants.tf` — `2 added`, one `CREATE_PROJECT_FROM_PROJECT_PROFILE` per profile, `experimentation` to the data scientists and `engineering` to the deployment managers (**the second is the instrument of D21's open half, not a settled entitlement**), every field `createOnly`. That entry also carries **`buildbox.md` §P**, step 5.0's push procedure written from measurement: build and push are ONE buildbox session, no Sandbox identity can push, and the S3 gateway endpoint is not on the push path**, because execute-time decisions were settled ahead of it and the stage file homes them here. **The first two made no AWS call of any kind, not a write and not a read**; the third made **two reads** (`sts get-caller-identity`, `ec2 describe-vpc-endpoint-services`) and nothing else. Every other source is AWS documentation, fetched from the public web, which is a class of evidence that ages and is flagged as such. **Decision 3 taken in full by the user**: Athena Spark off by SCP (`athena:StartSession`/`UpdateSession`, AWS's own statement, `Resource` left at the wildcard because narrowing to `us-west-2` would *permit* Spark elsewhere), **not pulled forward** into Stage 5's phase-4b sitting, **no Athena clause in step 2.1's boundary** (Lesson 20: where two policies deny one call only one is proven, so the second reads as coverage), and the three optional `athena.*` session endpoints **declined in writing** — commented exclusions in both Interactive `egress/main.tf`, plus 4.1's instruction to keep the Spark session domains off the DNS Firewall allow-list (Lesson 5). **Four corrections from re-reading the network-isolation page** rather than trusting the plan's 2026-08-16 summary: AWS ships the deny statement verbatim; the Tooling Athena flag applies to **new projects only**, so it is non-retroactive as well as blunt; the documented third control is *"remove Athena Spark permissions from individual project IAM policies"* — grant-shaped, on blueprint-authored policies, so 2.1's boundary is a **deviation to record, not a copy**; and `DenyUserAccessFromUnauthorizedVPCs` carries a **third** condition, `aws:userid` `*:user-*`, which means INT-16's on-behalf carve-out is already in AWS's shape and rests on the `userid` form rather than `ViaAWSService` (propagated to INT-16). **The user's challenge and its answer, the entry's most reusable part:** the 2026-04 *Athena Spark adds support for AWS PrivateLink* release moves the **client → session** path (Spark Connect, Live UI, History Server) and **not where the session runs** — no `NetworkConfiguration` exists in the API, confirmed twice independently — so open question 12 stands; and two details on the feature's own page argue *for* the deny (no VPC endpoint policies on the three session endpoints; a session URL minted inside the VPC is reachable from the public internet by design). **No intersection with Athena SQL**, which rides the required `athena` API endpoint — the one shared edge being `GetSessionEndpoint`/`GetResourceDashboard`. Two findings landed elsewhere: the required-endpoint table is longer than the plan's four-name sample and pairs `q` with a `us-east-1`-only endpoint a `us-west-2` VPC cannot reach, and **decision 1 was reopened** on an idle endpoint cost (four endpoints for EMR Serverless against one for Glue sessions) the compute comparison never saw. **Nothing built, applied, attached or probed**; `POLICIES.md` owes its row at 1.6, with the probes — the negative one, `athena:StartQueryExecution` still succeeding, being the one that matters. **Second: decisions 4 and 5 taken by the user, same day** — the *Supported blueprints* page re-read for its **Resources created** column found the plan's two Lakehouse names **inverted** (`LakehouseCatalog` is Redshift-Managed-Storage-backed; the Glue/Athena form is **`DataLake`**, correcting D26's wording a second time) → decision 4: `DataLake` alone, `LakehouseCatalog` disabled. Decision 5 reframed as an **allow-list in three categories, every blueprint owned**: category 1 enabled by default (`Tooling`, `DataLake`, `EMRServerless` following reopened decision 1, `AmazonBedrockGenerativeAI` — its `PRICING.md` row owed before the 1.4 apply); category 2 on demand by named trigger (`Workflows` OnDemand = D28's last-rung fallback, then `[E]`; `MLExperiments` = tracking needed, priced first); category 3 by amendment (`EMRonEC2`, `PartnerApps`, `Quicksight`, `LakehouseCatalog`), `RedshiftServerless` a never (reopens D26/D12). The entry also records the three billing states of a blueprint (enabling creates nothing; the MWAA environment is born at first project use) and where the pieces went: `docs/SMUS.md` created as the reference table with a `CLAUDE.md` routing row, `US-3` tightened from Redshift-absence to the category-1 allow-list with the `AmazonBedrock*` prefix rule. No AWS call in the sitting; nothing applied. **Third: decision 1's reopening re-read on the user's request — the reopening stands, its number does not.** It had priced **two AZs**, AWS's HA recommendation and exactly what Stage 3 step 8.5 declined (D9), so the delta is ≈ **USD 0.06/h** and not 0.12; and it is **not 24×7** — the endpoints are `[E]` in `egress/`, so the always-on case is a teardown-discipline failure (Stage 4's host left `running` is the precedent), kept as a named risk under D12's silent budget rather than as a rate. **The symmetry is the reusable part**: the original recommendation weighed only per-use compute and missed a standing line; the reopening weighed only the standing line and missed that a *started* EMR-S interactive application keeps one 4 vCPU/16 GB kernel worker regardless of pre-initialized capacity (`autoStop` 30 min, **kernel idle 60 min not modifiable**) — and that Glue's floor is its **5-DPU session default ≈ USD 2.20/h**, not the 1-DPU minimum cited. **The axis neither weighing saw: FGAC** — the notebook's Spark Connect path documents fine-grained access control *and* TIP as unsupported on **all three engines** (full-table only), while only an EMR-S **compute connection** documents `project.spark.fineGrained` (Glue's is for Visual ETL), so that is the sole documented route by which a notebook Spark session inherits LF column/row filtering — reaching D13 and Stage 5's `restricted` column, and making "column scoping lives on the Athena SQL path alone" the scope statement a Glue outcome must write down. Left as a **reading, not a decision**, precisely because it reverses the current lean. **Decision 1 therefore ends in two in-stage readings rather than a number** (which endpoints a session exercises, per 4.2's flow logs; whether `fineGrained` is usable from an IdC-domain notebook), with decision 5's category 1 pre-wired to follow either outcome in one commit. The one measurement against the account: `describe-vpc-endpoint-services` confirms every name in AWS's optional table is real in `us-west-2` — and answers nothing about which a session needs. `make check` green; `make check-docs` red only where it already was, plus a **new false positive worth knowing** — `check-plan-refs.py`'s `\brows? \d+` reads the phrase "decision row 1" as a stale row reference, so the sentence was reworded rather than the blunt rule loosened. One thing the third entry settles rather than opens, and it travels to decision 2: with TIP unsupported on every notebook Spark path, the TIP lever is a **SQL/query-path lever only** — recorded in open-questions item 13. The sitting also pushed `CLAUDE.md` further over the then-current size budget, the re-trim recorded as owed at the stage's close. Nothing committed by Claude. **FOURTH, 2026-08-20 (Claude's hand, no AWS write): the stage re-read against Stage 5 as CLOSED — 23 candidate defects, 9 survived adversarial verification, 14 refuted, and the refutations are kept in the entry so they are not re-derived.** Staleness was the smaller half. **Two places the plan could not have been executed at all:** the pass table filed 1.6 — an `awsds-org-scp-ou-interactive` amendment — under `data-governance/governance/`/`awsds-infra-data`, which cannot update an Organizations document (Lesson 35, 4e's trap); and 2.6 prescribed widening `data_scientist_role_arn`, the **single-string `principal` of three `aws_lakeformation_permissions`** — a plan-time failure, or a silent fan-out of the persona's re-grants to the project roles. **One place executing it exactly would have BUILT what the stage forbids:** step 0.2 named Development for a creation-shaped probe with no `--dry-run`, so a deny that failed to fire would have created a DataZone V2 domain there — moved to `Policy Canary`, which the `safety` rule required all along. **Decision 6 rested on a false premise** (a project's own workgroup does NOT write into the derived zone), and the unsound half hid a **fourth** designed destination outside the CMK rule, the expiry and Stage 11's pre-declared scope. **`docs/SMUS.md` was corrected in the same sitting** — its three-bucket table listed a *workgroup* as a bucket, which is what made that premise hard to see. Two verification rows added, (xviii) the project path's **key** and (xix) the real `aws:SourceVpce` — **flow logs cannot answer (xix)**, gateway traffic crosses no ENI. `INT-05` and `AWS_STATE.md` deliberately UNCHANGED until (xix) returns. OQ 19 gained its addressee. **The audit's own first pass capped verification at 8 of 23 and reported the cap**; it was re-run uncapped before anything was applied. **FIFTH, the same day — STEP 0 RAN (Claude's hand on the user's explicit instruction; the stage assigns it to the user, so this is an authorized one-sitting deviation) AND DID NOT CLOSE.** Four variants — throwaway vs the conventional `AmazonDataZoneDomainExecutionRole` name, trust with and without `aws:SourceAccount`, `--domain-version` V2 and V1 — returned the **byte-identical** `AccessDeniedException … Cross-account pass role is not allowed`, in Data Governance **and** in `Policy Canary`. Caller and role proved same-account each time; `InfrastructureAccess` carries `AdministratorAccess`. **The contrast came out FLAT, so neither call reached authorization and `DenyDataZoneDomainOutsideDataOu` is STILL unexercised in both directions** — step 0's premise (that a hand-built execution role is the missing ingredient) is wrong, and *"fix the role and retry"* has no CLI fix of this shape. **What it does NOT license:** "DataZone forbids a same-account pass role" — the message is not attributable from its own text (Lesson 24) and a tool's failure is not a property of the world (Lesson 30). **Next lever: the console**, which builds the role and names its fields (Lesson 16). Nothing created — `list-domains` reads 0 in both accounts, all three probe roles deleted. **SIXTH, 2026-08-21 (Claude, no AWS call — the same sitting, past midnight): step 0 REBUILT around the dead instrument.** Annotating it was not enough: for one sitting the stage carried a step whose procedure was known not to work, and two other files still pointed at it. **New 0.1a — the probe now rides step 1.2's own `aws_datazone_domain` apply**, staged domain-first under Recipe D, with a three-outcome fork whose third branch is **adoption, not force**: console-create + `terraform import` (Stage 2's *adopted, none created* precedent), and the wizard's fields are the Lesson 16 reading four hand-built roles could not synthesize. **The canary half returns via CloudTrail, not a console wizard** — replaying the request shape of the first *successful* `CreateDomain` **holds the instrument constant across the two accounts, the property the flat contrast lacked**. `scp-battery.md` and `POLICIES.md` both corrected so neither still describes a runnable CLI probe. **And the Status row committed in `5df5a83` had been born stale — *"step 0 is now runnable"*, hours before execution disproved it — the same shape as the `CLAUDE.md` VPN bullet in Stage 4's log that day; two instances is a pattern, a third makes it a lesson.** **SEVENTH, later the same day (Claude, no AWS call — `git` and `grep` only): the pull-forward AUDITED and RE-CUT.** Preparing the next step found `production/pki/` and `production/registry/` absent from `terraform-live/production/`, though the Prerequisites row called both *"pulled forward and **applied** before this stage"*; the user asked whether any earlier log said why. **It does not, because there was no event**: `git log -S` puts the clause in one docs-only commit of 2026-08-16 (`f44559f`, 14 files, none under `terraform-live/`) and `git log --diff-filter=ADR` over both paths is **empty on every ref** — never added, never deleted, never attempted. `history.md` and `AWS_STATE.md` hold nothing either, and this log's own `grep -ic 'pki'` returned **0**. **The tense disagreement was four-sided and this stage was the isolated party** — Stage 7 said the same thing in the *future* tense, `production/bootstrap/pki-key.tf` scheduled the slice at "Stage 7" **a day before** the clause was written, and D36's *Referenced by stages* row never listed Stage 6. **Half of D36 HAD been executed**: the second state key `alias/awsds-prod-tfstate-pki` (2026-08-15), ~USD 1/month encrypting nothing, plus three name-only pre-wirings for `pki` and **nothing at all for `registry`, not even a rank**. **No check would have caught it** — `supplychain.py` gates its note→fail flip on the GitLab host two stages away and stays green over a missing pass 0. **Re-cut on the user's instruction, by consumer:** `pki/` back to **Stage 7 pass 1** with the leaves (**D36 §3 amended**, D36 dropped from Stage 6's Consumes, step 5.0's image keeps an **empty CA-install layer**) and a new **Stage 7 step 2.6** rebuilds the image with the root, in the sitting that first has something to clone; `registry/` **narrowed to 5.a and made Stage 6's PASS 0** (ECR `base`/`dev-env`, CodeArtifact, key, consumer policies), with the cache and the per-application repositories left at Stage 7 as **5.b**. The obligation moved out of prose into a build-table row, a pass-table row, the ordering paragraph and a `layers.py` rank. **Lesson 37 promoted** by the trigger the sixth entry declared. No `history.md` row: nothing the amendment describes has been provisioned. **EIGHTH, 2026-08-21 — THE FIRST BUILD SITTING IN THIS FILE, and everything in it is Claude's hand on three authorizations the user gave in the sitting** (the standing `terraform apply` one, then the git commits with their module tags, then the two probe batteries — the stage assigns the batteries to the *user*, so the entry names the deviation rather than absorbing it). **Passes 0, 1 and 2a applied**, nine acts in order, every re-plan `No changes`: `production/registry/` (`14 added` — Stage 7 step 5.a a stage early), both `*/sagemaker/` (`7 added` each, then `1` more for the step 9.1 log group), `identity/sso/` (`6 to change` — the step 3 pair reaching all six persona sets in one diff), `data-governance/governance/` (`5 added` — the domain, `AVAILABLE`, V2) and `identity/org-policies/` (`1 to change` — `DenyAthenaSparkStartSession`). **No `[E]` slice was applied** — design A's DNS Firewall is written, validated and left waiting; `make status` ends at USD 0.0052/h. **Step 0.4 read the domain slice's plan BEFORE its first apply**: five resources, nothing `sagemaker`-shaped. **VERIFICATION (i) IS ANSWERED IN BOTH DIRECTIONS and had been open since 1c**: the apply created the domain from `Data`, and the *identical* request shape replayed on the canary returned `AccessDeniedException … with an explicit deny in a service control policy`, resolved by `describe-policy` to `awsds-org-scp-baseline` — so `aws:PrincipalOrgPaths` **does** populate for DataZone and INT-12's forbidden fallback is closed rather than intended; both probe roles deleted, the canary left empty. **It also explains the fifth entry's flat contrast by measurement:** those four variants passed `--domain-execution-role` and **no `--service-role`**, and `Cross-account pass role is not allowed` was DataZone objecting to the absent service role in a message naming neither field nor account (Lesson 24). **Phase 4b: `25 as expected, 0 unexpected`** — `StartSession` denied in Development and in Sandbox (by *inheritance*, D37), still authorized in Production, and the **negative probe passed**, so the amendment did not take D13's query path; `StartSession` also turns out to **authorize before it validates**, which Lesson 21 forbade assuming. **Four readings that were measurements**: the SMUS managed-policy family (three plausible names do **not** exist; a fourth, the Athena-federation query role, exists and was deliberately not created); **`athena:UpdateSession` is in no Athena API model** — shipped anyway, with `StartCalculationExecution` added beside it; **only `awscc` carries `environment_role_permission_boundary`**, which is INT-15's mechanism arriving above its own fallback chain (Lesson 8); and **`PutEnvironmentBlueprintConfiguration` takes no account parameter**, so a blueprint configuration is applied from the MEMBER account — the pass table said the domain account and step 1.4's body was right. Two pieces of unplanned hygiene: the new slices lock `aws` at **6.61.0** against the tree's 6.60.0 (left as it landed) but their **platform coverage was fixed** for the Stage 7-8 runners, and **`versions.tf` stops being byte-identical in three slices**, deliberately, for the second provider. Two commits in Recipe B's order with four module tags **confirmed on origin before commit 2**, then the four applied slices **re-initialised against the pushed tags** and re-planned `No changes` — which is what says the tag serves the code that was applied. `make check` and `make check-ou` green; `studio.py`, `supplychain.py`, `org-policies.py` and `datalake.py` all 0 FAILED. **NINTH, the same day — THE PLAN REVIEWED AGAINST THE BUILD, and it found two defects in the eighth entry's own work.** Six lenses over the surfaces a build sitting can invalidate, every candidate **verified adversarially before it counted**: **97 candidates, 37 survived, 20 edits**, and the fourteen-odd refutations are kept so they are not re-derived — most of them *already written two files away*, which in a one-copy-per-fact repository is a defect rather than an improvement. **The sync first**: a rebase merge left all four module tags `orphaned` (the normal outcome the runbook says not to fix) and all four tree hashes **IDENTICAL** to `main`, so nothing was owed — with a zsh trap recorded, `"$t:path"` applying the `:t` modifier and producing four false DIFFERENTs. **The two defects:** seven executor instructions named `SMUS_MEMBERS` where the gate reads `SMUS_ASSOCIATED`, so **following them is a no-op that announces nothing**; and **`profiles_enabled` REVERSES** — a `SMUS_MEMBERS` row added ahead of its association destroys the project profiles that already exist, which is why the ordering now lives in **Stage 14 step 4**, rewritten from one sentence naming a blueprint that does not exist into six ordered acts with a console act in the middle. **A four-month gap exposed sideways:** three `*/data/` slices carried ONE platform in their lock files where Stage 2 step 6.3 requires three and **nothing had ever read one** — under the mandated `TF_PLUGIN_CACHE_DIR` that fails `init` outright on a Linux runner, and without the cache it silently rewrites a committed file. Fixed by **copying** a sibling's lock (not `providers lock`, which would have re-resolved the constraint and made a third version island), and landed **with** `scripts/check-provider-locks.py` in `make check` and `pre-commit` — **negative control run**: two hashes removed on purpose, both checks fired, restored. **The cost cluster:** the KMS enumeration resolved to ten keys against **twelve measured live**, and none of Stage 6's three matched any clause — the unit turns out to be a key **VERSION** (365-day rotation on every key, measured), so the count is a year-one figure; a business unit costs three CMKs, not two; **`datazone` had taken the Interactive accounts from 11 endpoints to 12 without propagating**, so `make status` had been under-reporting by a cent an hour per account; and D12 stopped carrying a second, flattering era of arithmetic (Lesson 7). **The one AWS write corrects a claim from the eighth entry**: a cross-account ECR pull needs **no KMS grant** — ECR grants itself on the key and decrypts on the caller's behalf — so `alias/awsds-prod-registry` was narrowed to `codeartifact` alone, applied, read back; `SC-7` proved the repository policy and **not** the key, because `describe-images` decrypts no layer. Six more corrections in the files that own them, and **`US-3` stopped reading empty on success and on failure** (Lesson 13) — it now reads every member account, because a blueprint configuration belongs to the account that *called* the API. **Deliberately not done:** no `history.md` row, no `CLAUDE.md` edit, and **no new lesson** — the candidate was refuted as an instance of Lessons 24 and 30. **TENTH, 2026-08-21 — THE BUILD CODE AND A HOST TO RUN IT ON, and the entry ends in a defect of mine.** Two asks in one sitting: *write the Dockerfiles*, then *build them somewhere else*. **A doc correction opened it and was load-bearing**: step 5.0's *"base on `jupyterlab/default`"* is the health-check **Base URL**, not an image — the required base is `sagemaker-distribution` ≥ `2.6-cpu`, so building to the old sentence would have produced an image SMUS refuses to run; **no `ENTRYPOINT`**, AWS's metadata directories and the fixed `/home/sagemaker-user` mount joined it. **The ancestry is forced from both ends and nearly collides** (BYOI wants the distribution, D17 wants ONE ancestor), so `base` IS the distribution plus this project's layer and the cost is named. `images/` ships **three build-time assertions rather than three assumptions** — the activity-monitor extension is *checked, not installed*; the CA-root count fails in both directions; the Julia kernelspec is relocated and asserted, because `installkernel` delegates to `--user` and lands it in root's home. **Four defects in my own first draft, found by re-reading** (`ARG` does not survive `FROM`, `pip config set` defaults to USER level, a `sed` join emitting `""`, `--component` needing the repeated form). **Then the re-scope, on two measurements:** the distribution publishes **no `arm64` tag at all** and this laptop is `arm64` **with no docker** — so `sandbox/buildbox/` was built, an `[E]` `t3.xlarge` in the **isolated** tier (the private tier's default route is `egress/`'s, and two slices writing `0.0.0.0/0` into one table is a collision), reachable only from the tunnel and reaching the internet only through the WireGuard host, which `wireguard-v0.4.0` turns into a **NAT instance** via `vpc_nat_cidrs` — rules on `wg0`'s `PostUp` because the host is `[D]` and user data runs once. **`source_dest_check` had to go off, and the module's argument for keeping it on was never wrong — it was only ever about the tunnel**: the ENI applies the check on the way IN, before the kernel could masquerade. **The first apply of `vpn/` REPLACED the host exactly as predicted before the plan was taken**, EIP and key surviving, and it is not a lockout because `DenyControlPlaneOffVpn` omits `InfrastructureAccess`. **THE DEFECT: reach is an INTERSECTION and I built two thirds of it (Lesson 28).** The `[P]` WireGuard security group still admitted UDP/51820 and nothing else, so forwarded packets were dropped **after** the route and the masquerade had done their jobs — three pieces in three slices, no file wrong. The symptom was a buildbox whose `dnf` **succeeded** (S3 gateway endpoint, no route needed) and whose SSM then timed out, which reads as a broken mirror. Fixed in `foundation/` and **not** in the `[E]` slice, because that group declares its rules **inline** and a separate rule resource would be silently removed by the next apply (INT-11's failure mode in a security group). `VP-3` **checked, not assumed**: still exactly one world-open rule. **The end-to-end reading is the only thing that proves the design** — the build host leaves under **`52.89.212.1`**, the WireGuard Elastic IP, from a tier with no internet gateway. Also: `t3.xlarge` measured at **0.1664 USD/h** through the Price List *Query* API (the bulk EC2 offer file is hundreds of MB — the method is now named in `PRICING.md` §0), the build context measured at 27 KB against user data's 16 KB ceiling (which is why `buildbox.py sync` rides `ssm:SendCommand`, fenced like `vpn.py --on-host`), a new runbook `buildbox.md` with its routing row, and **one mistake already written down two lines above the block I was editing** — a security-group rule description carries no apostrophe. **No new lesson from that one**: the fact was recorded in the right place and I did not read it. **Owed, both the user's:** the `docker build` itself and the push. **The buildbox was left UP and billing.** — **Eleventh (step 1.3, the account association, two hands):** the association is **auto-accepted** — zero RAM invitations in either member, one new `DataZone-EXTENDED_ACCESS-…-ORG-ONLY` share beside the four `LakeFormation-V4-*`, so the V1 guide's 7-day expiry never starts; the RAM permission is `AWSRAMPermissionsAmazonDatazoneDomainExtendedServiceAccess` and **neither name the step predicted exists in RAM at all** (six are published for `datazone:Domain`), a strict superset of the 111-action default by **41** actions that are the SMUS **V2** workbench surface — recorded as a **ceiling** that widened, with the IAM and SCP halves measured so it is not read as access (Lesson 28); `list-environment-blueprint-configurations` **succeeds** from both members returning empty, which is the functional proof a console label cannot give. **And `./aws/studio.py` FAILED because the step worked** — `US-2` counted domains `list-domains` returns and discarded the ARN, so the shared domain read as a locally created one (**Lesson 31 as a false FAIL**); the owner is now split out of the ARN, the report gained an OWNER column, two stale notes were made to follow the measurement, battery back to **0 FAILED**. **Deliberately not done: the `backend.SMUS_ASSOCIATED` row** — it looks clerical and is the trigger for 1.4 *and* 1.5, and would cross two gates the stage has open in writing. — **Twelfth (steps 1.4 and 1.5, Claude's hand under the sitting's three authorizations — the applies, the two write probes, and the user's reclassification, quoted verbatim):** the first 1.4 apply failed **twelve for twelve** (`Managed Environment Blueprint with <id> doesn't exist`) while `get-environment-blueprint` answered those same ids — the `awscc` resource takes the blueprint **NAME** where the `aws` one takes the id (Lesson 32), measured by the CFN GetAtt split, the official example's literal `"DefaultDataLake"` and the live type schema, then **bracketed by two authorized probes**: a plain-CLI put **with the id** (succeeded — the member-account path was healthy, Lesson 30 — then deleted) and a Cloud Control create **with the name** (`SUCCESS`, identifier built from the resolved id, then deleted). `sagemaker-prereqs-v0.2.2` passes the name through the data source's `.name`; **`v0.2.1` is a STILLBORN tag** — cut while commit 1 was still hook-blocked, the failure hidden behind a piped exit code — left in place per runbook §8, never to be referenced. Applied: **`12 added` per member**, re-plan `No changes`, 12/12 carrying the boundary, `get-data-lake-settings` **byte-identical before/after** in both members (verification (xiv)'s enablement half) and the two-subnet regional parameters accepted ((iii)'s first half); the schema also shows **`EnvironmentRolePermissionBoundary` is write-only**, so boundary drift can never appear in a plan and `US-8` is verification (v)'s sentinel. The first 1.5 apply was then **refused by DataZone 400, both profiles** — *"ToolingLite environment blueprint configuration must have deployment mode ON_CREATE"* — and the domain's own descriptions make `ToolingLite` a second **BASE**, not a capability; the user re-cut the roster: **"Reclassifique ToolingLite como Categoria 3."** — decision 5 from 12/5/6 to **11/5/7**, `sagemaker-prereqs-v0.2.3` moving all three list copies, `1 destroyed` per member (**11 stand**). The second 1.5 apply: **`2 added`** — `experimentation`→Sandbox, `engineering`→Development, eleven environment configurations each, `Tooling` the sole base `ON_CREATE`, the five locked parameters read back non-editable and TIP `false` — **decision 2 delivered as coded**. `./aws/studio.py`: **0 FAILED** (`US-3` *all inside decision 5's category 1* in both members; `US-4` both contract names). Left owed after the sitting: **1.7 (user), 5.0 (user), passes 3-5**. — **Fourteenth (step 5.0's BUILD half, Claude's hand under the sitting's authorization; the tag convention is the user's decision):** the host was found **running and Online**, both ECR repositories **empty** (no tag spent, pass 0 confirmed applied), `/opt/awsds/images` already synced and **byte-identical to the repository** by per-file md5, 62 GiB free, and the egress path measured rather than believed — the box leaves under the **WireGuard Elastic IP**, so the `[E]` route, the `[D]` masquerade and the `[P]` security-group rule were all working at once. §P's interactive build was driven instead by **`ssm send-command` detached under `systemd-run`** (Claude holds no TTY), and **deliberately nothing of the push**: Run Command parameters stay readable for weeks, which is where §P's `read -rs` exists to keep a 12-hour token out of. `base` **`exit=0` 04:17:21Z**, `dev-env` **`exit=0` 04:27:35Z**, ~15 minutes, **17.46 GB de-duplicated** on a 64 GiB root. Four readings taken from the images rather than assumed: the **activity-monitor assertion passed with a name** (`jupyter-activity-monitor-extension 0.3.2`, conda-forge — verification (x)'s **build half only**; whether idle shutdown *fires* is still 8.1's), the **BYOI entrypoint rule holds** (`/usr/local/bin/_entrypoint.sh` + `/bin/bash`, `sagemaker-user`, inherited), the **CA layer empty and saying so** (*"0 (expected 0)"*, D36 §3 amended), and the runtimes (Python 3.12.13, uv 0.12.5, Julia 1.12.7, R 4.5.3, rustc 1.98.0) — plus one thing the image cannot answer: `/opt/awsds-runtimes.txt` records `awsds/base:local`, the **build-time** reference, never the ECR tag. **The tag convention was settled because `IMMUTABLE` spends it on first landing:** `<flavour>-v<major>.<minor>.<patch>`, same number in both repositories, **`default-v0.1.0`** first — the user's flavour axis (GPU / Spark / neither), after two corrections: the two repositories are **not** prod-vs-dev (both in Production; the axis is ancestor vs BYOI runtime) and the repository already is the name. **One of Claude's own arguments was checked and weakened by the check** — `tagPatternList` takes four `*` wildcards and would match a suffix, so flavour-first is grouping and convenience, not capability, and `docs/SMUS.md` §*Custom images* says the weaker thing. Docs only, no Terraform, `make check` OK. **Owed: the push itself** (`default-v0.1.0` in both, §P, on this host while it is up — build and push are one session), then the digests into the entry, then pass 3. **The buildbox was left UP and billing.** — **Fifteenth (step 5.0's PUSH half, two hands — the user's procedure and pasted output verbatim, Claude's readings under it; the account id redacted to `<Production Account>`, declared once):** **step 5.0 is CLOSED.** Both tags landed and are now **spent** — `base` `sha256:6c53def4…5b3a` (3.96 GB stored) and `dev-env` `sha256:76d9b5e8…3e56` (5.65 GB), against 12.1 and 17.5 GB uncompressed, with `dev-env` re-uploading its own copy of `base`'s layers because ECR stores per repository. **Scan-on-push ran and the first read of it was a trap**: `describe-images` carries **no scan field at all** for these images while `describe-image-scan-findings` answers `COMPLETE` for both — the natural place to look reports *"never scanned"* on a scanned image and says the same thing if scanning were off (Lesson 13); `supplychain.py` measures the configuration instead and its comment already names the right API. **The counts are IDENTICAL in the two images** (CRITICAL 30, HIGH 430, MEDIUM 514, LOW 3, UNDEFINED 7): `dev-env`'s Julia, R and Rust added **zero**, because basic scanning reads **OS packages only** — Stage 7 decision 2's input, measured. `./aws/supplychain.py`: **0 FAILED**, `SC-4` both `IMMUTABLE`, and **`SC-7` answered against a real image for the first time** (*"ok (1 image(s) visible)"* from Sandbox) — the first exercise of `AllowConsumerAccountsToPull` with something behind it, though **visibility is not a pull** and 5.1 is where that is settled. **One correction the tool caught**: `docs/SMUS.md`'s tag section had said the lifecycle policy was still to be written — `terraform-modules/ecr-repo` builds one (untagged 14 days, tagged kept to 30, `tagPatternList = ["*"]`), so **rule 2 counts every flavour together** and must be split when a second flavour lands; the section was corrected the same sitting. **The buildbox was STILL UP at 0.1664 USD/h** with its images now redundant. Owed: `buildbox.py down`, then **5.1**. — **Seventeenth (the plan read back against the session, Claude's hand under four requests; no AWS write, no apply):** the buildbox reads **`absent`** and the previous sitting's three files are in `HEAD`. **Seven places where the plan and the session disagreed** — four stale assertions, one unrecorded answer, two decisions. Applied: step 5.0 struck in both tables with its tags and digests, the Status row now spanning **two dates and four sittings**, verification (x) recorded **and fenced to its 5.0 half**, Stage 7 step 2.6 given its tag (`default-v0.2.0`), and a **comment-only revision trigger** in `terraform-modules/ecr-repo` for the day a second flavour shares a repository. (Item 1 — the owed table still calling `grants.tf` unapplied — **the user had already fixed while this was being written**, and the collision is the entry's one process note: the edit was refused as *"File has been modified since read"*, which is what kept two hands off one file.) **The propagation of `<flavour>-v<semver>` uncovered a live one**: GitLab's protected-tag pattern for `dev-env/` is `v*`, which **does not match `default-v0.2.0`**, and since 3.3's answer makes the protected tag the whole of the CE release authorization, leaving it would have unprotected every release while the settings page still read as protected — now `*-v*`, verified by *attempting* a tag creation rather than by re-reading the pattern. **And item 6 could not be framed until something was read**: Inspector's ECR language list is C#, Go, Java, JavaScript, PHP, Python, Ruby, Rust — **Julia and R at no price** — which turns the previous sitting's identical-counts measurement from a cost question into a permanent one, because Stage 7 decision 2 had deferred the language half *against a real bill*. The user chose **accept + re-frame**, written to four places on purpose (Lesson 34): the acceptance and its revision trigger in `institutional-delta.md`, the *"the review IS the control"* sentence in `images/README.md`, Stage 8 step 1.4's *"the two compose"* corrected to **OS + Python and no further**, and decision 2 keeping its recommendation while losing the implication that waiting covers everything. — **Eighteenth (pass 3's first project, and the sitting that closed the create path; the user's five portal pastes verbatim, Claude's measurements and fixes under them):** five attempts, seven findings, each one layer deeper: zero `CREATE_ENVIRONMENT_FROM_BLUEPRINT` grants on all 22 configurations (v0.3.0 — 11 per member, the undocumented `<member-account>:<blueprint-id>` entity id), Tooling's null manage-access role validated at deploy AND at the stuck project's delete (v0.3.1, plus the first user-authorized full-object Put — an applied configuration is IMMUTABLE through `awscc`), `Invalid S3 path provided null` (v0.3.2 — the projects bucket and `S3Location`/`KmsKeyArn`; the second Put predicted before it ran), then TWO independent service-role defects leaving NO CloudTrail event on either side — both trusts pinned the MEMBER account where the documented guard is the DOMAIN account, and the project CMK's policy reached no service principal (v0.3.3) — and finally CloudFormation 400 `lifecycleManagement must be one of AllowedValues`, the stage's FIRST in-account failure and the proof the five fixes before it landed (the three stuck projects deleted cleanly the same sitting): the profile had locked `"true"` against the template's `ENABLED`/`DISABLED` enum, and the fix apply met `UpdateProjectProfile` validating what `CreateProjectProfile` never did — the only two required-no-default parameters in all 11 blueprints, now editable placeholders. **Attempt 5 succeeded** — `fifth-experimentation` ACTIVE, its Tooling stack `CREATE_COMPLETE` in about four and a half minutes. **Verification (v) took its first real reading: the one provisioned role carries the D13 boundary, delivered via the stack template** (the write-only configuration field injected as the `ToolingUserRole`'s `PermissionsBoundary`; the template's two conditional EMR roles declare NONE — AWS's template, recorded before it bites). **US-8 first said the opposite and the instrument was wrong** (Lesson 30): `iam list-roles` omits `PermissionsBoundary` by documented contract, so the check would misread every bounded role and had never had a role to be falsified by; fixed to one `get-role` per role, re-run `pass — all 1 bounded`, battery 0 FAILED. Owed: the off-VPN portal reading, then passes 3-5 + 5.1. — **Nineteenth (the off-VPN reading, the user's report verbatim, Claude's records under it): the STRONG form** — all three rungs (project provisions, space starts, **JupyterLab reachable and usable**) pass **identically on and off VPN**; no error exists to quote, which is the finding. `VpcOnly` governs the app's ENIs and egress, never the user's ingress (the Studio front-end under the portal session) — an architecture sentence until rung (c) worked with the tunnel down. **Fallback (ii)'s premise — VPC-only compute limits an off-VPN portal — measured FALSE for ingress**; `README.md` item 3 states the full reach; **the ripe decision, the user's: fallback (i)** (`DenyUserAccessFromUnauthorizedVPCs` on the domain execution role, re-keyed on the EIP, AWS's `*:user-*` condition kept) **versus recorded acceptance** — recommendation on record: (i). **The twentieth asks whether that recommendation can even be built, and measures both halves before a line of Terraform exists**: CloudTrail holds **770 events under the domain execution role with three distinct `sourceIPAddress` values** — the Elastic IP and two carrier addresses — each sitting's tunnel-up and tunnel-down halves in the same hour, so **the portal user's address does reach that role** and the re-keying is viable; every session name is `user-<uuid>`, matching AWS's third condition against real sessions. Riders kept narrow: CloudTrail's field is not the IAM key, and **no non-`user-` session appeared in a window with no project**, so the catalog carve-out is kept on AWS's authority rather than on a demonstration (Lesson 31). **Cost measured too and it is not where it looked**: a portal sitting is tens of MB (USD 0.0017); the gigabytes on the VPN host were step 5.0's push (Lesson 7) — and the correlation that produced them nearly went wrong because **CloudWatch renders local time while CloudTrail returns UTC** (Lesson 30). It also records, from the user, **why the VPN exists at all — it stands in for device trust, the DLP living on the managed endpoint** — now a row in `institutional-delta.md`. Two wording corrections inside this file are noted at their own entries. Owed: that decision, the probe's teardown confirmed (the SSO token had expired before the apps could be read), passes 3-5 + 5.1. — **Twenty-first (the plan read back against the closed create path; no AWS write): the post-merge tag check the merge owed** (identical tree hashes for `sagemaker-prereqs-v0.3.3` and `main`; the `v0.3.x` tags orphaned by the rebase — expected, never re-cut) **and a sixty-five-finding sweep** — eight parallel readers holding the session's measurements against the whole plan, every finding verified before an edit, all accepted, twenty-six files. The two classes a closed stage produces: promises already delivered that nobody struck (verification rows (v)/(vii)/(xviii)/(xx) annotated; the stages INDEX no longer "not started" and its Stage-7 cell re-cut to D36 §3 as amended; `AWS_STATE.md`'s Stage-6 row brought to the live state, the probe project named as the one entry expected to disappear; INT-12/15/16 residuals closed or restated; D21's crossed boundary dated) and premises the measurements falsified (the "no consumer" project CMK, the "service-created" bucket, the association "invitation", `lifecycleManagement true` in `SMUS.md`, "SEVEN ENTRIES" over six Bedrock names in both roster copies and the `PRICING.md` header, the 24-slice count, `versions.tf`'s one-resource claim, every unqualified VPN-only sentence — all qualified with the measurement, the INT-16 choice stated as the user's and open everywhere). `aws/INDEX.md`'s pre-1.3 US-2/US-3 semantics corrected, `studio.py`'s header brought to its real call set and US-8 given an explicit zero-roles `note`, Stage 14's onboarding brought to the measured order and complete field set, OQ 21 given its measured two-verbs input (`AddPolicyGrant`/`RemovePolicyGrant` are exercised by the estate's own member applies). **Lessons: 39 cut** (the wizard-field ladder + the late validator; Lesson 16's inverse), dated occurrences under **24** (absent event — documentation as the outside channel) and **30** (the API's own documented contract as the instrument limit). **Three owners added on the user's review question**: the OQ-21 role-policy reading as an owed-table row (its "measure at 1.5" had closed unmeasurable — no role existed), decision 1's third reading (does the chosen runtime flip `createEmrResourceInTooling`? — the no-boundary EMR roles go live if so), and `SMUS.md`'s category-2 promotion checklist (Lesson 39 made reusable). **Then the DNS Firewall allow-list**: the user's eighteen working-session hostnames read against it — eleven already reachable, **six registrable domains missing** (`public.ecr.aws`, on the `aws` TLD and so not under `amazonaws.com`; `astral.sh` and `rustup.rs`/`rust-lang.org`, installer hosts no index covers; `julialang.net`, the storage server beside the `.org` package server; `duckdb.org`, fetched at first query from inside the notebook) — and the **wildcard rule measured** rather than recalled, because `us-west.pkg.julialang.org` sits three labels under `*.julialang.org`: the `*` replaces an entire leftmost label and reaches every depth beneath it, never the apex, never a sibling TLD. `vpc-egress-v0.2.1` cut by Recipe B — the `validate` hook refusing commit 2 until both slices re-`init`, §7 exactly — PR #33, and applied to Sandbox after planning **all three** `[E]` slices first, because `buildbox`'s SSM-parameter AMI could have replaced the running build host; read back from the API at **38 names**. Two findings: the Resolver API **canonicalises every entry with a trailing dot**, so this slice's plan never converges and *"re-plan `No changes`"* is unavailable on both Interactive `egress/` slices (pre-existing; the dotted-FQDN fix is a hypothesis needing one apply cycle), and **the rule group binds the VPC id, not a route table**, so the list also filters `buildbox` in the isolated tier — two of the five names its own security-group comment says it pulls were not on it. Owed: unchanged, plus `development/egress/` bumped in code and not in the account. **Then step 4.3 RAN (2026-08-23) and superseded that entry's diagnosis**: a JupyterLab session (`uv pip install`, `apt install`) failed on names that were on the list, and the six domains were necessary and nowhere near sufficient. **DNS Firewall evaluates the WHOLE CNAME CHAIN**, so a listed name whose target is unlisted is blocked — and the Resolver query log reports that block against the **queried** name with the catch-all list id, while an ALLOW match populates no action at all, so 881 records held zero `ALLOW` rows and the log *appeared to refute the correct hypothesis*, which was duly dropped (Lesson **24** occurrence — the discriminator is a different question, not a better reading). **Settled by a paired probe from the buildbox over SSM**, one variable: `blobs.duckdb.org` (A records) resolved and `index.crates.io` (CNAME to Fastly) did not, under wildcards of identical shape; the one apparent counterexample, `julialang.net`, has no A record at all. **The consequence is design A's, not the step's**: fourteen of twenty-one ecosystem hostnames are CNAMEs into shared CDNs, so the list carried every index and no download path — pip, cargo, rustup, CRAN, apt and ECR Public have **no path under design A**, and the only fix is to allow self-service CDN namespaces, which ends the control (step 6.1's input). Also found: **`datazone.<region>.api.aws` blocked 52 times** — the estate's own workbench, on the `aws` TLD no wildcard reaches and uncovered by the `datazone` endpoint. **Redesign on the user's instruction, `vpc-egress-v0.3.0`**: module default EMPTY and the ALLOW rule gated on content (declare nothing, get a closed door), each Interactive slice owning its measured list — Sandbox 18, Development 16, the difference being zones the accounts really do not share. Applied to Sandbox (`0 added, 2 changed, 0 destroyed`, 18 read back `COMPLETE`) and **verified behaviourally 23/23 from inside the VPC**, `us-west.pkg.julialang.org` resolving through its own CNAME being the mechanism confirmed in the positive direction. Lessons: occurrences under **24** and **38**; no new number cut. **A SECOND SITTING THE SAME DAY — no AWS call, no SSO session — opened by a verification question rather than a failure (*how does one check that a name goes through a CDN*) and found the list's own DESCRIPTION wrong: EIGHT of its nine external names ARE CDN-fronted** — `datazone.<region>.api.aws` CloudFront, `pypi.org` Fastly, five Cloudflare, and only `us-west.pkg.julialang.net` a host of its own — **resolving solely because their owner FLATTENS**, serving the A record under the queried name so the chain never leaves the list. So what the firewall matches is the **shape of the DNS answer**, never the provider, and *“the names that do not go through a CDN”* — the previous day's own design instruction — describes almost nothing. **The part worth the entry is that the sentence which hid it was TRUE**: *“every name that worked answered with A records”* was correct, the rule derived from it admits only names that do work, and the defect was the MODEL a reader builds from both — which no gate in this repository reads. *Candidate lesson 40, left uncut by the user's standing call and recorded in the entry: **a true statement can carry a false model, and the model is what gets reused**,* the mirror of the morning's Lesson 38 occurrence. The rule became **three shapes**, the restored middle one being the estate's own (a CNAME inside the project's namespace is listable if EVERY hop is — why `julialang.net` sits beside the `.org`), and `dig +short` became `+noall +answer`, which is the form that shows the record TYPE. **Registered as `EXC-05` by SYMPTOM** — *a name still on the list, code untouched, stops resolving in the VPC* — because flattening is a switch its owner can turn off unannounced and the query log will again blame the queried name. **And given an instrument, `aws/dns-allowlist.py`**: chain, verdict, address, `--whois` owner; `DN-1`/`DN-2` (the EXC-05 alarm)/`DN-3`/`DN-4`, exits 2, first run 22 names and 20 flat, all pass. **`DN-3` closes the cost `vpc-egress` v0.3.0 had merely ADMITTED** (two caller-owned lists, nothing comparing them). Negative control run per Lesson 13 — `static.rust-lang.org` alone on a list returns `BROKEN` naming both hops, and `ok-chain` once every hop is listed. It needs **no AWS identity** (it photographs public DNS, not AWS) and `--from-api` is deliberately not the default, `egress/` being `[E]`. *Not established:* this is **not the VPC's resolver** — a clean run is a screen, a `BROKEN` row is real anywhere. **Then the laptop→project-storage sitting (2026-08-23, the user's requirement, three candidate strategies proposed and 1-A chosen):** the persona was measured to hold **no allow on the projects bucket and none on the project CMK**, the project role's trust admits **no human principal**, and SMUS had already parked the answer in the account — an **S3 Access Grants** instance plus one **location per project** whose vending role IS the project role, with zero grants; `GetEnvironmentCredentials` is deprecated inside AWS's own SDK. The library `s3-read-write/` (boto3-only, `benes3` conventions) vends through it. **The inline statement did not fit and the slice's own instrument had been saying so**: 10217 of 10240, 23 characters against ~251 — so the handshake became `awsds-org-project-storage-vending`, the slice's **first customer-managed policy**, one per member account, each naming its OWN instance (the half the inline form could not have). **Decision 4's stated blocker expired at Stage 3** and the README now says so. Applied by the user in three slices, members first; **provisioning proved on the ROLES, not the status API** — both members' `AWSReservedSSO_DataScientistAccess_*` list the policy. Price measured where no public page carries it (USD 0.03/1 000 AG requests, Price List only). Review: 7 findings applied, 3 refuted — the no-delete promise was **false** (`READWRITE` includes `s3:DeleteObject`), grants are **membership-blind**, and the on-VPN branch stays **hedged pending CloudTrail**. Raised **open question 22**: nothing here notices an AWS managed-policy revision, and the load-bearing ones sit on the project role (`…ProjectUserRolePolicy` v74, `…MachineLearningPolicy` v42). **Then the first probe run failed and found a defect in a control three days old:** the persona was explicitly denied `sts:GetCallerIdentity` **with the tunnel up and `curl checkip` reading the EIP** — two true readings of two different paths. With `egress/` up the laptop resolves through the VPC resolver its own client config names, so every service holding an **interface** endpoint answers PRIVATE and the call presents that endpoint's id — which `DenyControlPlaneOffVpn`'s third branch did not carry and **may never** carry (`[E]`, Lesson 3). Negative control: `dig sts…` → `10.20.12.229`, `dig s3…` → public, the gateway doing no private DNS — so 4d's fix was **complete for the path it was measured on**, not wrong. Fixed as a **swap** to **`aws:SourceVpc`** (subsumes the gateway ids, anchors on a `[P]` id per Lesson 3's own wording, and is SMALLER — a fourth condition would have re-hit the 23-character ceiling the same day); the plan-time guard swapped with it. **NOT APPLIED.** **APPLIED, and the path then RAN END TO END** (`0 to add, 6 to change`, each document 66 characters SMALLER, re-plan `No changes`, the new condition read back off the provisioned role): the laptop's vended identity is `datazone_usr_role_…/access-grants-<uuid>`, the grant scope composed correctly, and write/list/read-back passed against the project's own `shared/` — **closing the one unknown the strategy left open**, that SSE-KMS under the project CMK passes through a vended, scope-reduced session while the persona holds nothing on bucket or key. The tunnel question **decomposed**: `sts` rides the interface endpoint (`aws:SourceVpc`, measured by the failure), `s3control` has no endpoint here and rides the EIP — **and the CloudTrail reading (2026-08-24) REFUTED that second half**: `GetDataAccess` carries the host's PRIVATE address with a **GATEWAY** id (`s3-control.<region>` resolves inside the `pl-s3` ranges, so it takes the S3 gateway; "no interface endpoint" says nothing about the IGW), while `GetCallerIdentity` carries the STS **interface** id — two doors, one session, neither the EIP. **So only `sts` ever needed the swap**, and a client not calling STS would have run on the unchanged policy. The same listing gave an unplanned negative control (this session's own off-tunnel calls: public IP, no endpoint key) and one trap: `GetDataAccess` is an event NAME shared with Lake Formation — filter on `eventSource`. Owed: passes 3/5 + 5.1, 4.2's measurement half, **4.3's friction reading**, and the user's `f4734dd` `"*"` on the DNS allow-list, raised twice and left to them. **The twenty-ninth (2026-08-26) is a read-only sitting that answered one open question and caught two instruments lying — every AWS call a read, nothing applied.** **OQ 21 measured**: five roles, eleven AWS-managed documents, **zero inline policies** — `AddPolicyGrant`, `RemovePolicyGrant` and `DeleteEnvironmentBlueprintConfiguration` appear in none, but **`GetDomainExecutionRoleCredentials` is in `SageMakerStudioProjectUserRolePolicy` v74**, so every project role holds it; combined with the estate's own Terraform exercising the other three, **all four are "recorded ceiling, no blanket deny"** and the SCP decision stays the user's. **`US-8` was reading ONE of the stack's THREE roles** — a name filter (`datazone`) against two `AmazonBedrock*Role-*` beside it, so `pass` over a third of its own subject, with AWS's unbounded conditional EMR roles as the edge ahead; discovery moved to the service's tag (`AmazonDataZoneDomain`), bounded by IAM **path**, now `all 3 bounded`. **OQ 22 happened**: the provisioning policy is at **v82**, not the v81 that question and `s3.tf` name — benign diff, but a revision inside four days found only because a human opened the document for another reason. Also: the live battery **0 FAILED** with **zero running apps** (closing the off-VPN probe's teardown row) and **no `engineering` project in Development**; `awsds-sandbox-derived` holds the same 8 objects Stage 5 recorded, `derived/` and `scratch/` never written; **step 8.2 was DONE since 2026-08-21** and the stage file did not know. **Two of Claude's own sentences corrected in the entry** — 8.2 reported as pending, and a stage map that missed **Stage 16**, applied the same day, which leaves this stage a residue: SMUS made itself a Lake Formation administrator in Sandbox. PR [#45](https://github.com/felipenoris/AWS-DataScience/pull/45), the user merges.**The thirtieth (2026-08-26 evening) is the largest change since the stage opened, and it DISSOLVES decision 6 rather than settling it.** Verification (xviii)'s path reading delivered the input 2.6 was waiting for and found a **fourth designed destination** — the project's OWN enforced workgroup writing to `dev/sys/athena/` — so **the user re-homed the derived zone onto the SMUS project path**: `awsds-<env>-derived` and `awsds-<env>-athena` are removed and `DataScientistAccess` carries **no `athena:` action at all**. The account data CMK survives **because a measurement said so** (the sandbox lake encrypts under it) and its persona statement leaves as a **tightening**, not a leftover. Beside it: a deleted project keeps its prefix and nothing expires a current object (**OQ 25**); the promotion door was read in the blueprint and is **empty** — five git parameters all blank, `gitConnectionArn` not editable per project, so **no project in this domain can have a project repository today** (**OQ 26**, and `docs/SMUS.md`'s "S3 or Git" was prose the profile contradicts); and Stage 9's dated note was found covering Production while Staging sat in the same position. **The applies CLOSED the same night, in two acts.** `identity/sso`, both derived buckets and both key policies landed; **both workgroups REFUSED first** — `DeleteWorkGroup` counts **query history** as contents (4 and 2 executions against 0 named queries), no API deletes an execution, and `force_destroy` was unreachable because the resource had left the configuration carrying the flag, so **a destroy-time flag has to be set BEFORE the resource is removed** (lesson-shaped, not yet in `lessons.md`). Deleted with `--recursive-delete-option` and re-applied: **all three slices re-plan `No changes`**, `DL-8`/`DL-9` `pass` on the absence they were inverted to measure, and `AllowDataScientistUseViaS3` is gone from both key policies. PR [#47](https://github.com/felipenoris/AWS-DataScience/pull/47), the user merges. |
| Stage 7 — GitLab, Runners and ECR | — | *no entries yet* — will carry the **internal CA fingerprint** (D36) |
| Stage 8 — CI/CD pipelines | — | *no entries yet* |
| Stage 9 — Deployment target platforms | — | *no entries yet* |
| Stage 10 — Workflow orchestration and promotion | — | *no entries yet* |
| Stage 11 — Data protection and DLP | — | *no entries yet* |
| Stage 12 — Observability, governance and FinOps | — | *no entries yet* |
| Stage 13 — Public-facing web tier | — | *no entries yet* |
| Stage 14 — Per-business-unit Sandbox vending | — | *no entries yet* |
| Stage 15 — GuardDuty org-wide | — | *no entries yet* |
| Stage 16 — The sandbox lake | [log-stage-16-sandbox-lake.md](log-stage-16-sandbox-lake.md) | **One dated section, 2026-08-26 — the whole stage in one day, four sittings, written by Claude at pass 6's close on the user's request**: the three applies and the Lake Formation admin-seat finding (`v0.4.0` adoption → `v0.5.0` ownership), the contrast pair and the laptop test, the project wiring with the connection form's four fields and the CloudTrail both-doors reading, the notebook readings verbatim (the auto-vend plugin finding), and the revoke exercise — `SL-4`'s defect found and fixed against the live anomaly, the ≤19 s vend-door window, the bearer residual exercised through expiry |

## How an entry gets written — two modes, both requested

Claude writes into a `docs/log/log-stage-*` file **only on a request in that sitting**, and there are two
shapes the request takes. **Ask for whichever fits; the difference is who holds the pen, not what is
allowed.**

| Mode | When it is the right one |
|---|---|
| **Claude drafts, the user pastes** | The user is at the keyboard and wants the file to stay in their hand. Also the only mode available when the entry is about acts Claude did not observe |
| **Claude writes the file directly** | The entry is long, or it interleaves Claude's own readings with the user's, or it amends an entry already there. The user says so explicitly — "escreva no log", "atualize o log" — and Claude states what it wrote |

Either way the entry names its hands (see "Provenance" above), and either way **the format rules below
apply** — they were the draft's format and are now the entry's, moved here from `CLAUDE.md` on 2026-08-15
so the instruction sits with the files it governs. In the drafting mode, the entry additionally goes to the
chat as **one single fenced block** so it is pasted untouched:

- **English and Markdown**, whichever mode is in use — the chat is in Portuguese and these files are not.
  **In the drafting mode, one single fenced code block** and never ordinary chat prose, so the user copies
  it in untouched and nothing is reformatted on the way: a ` ```markdown ` fence, or a longer outer fence
  (` ````markdown `) when the draft itself has to contain one, so the nesting survives.
- **No identifiers — and the redaction has a shape, because it is applied to *evidence*.** An account id
  becomes the account's AWS `Account.Name` in angle brackets — `<Audit Account>`, `<Sandbox Account 1>`,
  `<Management Account>`; the names are in [`docs/ORGANIZATION.md`](../ORGANIZATION.md), and which id is
  which account is **resolved from the entry's own surrounding text or from `aws/output/`, never guessed**.
  An **e-mail address inside an ARN** becomes that user's role — `<control tower admin user>`. **Nothing
  else in a pasted output is touched**: the `AWSReservedSSO_*` suffixes, the policy, organization and root
  ids, the error wording and the encoded authorization failure blobs stay verbatim, because a log that has
  been tidied stops being evidence. And the entry **declares the substitution once**, in its provenance
  note, instead of at each line — the tenth entry of [`log-stage-04-vpn.md`](log-stage-04-vpn.md) is the
  wording to copy.
- **Concise by default:** the command, the outcome, and any finding that does **not** survive elsewhere.
  Leave out what `aws/output/` already holds (it is regenerated on demand), what a `docs/plan/` file explains,
  and the reasoning behind a choice — a log entry restating either is a copy that will go stale. Prose
  belongs in `docs/plan/`; the log carries *what happened, in order*.

**The identifier rule has a gate, and it did not always** (2026-08-17). `./scripts/check-identifiers.py`
runs in `make check` and in `pre-commit`, over **every tracked file** rather than over `docs/` alone, and
fails on a standalone twelve-digit number or an e-mail address. It exists because the rule above was held
by attention alone and attention had already missed **three** log files — twenty-four ids and two
addresses, each with a correctly-elided neighbour a few lines away, which is Lesson 14 exactly. Two things
it does not do: it cannot reach what is already in **git history**, and it cannot decide *which* account an
id belongs to. And a hit inside an existing entry is still an edit to a stage log, so it waits for the
user's request like any other — the gate names the line, it does not authorise the fix.

**Adding a stage log:** the user creates `docs/log/log-<same-filename-as-the-stage-file>` and copies the
two-line header from an existing one; Claude replaces that stage's `—` above with the link plus a one-line
`Records` summary, and keeps it current as the file grows. A row whose `Records` cell does not say what is
inside the file defeats the point of the index — *"stage not started"* against a file with five hundred
lines in it is the failure this rule exists to prevent, and it had already happened once.

**Not in these files:** break-glass *use* is also recorded here (date, reason, actions), but the procedure
itself is [`docs/plan/runbooks/break-glass.md`](../plan/runbooks/break-glass.md); why the plan changed is
[`docs/plan/history.md`](../plan/history.md).

---

*Stage index: [docs/plan/stages/INDEX.md](../plan/stages/INDEX.md) · Plan core: [GENERAL_PLAN.md](../GENERAL_PLAN.md)*
