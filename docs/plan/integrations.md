# Cross-account integrations to prove

**Each row has a stable ID.** Reference an integration as `INT-11`, never as "row 11" —
inserting a row must not renumber a reference. Stage files list the IDs they prove.

---

### 4.4 Cross-account integrations to prove

The account split (see `README.md`, "Account segregation") is the right call, and it is not free: it turns
several things that are one API call inside a single account into a resource policy, a KMS grant and a RAM
share spanning two. Earlier versions of this plan carried these as "verify this rather than assume" notes
scattered across five stages. Scattered, each one is an evening lost in isolation and re-derived from
nothing. Consolidated, they are a checklist with a stated fallback per row — which is the shape that
survives contact with a Tuesday night.

| ID | Integration | Stage | Fallback if it does not work |
|---|---|---|---|
| **INT-01** | Studio custom image pulled from the **Production** ECR (D14) into the Sandbox **and Development** domains. **The repository it is pulled from does not exist until Stage 6's own pass 0** (Stage 7 step 5.a, applied there — re-cut 2026-08-21): `awsds-prod-ecr-base` and `awsds-prod-ecr-dev-env` are created by that apply, and Stage 6 step 5.0 is the first push. **BOTH EXIST SINCE 2026-08-21** — `production/registry/` applied, `14 added`, re-plan `No changes` — so the half of this row that was a prerequisite is closed and what is left is the pull itself, which needs an image to pull. **`SC-7` already reads both repositories cross-account from both Interactive accounts** (0 images visible), which proves the **repository policy** and nothing else — `describe-images` decrypts no layer. **The sentence this replaces claimed it proved the repository policy and the key policy *agree*, and that was wrong twice** (corrected 2026-08-21): the read never touched the key, and a cross-account ECR pull **needs no KMS grant at all** — ECR creates two grants on the repository's key for itself at repository creation and makes the `Decrypt` call on the caller's behalf (`docs/REFERENCES.md`, the ECR-encryption row, which is also what deleted half of Stage 9's old step 7). The registry key's consumer grant was narrowed to CodeArtifact alone the same day. **The measurement is Stage 6 step 5.1's first real cross-account pull**, and if it fails on KMS the page is wrong and the fix is one service name | 6 | An ECR cross-account replication rule into a repository in each Interactive account. Not a pipeline |
| **INT-02** | CodeArtifact consumed cross-account from Sandbox and Development — domain policy *and* KMS key policy. **The PRODUCER half is applied (2026-08-21, Stage 6 pass 0)**: the domain `awsds-prod-packages` with `pypi` (`public:pypi`) and `crates` (`public:crates-io`), a domain policy granting the two consumer accounts `GetAuthorizationToken`/`GetRepositoryEndpoint`, a repository policy granting the read family on each, and `alias/awsds-prod-registry` granting `kms:Decrypt` **under `kms:ViaService = codeartifact`** — which is the pair this row is actually about, and CodeArtifact is where it is genuinely a pair: its assets are encrypted under the domain key and a cross-account read decrypts them **as the reader**, so an account that may read and cannot decrypt gets an `AccessDenied` on a path everyone believes is open (Lesson 28). **ECR is not that shape** and the grant was narrowed off it on 2026-08-21 — INT-01's row carries why. **The consumer half is HALF proven, the same day**: `./aws/supplychain.py` `SC-7` reads the ECR repository (0 images — nothing pushed yet) and gets an answer from the CodeArtifact endpoint **from both `awsds-infra-sandbox-1` and `awsds-infra-dev`**, so the *authorization* path across the account line works in both directions. What is still unproven is the part that needs bytes and a network: an actual `pip install` from a project subnet under design B, at pass 4 | 6, 7 | Bake the packages into the dev-env image (`docs/plan/architecture.md` §4.3), which is the delivery mechanism anyway |
| **INT-03** | Lake Formation cross-account shares through AWS RAM — **three at N=1, and N+2 in general** (D22, D35: one share per business unit's Sandbox): Data Governance → Sandbox (read), → Development (read), → Production (read + governed write, the producer path) — resource links and `IAMAllowedPrincipals` have version-dependent behaviour, and the *write* grant is the least-travelled variant. **Two of the three are APPLIED, 2026-08-19 (Stage 5 pass 3):** Sandbox and Development, as LF-TBAC expressions rather than named resources, **four `LakeFormation-V4-*` RAM shares `ACTIVE` and held by both consumers with no invitation anywhere**. **Two corrections the row did not anticipate** — every cross-account grant carries the **grant option** (the receiving account's own data lake administrator must pass it on, and can only pass on what it received with the option), and the expression needed a `layer` gate or it matched the drop-box. **And the receiving side needs a data lake administrator before a share is visible there at all** — both consumers read `DataLakeAdmins: []` until pass 4, which is why their catalogs were empty while their RAM held the shares. **Both consumer legs are now COMPLETE, 2026-08-19 (Stage 5 pass 4b)**: each account has its own `DataLakeSettings`, two resource links, and the local re-grants without which a held share is unusable — `glue:GetTables` through the `curated` link returns `sample_trades` from both accounts. **The re-grant is a PAIR and the row did not say so**: `DESCRIBE` on the resource link *as a local database object*, plus the permission on the target through the owner's `catalog_id`; the first half is the one that gets missed and its symptom is *no database visible at all*. **What travelled is metadata, and the row should not be read as more**: the pandas pair — the proof this column asks for — is 4d's and needs a persona session. Production's leg remains Stage 9's | 5, 9 | None; instead, prove each grant restricts with the "read it with pandas" test *before* believing it, and prove the write path with a job that writes a curated table cross-account |
| **INT-04** | Model Registry: reading or approving a Production model package group from Development (D17) | 9, 10 | Registration happens only under the pipeline's own Production role; the Development side never writes to the registry |
| **INT-05** | S3 bucket policies (now mostly in **Data Governance**, D22) whose `aws:SourceVpce` condition must admit the endpoints of *every* consuming account — every business unit's Sandbox (D35), Development, Production — plus the WireGuard Elastic IP (D18). **The IDs must come from the `[P]` S3 gateway endpoints in each consumer's `foundation/`, never from the `[E]` interface endpoints in `egress/`**, whose IDs change on every `make up` — and which now sit in a different account from the policy. **Built 2026-08-18 (Stage 5 pass 1)** for Sandbox and Development, exactly as this row requires — the ids read from each consumer's `foundation/` state; the branches of `DenyOutsideTrustedNetworks` are listed in `terraform-live/data-governance/data/README.md`. **So the next column's first option is the applied shape, not a fallback.** Production joins at Stage 9. **PROVEN BEHAVIOURALLY 2026-08-20 (pass 4d), and by a call nobody planned as this row's proof**: the persona's drop-box `PutObject` succeeded from the tunnel, and every lake bucket — the drop-box included — carries `DenyOutsideTrustedNetworks`, so the call had to satisfy the `aws:SourceVpce` branch. It did, arriving through **Sandbox's own `[P]` S3 gateway endpoint**: this row's mechanism, end to end, on a real write. **The finding this row now owes its readers**: the same measurement showed the identity side needs endpoint ids too — `DenyControlPlaneOffVpn` was amended the same day to test `aws:SourceVpce` over the **VPN home's** endpoints. So `aws:SourceVpce` is no longer a bucket-policy concern only, and the two lists are on **different axes**: this row's is *who consumes the lake*, the permission set's is *what is on the network path*. They intersect by coincidence today (the single VPN home is also a consumer) and Stage 14's second home is where they part — read Lesson 33 before merging them | 5, 9, 14 | Replace the condition with `aws:SourceVpce ∈ list` **or** `aws:SourceIp = <WireGuard EIP>`, maintained as a variable per consuming account rather than edited by hand; or anchor on `aws:SourceVpc` (the VPC ID, also `[P]`) |
| **INT-06** | Whether S3 **console** browsing survives the `aws:SourceVpce` deny at all — console operations issued by the console backend carry neither the endpoint nor the user's source IP | 9 | Tell users to use the CLI over the tunnel, and write that in `README.md` rather than leaving a broken console as a surprise |
| **INT-07** | **Staging** (D20) consuming Production: pulling the application image from the Production ECR and reading the approved model version from the Production Model Registry, both under the pipeline's Staging role | 8, 9 | Replicate the image into a Staging ECR repository as part of the promotion, and pass the model artifact's S3 URI explicitly instead of resolving it through the registry |
| **INT-08** | The deploy roles assuming **across** accounts — the runner is in Production (D14), so the trust policies run Production → Staging and Production → Production | 8 | None needed; but write the two trust policies as separate roles with separate names, so an audit can tell which one was used |
| **INT-09** | **Development ↔ Production VPC peering** (D21): Studio in Development must reach GitLab in Production to clone and push — the same narrow, per-subnet route shape as the Sandbox peering | 3, 7 | None at the network level; if the second peering proves troublesome, the mirror-to-GitHub policy from Stage 7 step 7 is the interim path for Development commits |
| **INT-10** | **The ingestion drop-box pickup** (D25): the Production job role reading and deleting from a Data Governance prefix that the Interactive-OU roles write to — a bucket policy with two asymmetric statements, plus a grant on the drop-box KMS key. **AMENDED 2026-08-19 (Stage 5 pass 4c): that description names only the RESOURCE half, and a cross-account permission needs both.** Each of this row's three principals needs a matching **identity**-side allow in its own account or the call is denied with the resource side perfect. Where they are: the writer's is **applied** (`WriteIngestionDropBox` + `UseLakeDataKeyViaS3` in `DataScientistAccess`, Stage 5 pass 4c; the KMS Sid was `UseLakeZoneKeyViaS3` until 2026-08-19's revision); the maintenance role's is same-account and lives in its inline policy; the **pickup's is Stage 9 step 3.1**, which already names it as "the identity half of Stage 5's statements" — so the plan was right where the role is authored near its grant and wrong where the halves sat five stages apart. **Nothing here is proven until both halves are exercised by one call** (Lesson 28, amended) | 5, 9 | Have the Interactive-OU roles write to a bucket in Production instead, and accept that the file lands outside the governed account before it is curated. Strictly worse — it puts an ungoverned copy in the deployment target — so treat it as a stopgap, not an alternative |
| **INT-11** | **Organization-wide sharing enablement for Lake Formation** (D22): `ram:EnableSharingWithAwsOrganization`, LF **cross-account version 3 or above** (required to grant to an Organization or an OU rather than to an account list), and `AWSLakeFormationCrossAccountManager` on the Data Governance grantor. **Version 3 is also what lifts the mechanical ceiling on the number of consumers** (D35) — which is not the same as answering *whether* every business unit should get the same data, a governance question that arrives with the second unit and belongs to the governance manager. **Measured 2026-08-14 and it changes which half is at risk:** the version already reads **`4`** in Data Governance, with `SET_CONTEXT: TRUE`, in an account with no lake — nobody set it, so **nobody is defending it**, and Stage 5's `aws_lakeformation_data_lake_settings` resets the whole `Parameters` map if it names `admins` without them. **The RAM half is done — 2026-08-14, Stage 1d step 11.1**: `ram enable-sharing-with-aws-organization` returned `true`, `ram.amazonaws.com` is the eighth trusted-access principal (INV-09 restated) and `AWSServiceRoleForResourceAccessManager` exists in Management. **So both organization-level halves are now settled and every remaining risk in this row is Stage 5's**, which is a change of location and not of size: the version is defended by nobody, and the RCP is untested against `sts:SetContext`. **And version 4 vends credentials through `sts:SetContext`, which `awsds-org-rcp-perimeter` names**: in-org principals populate `aws:PrincipalOrgID` and should be unaffected, but that is the reasoning that failed for `AssumeRoleWithSAML` and cost every SSO user access to six accounts, so the first cross-account grant tests it rather than assuming it. **Re-read 2026-08-17, and the second reading adds two things the first did not look at** (Stage 4's control-plane pair, incidentally — the full `GetDataLakeSettings` response rather than just its `Parameters`): `CROSS_ACCOUNT_VERSION: 4` and `SET_CONTEXT: TRUE` are **confirmed a second time, from a different principal**, so what Stage 5 must preserve is now a written value rather than a remembered one; and **the settings this row never examined say Lake Formation is not enforcing at all** — `DataLakeAdmins: []`, with both `CreateDatabaseDefaultPermissions` and `CreateTableDefaultPermissions` granting `ALL` to `IAM_ALLOWED_PRINCIPALS`, the untouched default under which every new database and table defers to IAM and an LF grant governs nothing. **That turns this row's Stage 5 obligation from one act into three that share an apply and an order**: name the admins (nothing is changeable until somebody is one), preserve `Parameters` (this row's original warning), and clear the two defaults **before any database exists** — they act at creation time, so clearing them afterwards does not reach what was already created. Recorded in [`AWS_STATE.md`](../AWS_STATE.md) §C. **CLOSED 2026-08-19 by the first real cross-account grant (Stage 5 pass 3), and both halves came back clean**: `Parameters` still read `4`/`TRUE` after the shares existed — the third of the three brackets — and the org-sharing path produced **four `ACTIVE` shares held by both consumers with ZERO invitations**, so the fallback in the next column is not being paid. The RCP's `sts:SetContext` statement left the metadata path untouched; the *credential-vending* half of that action is exercised by pass 4's first query, not by a share — **which is 4d and has still not happened**. **And one thing this row asserted implicitly turned out to be false — measured 2026-08-19, at pass 4's before-reading: the `Parameters` hazard is SYMMETRIC.** Everything above describes it as a property of Data Governance, because that is the account whose settings were read. Both consumer accounts turn out to carry `CROSS_ACCOUNT_VERSION: 4` and `SET_CONTEXT: TRUE` **of their own**, set by nobody in this repository and defended by nothing — and pass 4 writes an `aws_lakeformation_data_lake_settings` into each, which replaces the whole structure exactly as it does in the producer. So the reset can happen in **any account that gains this resource**, one step further from where anyone looks, and the values that go into `parameters` are copied from *that account's own reading*. `DL-5` now reports per account for this reason; Stage 9's 1.5 carries the same instruction for Production and Staging | 1b, **1d step 11**, 5 | Grant to explicit **account IDs** rather than to the OU, and accept a RAM invitation per share. A handful of shares accepted by hand, once, is survivable — but the invitations reappear whenever a share is recreated, so it is a tax on every rebuild, and one that grows with the number of business units |
| **INT-12** | **The unified domain's account associations** (D26): the DataZone V2 domain in **Data Governance** associated through RAM with **every Sandbox plus Development — N + 1 accounts, not two** (D35: one Sandbox per business unit, N=1 today), each needing its own blueprint configuration against the same single domain, and blueprints provisioning project environments into those accounts under their provisioning roles. This is the row that carries the whole D26 shape — if associations do not work, the domain is a catalog with no compute attached. **The association itself has no public API (read 2026-08-16)**: it is a console flow in which DataZone creates the RAM share on the domain's behalf — a by-hand step in the middle of an otherwise Terraform stage, recorded rather than hidden (Stage 6 step 1.3). **CLOSED 2026-08-21: the domain exists (`awsds-studio`, `V2`, `AVAILABLE`) and BOTH Interactive accounts are associated** — measured functionally, not off a console label: `list-environment-blueprint-configurations` succeeds from each member against `dzd-d8yrvx1ko7im6o` and returns empty. **Two particulars this row used to carry are wrong.** There is **no 7-day invitation window** — the share is created organization-scoped into an organization with RAM sharing enabled (Stage 1d step 11), so it **auto-accepts**, raises zero invitations either side, and the clock never starts; the accept pages the console documents are never reached. And the share carries **`AWSRAMPermissionsAmazonDatazoneDomainExtendedServiceAccess`** — **152 actions against the resource-type default's 111** — which is a **ceiling on the share, not access** (Lesson 28), and the reason **open question 21** now exists. What is left of this row is per-account blueprint configuration (Stage 6 step 1.4), not the association. **And the forbidden fallback is now CLOSED rather than merely intended** — the same day, the identical `CreateDomain` request shape replayed as `awsds-policy-canary` returned an `AccessDeniedException` **naming `awsds-org-scp-baseline`'s policy id**, so a second domain outside the `Data` OU is not a degraded mode anyone can slip into: it is refused. Taking this row's fallback therefore still means amending that document first, deliberately, exactly as the column says | 6 | One V2 domain **per Interactive account**, no associations — losing the single portal and the cross-account project model, but keeping projects, the catalog and the Terraform module. The domain would then sit on the lifecycle axis after all, which D26 rejects on principle, so treat this as a degraded mode rather than a design. **Taking this fallback requires amending a policy first (added 2026-08-08):** Stage 1c step 7 denies `datazone:CreateDomain` at the organization root with a carve-out for the `Data` OU only, so a second domain in an Interactive account is *forbidden* until the `Interactive` OU is carved in or the deny is narrowed to `Workloads`. Amend it deliberately and record why — do not discover it as an `AccessDenied` mid-Stage-6 |
| **INT-13** | **Unified Studio project git ↔ the self-hosted GitLab** (D14, D26): projects attach a repository through CodeConnections, which for self-managed GitLab requires a **CodeConnections host** reaching the instance in Production's private subnet. **Since D26 the domain is in Data Governance, which has no VPC** — so the host has nowhere to attach, and this row is expected to fail rather than merely at risk | 6, 7 | Accepted as the normal path, not as a fallback: the project keeps its default repository and the push into GitLab is a manual `git remote add` + push. The D21 graduation is a rewrite through a repository either way, so what is lost is convenience, not a control. Giving Data Governance a VPC and a peering to buy it back was considered and declined (D26) |
| **INT-14** | **The pipeline creating `awscc_mwaaserverless_workflow` in Production** (D28): the Cloud Control path from the deploy role — verified to exist 2026-08-08, not yet verified to *apply* cleanly under a CI role with a permission boundary. **Re-verified to exist 2026-08-16** (the `awscc` schema and the `mwaa-serverless` CLI both present; the classic provider merged only service scaffolding — PR #45256 — with issue #45254 still open); the boundary must admit the Cloud Control actions **plus the `airflow-serverless` prefix**, which is the service's real IAM name. The apply is Stage 10 pass 2's, run by the pipeline because the pipeline running it *is* the proof | 10 | `aws_cloudformation_stack` wrapping `AWS::MWAAServerless::Workflow`; second fallback, provisioned MWAA (`aws_mwaa_environment`, `[E]`, metadata-database caveat in force — D7) |
| **INT-15** | **Whether D13 survives D26: who authors the project execution roles.** D13 requires that the roles running notebooks hold *no* S3 access to Lake Formation-registered prefixes. Until D26 those roles were written here, in Terraform; now the blueprints configured for the account — `Tooling` above all — provision the project environment **and its roles** (the enabled set's one copy is [`docs/SMUS.md`](../SMUS.md); there is no "ML blueprint", and of the two Lakehouse-named ones only `DataLake` is enabled). Verify what is actually attached to a provisioned project role, and whether a customer-managed policy or a permissions boundary can be imposed on it without the blueprint reconciling it away **THE MECHANISM WAS FOUND ON 2026-08-21, AND IT IS BETTER THAN FALLBACK (i)** — measured against the pinned provider schemas rather than reasoned: `awscc_datazone_environment_blueprint_configuration` carries **`environment_role_permission_boundary`**, a field the `aws` provider's equivalent resource does not have. So the boundary is not attached *after* DataZone writes the role and hoped to survive; **the service is told to impose it while creating the role**. The question does not disappear, it gets narrower and stays a measurement: does the boundary survive a reconciliation, and does the role AWS builds still work under it? `awsds-<env>-project-boundary` is applied in both Interactive accounts since the same day, and `./aws/studio.py` `US-8` is what reads it back per project role | 6 | In order: (i) **the blueprint configuration's own `environment_role_permission_boundary`** — found 2026-08-21, and it demotes the rest of this chain from *plan* to *contingency*; (i-b) failing that, attach the same boundary to the project role through the `sandbox/sagemaker/` and `development/sagemaker/` prerequisite slices, which is the mechanism least likely to be overwritten; (ii) if the blueprint's own grants cannot be narrowed, fall back to Lake Formation **hybrid access mode** for the affected prefixes and record it as a D13 exception rather than a silent widening; (iii) worst case, keep the lake's registered prefixes out of every blueprint-provisioned role's reach by putting them behind a *separate* LF share that is granted per project rather than per account |
| **INT-16** | **Whether an `aws:SourceIp` deny on a permission set actually gates the Unified Studio portal** (D26, Stage 4 step 8). The portal is opened by signing in to Identity Center and following the domain URL, not by an IAM-authorized API call under a permission set — so the control `docs/plan/architecture.md` §3 claims for the "API and portal" path may cover the API half and not the portal half, which is the data scientist's primary working surface | 4, 6 | In order: (i) an IdC-level or DataZone-level network restriction, if one exists — **one candidate is now documented (read 2026-08-16, Stage 6 step 1.7): the SMUS network-isolation guide ships a network-conditioned deny for the domain execution role** (`Sid` `DenyUserAccessFromUnauthorizedVPCs`), which would need re-keying from `aws:SourceVpc` to the WireGuard `aws:SourceIp` list — **and the re-read of 2026-08-19 changed what is left to prove**: the policy carries **three** conditions, not the two recorded here before, and the third is `StringLike aws:userid = *:user-*`, which confines the deny to **portal users** and spares the catalog service running on the same role. So the on-behalf carve-out is **already in AWS's shape**, and its mechanism is the `userid` form rather than `aws:ViaAWSService` alone; what remains for adoption is proving that pair against Stage 4's, not inventing one; (ii) accept that the portal is reachable from anywhere with a valid IdC session and rely on the fact that *the project compute it fronts* is VPC-only and its data access is LF-gated — stating plainly in `README.md` that "all access through the VPN" holds for the AWS control plane and not for the portal; (iii) the honest fallback of last resort, which is to say so in the threat model rather than to leave a control listed that nobody implemented (the same discipline Stage 11 step 3 applies to Studio file download) |
| **INT-17** | **What makes the `dev-env` image selectable, now that the domain is blueprint-provisioned** (D26, Stage 8 step 1). Until D26 the chain was ours end to end: `aws_sagemaker_image` → `image_version` → `app_image_config`, attached to a domain this repository wrote. The attachment point now lives inside the per-project SageMaker AI domain that the **`Tooling` blueprint** provisions. Verify *which* call puts an image in the selector, whether infrastructure we own can make it, and whether it survives a blueprint reconciliation. **The attachment point is now documented (read 2026-08-16): SMUS BYOI attaches the image to the Tooling-provisioned SageMaker AI domain in the member account** (image + version + app image config, then the domain's `CustomImages`) — what stays for execution is whether that update survives reconciliation, and INT-01's cross-account half: same-Region is documented as required, cross-account pull is documented only for RStudio. **Distinct from INT-01**, which is about pulling the image cross-account and whose fallback (ECR replication) does not touch this question at all | 6, 8 | In order: (i) configure the image through the **project profile** in Data Governance, so it flows in with the blueprint; (ii) register the image resources in the target account's `sagemaker/` prerequisite slice and reference them, checking whether the blueprint reconciles the reference away — the same mechanism-of-last-resort as INT-15; (iii) if the image can only be chosen **per JupyterLab space**, accept it: the pipeline and its approval are unaffected, but "identical runtime by construction" (D17) degrades to a convention the user must follow, and that is recorded as a weakening rather than absorbed; (iv) worst case, the pipeline delivers everything up to the approval and the **steward performs the registration by hand** — the control survives, the automation does not |
| **INT-18** | **The dev-env deploy roles reaching from Production into the Interactive accounts** (Stage 8 step 1 step 6): the runner lives in Production (D14) but registers the approved image in **Sandbox and Development**, so trust runs Production → Interactive. Every other artifact flow in this plan runs the other way, and this one hands the supply chain a write path into the accounts where people work | 8 | Roles named per target (`awsds-deploy-devenv-sandbox`, `awsds-deploy-devenv-dev`), scoped to the dev-env slice and to `sagemaker:*Image*` on the named resources — never a general deploy role reused. If the direction proves uncomfortable, the fallback is to have each Interactive account's `[P]` slice hold the image version as a variable and to apply it by hand after approval, which costs the automation and keeps the boundary |
| **INT-19** | **The internal CA trusted on every client surface** (D15 as revised 2026-08-09; custody in D36). The root is generated in **Production**, but the clients that must trust it are the **laptop** (over the VPN), the **`dev-env` image** (built in Production, run in every Sandbox and in Development) and the **runners** — so a Production-owned trust anchor has to be present in N+2 accounts and in one artifact that is rebuilt by a pipeline. It is not an IAM crossing, which is exactly why it is easy to miss: nothing denies access, the TLS handshake simply fails, and it fails at the moment somebody runs `git clone` inside a notebook. Each surface has its own opinion about CA bundles — the macOS keychain, `git http.sslCAInfo`, Python's `certifi`/`REQUESTS_CA_BUNDLE`, Docker's `/etc/docker/certs.d/`. **When each surface takes the root, written here so the obligation is recorded at the receiving end (added 2026-08-21, D36 §3 amended):** the laptop at Stage 7 step 2.5, the runner in Stage 7 step 6's user data, and the **`dev-env` image at Stage 7 step 2.6** — *not* at Stage 6 step 5.0, which builds that image a stage before this CA exists and leaves an empty CA-install layer for 2.6 to fill | 7 | In order: (i) bake the root into the `dev-env` image and the runner user data, and publish it from a `[P]` S3 object or SSM parameter so there is one source rather than N pasted copies (Lesson 14); (ii) if a surface cannot take a custom root, that endpoint stops being HTTPS-internal for it — reach it through a path that terminates elsewhere, or accept plain HTTP inside the tunnel *for that surface only* and record it; (iii) the escape hatch of last resort is D15 phase 2 arriving early — register the domain and use public ACM, paying the name leak knowingly rather than by default |
| **INT-20** | **Managing the organization's policies from the Identity account** (D10, D34; Stage 2 step 5). Permission sets reach that account through the Identity Center delegated administrator, which 1b step 1 already proves — **SCPs, RCPs, tag policies and the declarative policy do not**. They are AWS Organizations objects, and reaching them needs a second, different mechanism: a **resource-based delegation policy** on the organization (`organizations:PutResourcePolicy`), written from Management by `AWS Control Tower Admin`, which **no stage before Stage 2 performs**. Two things make this a row rather than a step note: it must survive the Control Tower landing zone, and AWS's own delegation examples grant the delegated administrator CRUD over **every** policy in the organization — Control Tower's guardrail SCPs included — so it widens the Identity account's blast radius a second time, with only 1b step 8.3's alarm above it. The nesting also bites here: a delegation naming a single OU *"excludes child OUs and accounts under child OUs"*, and this organization is two levels deep (D23). **And there is a sharper edge, unverified and worth verifying first: six of the ten documents — the three root SCPs, the RCP, the tag policy and the declarative policy — are attached to the organization *root*** (1c step 7.8; this sentence used to say "the two documents that matter most"). A delegated administrator's reach over root-level attachments is exactly the place AWS reserves operations for the management account, so the plausible outcome is not "the delegation is hard" but "the delegation works and still cannot touch a root attachment" — which now costs most of the slice, not a corner of it. Answer that before writing any of this slice — the whole design of Stage 2 step 5.5 turns on it, and Stage 2 step 5.0 is the answering procedure | 2 | **Half the stage lands anyway, by construction:** `terraform-live/identity/` is two slices (Stage 2 step 5), and `sso/` depends only on the Identity Center delegation 1b already proved. `identity/org-policies/` then stays empty — with a `README.md` naming this row as the blocker — the four policy types **stay console-managed** exactly as Stage 1c left them, and Stage 2 step 9.2's wildcard check degrades from a script to a manual review: strictly worse, and recorded as such rather than absorbed |

INT-05, 6 and 11 are the ones most likely to surface as an `AccessDenied` — or, in INT-11's case, as a
share that appears to have been granted and simply never arrives. INT-07-10 are the price of real
environment separation: the promotion crosses an account boundary twice, the lake is consumed
cross-account from everywhere (D22), and each crossing is a place where a resource policy can be missing.
INT-12-18 arrived with D26-D28 and are the Unified Studio set — INT-13 is the one with no workaround
that preserves convenience, and INT-14 is the one that decides whether D7's alternative A ships in
Terraform or in a wrapper. **INT-19 arrived with D15's revision and is the odd one out: the only row here
that is not an IAM or resource-policy crossing.** It fails as a handshake error rather than as an
`AccessDenied`, which puts it outside the habit the rest of this table trains. Check them deliberately
rather than by symptom.

**INT-15 and 16 are different in kind from the other seventeen and should be read first.** Every other row
is an integration that either works or has a fallback that costs convenience. These two are rows where a
*stated control* may not exist:

- **INT-15 is the one that can invalidate a stated objective of [`docs/plan/objectives.md`](objectives.md).** "Fine-grained access
  control" is real only because of D13, and D13 is real only if the execution role's S3 permissions can be
  constrained. D26 moved the authorship of those roles from this repository to a blueprint, and nobody has
  checked what that changed. If INT-15 fails and no fallback holds, the correct response is not to widen
  D13 quietly but to record that column and row filtering is an entitlement mechanism whose enforcement is
  incomplete — which is the same honesty Stage 11 step 3 demands about file download.
- **INT-16 is the one that can invalidate a stated objective of the same file** — "all user access to the
  cloud infrastructure will be performed through a VPN". It is cheap to answer (open the portal with the
  tunnel down) and it is answered in two halves: the deny, the API/console pair and the IdC sign-in
  reading at Stage 4, and the portal itself at Stage 6 step 1 — the first moment that surface exists.

Both follow the pattern of Lessons 3 and 11 in `CLAUDE.md`: a decision taken for good reasons moved
something, and the conditions that referenced it were not re-checked. Here the thing that moved was *who
writes the IAM policy* (Lesson 11) and *what a session is authenticated by*.

---

*Plan core: [GENERAL_PLAN.md](../GENERAL_PLAN.md) · Decisions: [docs/plan/decisions/INDEX.md](decisions/INDEX.md) · Stages: [docs/plan/stages/INDEX.md](stages/INDEX.md)*
