# Stages — index

Dependency order, not importance. Open one stage file plus the decisions its `Consumes` row lists;
that is the whole reading list for executing it.

| Stage | What it builds | Status | Prerequisites |
|---|---|---|---|
| [Stage 0 — Baseline](stage-00-baseline.md) | Management account by hand, local tooling, the documentation set. | **DONE** | none |
| [Stage 1a — Landing zone, accounts and OUs](stage-01a-landing-zone.md) | Control Tower, the accounts and OUs, root secured, budget — the slow, hard-to-undo half. | **DONE** ([log](../../log/log-stage-01a-landing-zone.md)) — no vend is owed since 2026-09-05: `Staging` is made by renaming `Development` (6b) | none outstanding (D1 decided, every account e-mail registered) |
| [Stage 1b — Identity Center, permission sets, and the alarm above them](stage-01b-identity-and-controls.md) | Steps 1-6, 5.1 and 8: the alarm **first**, then delegation, users and groups, the administrator permission set, SSO profiles, retiring the direct assignments, AZ mapping, Access Analyzer. The other six permission sets are specified here and **written in code at Stage 2**. | **DONE** (2026-08-12, [log](../../log/log-stage-01b-identity-and-controls.md)) | Stage 1a complete, bar the deferred `Staging` vend |
| [Stage 1c — Preventive policies](stage-01c-preventive-policies.md) | Step 7 alone: SCPs, RCPs, tag and declarative policies, the Control Tower managed controls — **the only irreversible-from-inside stage of the three**. | **DONE** (2026-08-14) — ten documents, four policy types, battery 93/93 | **Stage 1b, complete.** What it actually consumes: the six SSO profiles and an administrator in the canary. Not its permission sets |
| [Stage 1d — Audit trail, Config scope, org-wide enablement](stage-01d-org-wide-enablement.md) | Steps 9, 10, 11 **and 12**: Object Lock in compliance mode, measuring and scoping AWS Config, org-wide RAM sharing + the Lake Formation cross-account version, and **the Region ceiling on `Security`**. **The four steps are independent of each other.** | **DONE 2026-08-15 — the landing zone closes here.** All four steps executed; every decision (3, 4, 8, 9, 10) taken. Object Lock at `COMPLIANCE`/90 days through `AWSControlTowerExecution`, which creates a **floor under the bucket's lifecycle** (INV-14, binding Stage 12 step 5); (iv)'s and (xiv)'s second halves stay provisional by construction | **Stage 1b.** *Not* Stage 1c for its steps — but 1c is done, and three of its findings changed this stage's roteiro |
| [Stage 2 — Terraform foundation](stage-02-terraform-foundation.md) | State buckets, **the six persona permission sets written from scratch**, the policy import, the hygiene checks, D11's `up`/`down`/`status`. **The module skeletons are no longer here** — step 7 moved to Stage 3 step 1.1a. | **DONE 2026-08-16** ([log](../../log/log-stage-02-terraform-foundation.md)) — one state bucket per Terraform-managed account, `identity/sso/` written and applied, `identity/org-policies/` **adopted** (ten documents + ten attachments, none created, second plan `No changes`), six offline checks, the lifecycle targets with their four refusals, and the Validation run against a real `[E]` slice. **All nine verifications answered**, (iii) last and from **Management**: the landing zone is `IN_SYNC` and the delegation intact — with two limits recorded rather than glossed, since its manifest has no concept of a resource policy and it has run exactly once (`INV-17`) | **Stages 1a, 1b, 1c and 1d.** `Staging` is unvended, so its bootstrap slice and its assignments were skipped and are picked up at the vend. One Management-account action was needed and no earlier stage performs it: the **Organizations policy delegation** to the Identity account (step 5.1, INT-20 — proved). |
| [Stage 3 — Networking](stage-03-networking.md) | One VPC per account that has one (Sandbox — one per business unit, D35 — Development, Staging, Production), split `foundation/` + `egress/`. **Step 0 came first and is done**: the Account Factory VPCs every vend left behind are gone (`docs/AWS_STATE.md` §C). | **DONE (2026-08-16) — applied, measured and torn down.** Step 0 first: the Account Factory VPCs gone by StackSet instance deletion on Management, **nothing survived** (verification (vi)). Then three passes: `foundation/` `[P]` in Sandbox/Development/Production (31/30/32), the four zone associations and the two peerings in **one ordered apply on the accepting side**, and `vpc-egress-v0.1.0` with the three `egress/` `[E]` slices (16/15/14). **`./aws/networking.py` and `./aws/egress.py`: 0 FAILED.** **The D11 cycle ran twice**: `foundation/` outputs byte-identical and re-plans `No changes` every time, **39/39 `[E]` ids new**, gateway endpoints surviving — the pair INT-05 rests on. **Every Deliverable answered** by three `[E]` probe slices carrying **no IAM principal**: verification (iii) as a 200/403 pair against two equally public buckets, peering measured from Sandbox **and** Development against one host with two interfaces, flow logs ACCEPT+REJECT, private DNS across the account boundary — and **INT-09 exercised for the first time**. **Torn down: 59 resources, USD 0.0000/h.** Left, and both belong elsewhere: the `Staging` NXDOMAIN clause (no host to refuse until the vend) and verification (ii), which is Stage 6's | Stage 2. **`Staging` is unvended** — quota-increase support ticket open — so its slices and its two proofs apply at vend; nothing else waits. |
| [Stage 4 — VPN access](stage-04-vpn.md) | WireGuard, the only entry point to the private network; the control-plane deny that made "only" reach the AWS APIs, retired by [D39](../decisions/D39-access-by-identity.md) and removed at [Stage 6g](stage-06g-access-by-identity.md). The tunnel reaches GitLab over the Stage 3 peering. *(GuardDuty left this stage for [Stage 15](stage-15-guardduty.md) on 2026-08-18.)* | **DONE 2026-08-18 — closed by the GuardDuty split.** Passes 1-3 executed and measured (host, anchors, peers, tunnel, the control-plane deny on the six persona sets, both validations, the behavioural deliverables, the server-side MTU); pass 4 left the stage whole for [Stage 15](stage-15-guardduty.md), prepared — its findings travelled with it. Verifications i-viii answered or moved; the close-out log entry is the user's. 1.3's rank and `dormant()` body merged (2026-08-16, PR #9). Pre-instrumented by `aws/vpn.py`, which carries the mechanical readings; the host key pair lives in the git-ignored `.tfvars` (decision 4) | Stage 3 — its `sandbox/foundation/`, the 9.3 allow-list (the WireGuard host is the first EC2 to exercise it) and the peering. D4 is decided: self-managed WireGuard. |
| [Stage 5a — Data foundation](stage-05a-data-foundation.md) | Lake buckets, Glue catalog, Iceberg, Lake Formation registrations and the two read shares (the Production share waits for Stage 9); Security Hub on, with the first governed data. | **DONE — every pass, 2026-08-18/20** ([log](../../log/log-stage-05a-data-foundation.md)) — the governed lake exists, its grants are made and it is shared: `58 to add` applied in two steps, then `9 added` (the governance manager's grants), then `4 added` (the two cross-account shares — four `LakeFormation-V4-*` RAM shares `ACTIVE` and held, **zero invitations, INT-11 closed**). **Three findings travel out of this stage** and are folded into the stages below: the `Create*DefaultPermissions` clearing is **not expressible in a plan** and acts at creation time, so every account that gains a `DataLakeSettings` applies it in two steps (Lesson 27, Recipe D); **every** cross-account grant carries the grant option, not just Production's; and a TBAC expression written on `classification` alone matches the **drop-box** (Lesson 29 — the `layer` gate is the control). 4d's behavioural proofs, 4e's SCP amendment and pass 6 (Security Hub CSPM org-wide, 2026-08-20) closed it. The data-lake-administrator prerequisite pass 3 discovered was delivered by 4a/4b — one `DataLakeSettings` per consumer account, applied under Recipe D. Pre-instrumented by `aws/datalake.py`, whose `DL-5` measures the INT-11 parameters it defends. The LF-Tag ontology carries the `zone` dimension, consumer grants are classification-scoped LF-TBAC expressions (`restricted` by explicit grant only — Stage 11 narrows within them), the raw share to Sandbox is kept deliberately (institutional-delta row), and the NFS requirement's withdrawal (D24 withdrawn) removed pass 5 and steps 10-12 | Stage 3 (the `[P]` gateway-endpoint IDs, INT-05). **Stage 4 for one named input**: the WireGuard EIP (bucket-policy branch). Stage 1d step 11. |
| [Stage 5b — Redshift Serverless](stage-05b-redshift-serverless.md) | The warehouse beside the lake: one namespace and workgroup in `Sandbox` at the documented floor of **4 base RPUs**, its cost ceiling and its audit trail applied in the same act that creates it, and the access model for **both** classes of database written before either has a member. `production/warehouse/` is specified here (§7) and applied at [Stage 9](stage-09-deployment-targets.md). | not started. **The requirement is in [`objectives.md`](../objectives.md) since 2026-09-20** (step 0.0, Claude drafting at the user's request): Redshift is a **second possible engine**, the Glue/Iceberg lake stays the warehouse of record, an engine is a choice per workload, it is **one environment to a data scientist with its databases' location an implementation matter** — which legalises D40's two-account split — and **the controls do not change**, so there is no new class of reader and the per database × project grant is the only new rule. Written 2026-09-19 from the vendor documentation. Its central choice is pre-settled by **[D40](../decisions/D40-redshift-warehouse.md)**: a warehouse built by hand, the `RedshiftServerless` blueprint still disabled, two classes on two accounts. **Nothing is measured** — the one priced reading is 0.36 USD/RPU-hour (offer file 2026-09-11), which makes 4 RPUs **1.44 USD per query-hour** and the warehouse the most expensive object per unit of time in the estate; a workgroup serving no query bills nothing. Two findings shape the whole stage: the **ratchet** (*"Amazon Redshift won't scale your data warehouse back down to 4 RPUs"*), and the fact that the **provider documentation demands three AZs while the service documents two** and the provider validates nothing — 0.1's apply is the only authority, and this estate has two | **Stage 5a** (the account data CMK and the lake), **Stage 3** (the private tier's two subnets in two AZs), **6c** (no default route, no NAT) |
| [Stage 6a — Unified Studio (executed)](stage-06a-unified-studio.md) | **A record, not a plan.** What ran of the old Stage 6: `production/registry/`, the two `sagemaker/` prerequisite slices, the step 3 deny fragment, 1.6's SCP amendment, the domain `awsds-studio`, both member associations, 11 blueprint configurations and 22 grants per member, both project profiles, the create path measured end to end, `default-v0.1.0` pushed, 2.6's derived-zone removal, and the dated readings of passes 4-5. | **CLOSED as a record 2026-09-05** | — |
| [Stage 6b — `Development` becomes `Staging`](stage-06b-development-becomes-staging.md) | The account changes role: the SMUS surface is unwound **inside `Interactive`** (the `Workloads` OU denies `datazone:*` on arrival), the lake share and the read-write persona are revoked, the account is renamed and moved, and the tree migrates from `development/` to `staging/` on a new state bucket (Recipe E). | **DONE 2026-09-06**, every pass in one day ([log](../../log/log-stage-06b-development-becomes-staging.md)). The corrections it carried: `CIDRS["staging"]` is re-pointed to **10.50.0.0/16 before** the token flip, or the folder rename proposes a **VPC replacement**; the assignment key is renamed behind `moved {}` blocks *after* the account is, not with the permission-set swap; `production/foundation/peers.tf`'s `awsds-<env>-vpc` lookup moves in the same commit as the token; `put-account-name`'s documented constraints replace the undocumented "four hours"; and Recipe E, Recipe F and `./aws/rename-check.py` are **used, not authored** | 6a. **Runs before 6c**, and the reason is in the file: 6c then writes the peering map once, against the final name |
| [Stage 6c — Networking: the single egress hub](stage-06c-networking-hub.md) | Three VPCs in Production (`Networking`, `SharedServices`, `Workloads`), five peerings, the `awsds.internal` zone family, two `[D]` hosts (WireGuard with the **transferred** Elastic IP, Squid with a new one), **zero NAT gateways** and no default route in any spoke — and the structural repair of the client-plane DNS shadowing. Writes **D38**. | **DONE 2026-09-08, pass 8 included** — the second client profile (the split-tunnel profile beside the monitored one: laptop-only, no host change, measured the same night — the reach difference is by identity, never by network, the host's `REJECT` counter flat). Passes 0-7: every pass executed and measured (2026-09-06/08); pass 6 closed with 6.5 (two doors by service family, the union trimmed, `sandbox/foundation` unfrozen, `sandbox/vpn/` retired) and 6.6 (INT-16 accepted, then superseded by D39); decision due 4 taken as (c), the access log's export waiting for Stage 11 step 5.1 with the organization trail as the tamper record meanwhile | Stages 3 and 4, 6a, **and 6b** (the account is already `staging`, and 10.40.0.0/16 is free) |
| [Stage 6d — Unified Studio: what 6a left owed](stage-06d-unified-studio-remainder.md) | The deny pair exercised, the house image made selectable and pulled across accounts, a working session measured **under the proxy**, the workflow surface measured (Stage 10's verification (i), pulled forward), the lifecycle proven, **the remote-IDE channel opened from a laptop for the first time**, and ten verification rows closed. | **in progress** — steps 8 and 9 closed 2026-09-08/09 (the Code Editor reaches the gallery through the proxy, the build plane is `open`, `vpc-egress-v0.11.1` generates the bypass list from the endpoints' own `dns_entry`); step 4 measured 2026-09-09/10 and closed as a decision — the portal's notebook operator dies on the D13 boundary and nothing the operator exposes repairs it. Step 2 closed 2026-09-10: the house image selectable and pulled across the account boundary (`default-v0.4.0` since 2026-09-12). The remote IDE opened from a laptop on 2026-09-11. Owed: 1.1's persona half and 1.2/1.3, 2.4's after half, 3.4's endpoint half, 5.2 and 5.4, 6.1 and 6.3, 7.6, 7.7's 12-hour half, step 10. Decision due 4 keeps the connection method and the file channel; D39 dissolved its VPN half | **6c pass 5** |
| [Stage 6e — Claude Code on Bedrock](stage-06e-claude-code-bedrock.md) | A coding assistant inside a Sandbox space, answered by **Amazon Bedrock** in this estate's own perimeter: the model-access form, the inference-profile pins, an interface endpoint that keeps the invocation off the proxy, the configuration delivered by the house image, and the data-protection question answered in writing. | **in progress since 2026-09-11** — a session answered from a space on 2026-09-12 (Haiku 4.5, the project role, through the `bedrock-runtime` endpoint). Owed: the SCP battery's rows for the new statements, 8.3's budget notification, 6.4's token volume, decisions 11 and 13. Its first decisions were taken before execution (2026-09-11, by the user): the assistant runs **in the space** and a laptop's VS Code over a remote session is the same answer; the form goes in **`Sandbox` alone**; `availableModels` **locks** the picker; the retention denies go in **`awsds-org-scp-baseline.json`**; invocation logging **stays off and the question moves to [Stage 11](stage-11-dlp.md) step 5.6**, written there rather than only here (Lesson 34). The scoped set was **Opus 5, Sonnet 5 and Haiku 4.5** until decision 14 moved it to the 4.5 generation (`EXC-08`), Haiku pinned for background work — which caught a defect in the first draft: a session that sets `ANTHROPIC_MODEL` runs session titles on the primary model, at the Opus rate. A runbook for the two audiences, `claude-code-sagemaker.md`, is step 9.6. Written from the vendor documentation and eight read-only readings taken the same day. What those readings changed: Opus 5 is **inference-profile only** and its `us.` profile routes to three US regions, so this is the estate's first resource that cannot honour D1; the modern Claude rates live in the **`AmazonBedrockFoundationModels`** offer file, not the one `docs/PRICING.md` §5 read; the model-access form is a **declaration, not an approval queue**; `InvokeModel*` is a CloudTrail **management event** carrying `modelId` and no prompt; and `bedrock-runtime` is already reachable from a space **by the wrong door** — allowed by the compute plane's `.amazonaws.com` entry, absent from the image's `NO_PROXY`, therefore public. The requirement it is measured against is that nothing transiting the model is persisted outside the private cloud: Bedrock's ZDR/ZOA default answers it, the exceptions are **per model**, and step 7.5 turns the default into an SCP rather than trusting it | **6d steps 2 and 7**, 6c pass 5 |
| [Stage 6f — Data governance: the SageMaker Catalog over the governed lake](stage-06f-data-governance.md) | The business catalog on the Glue and Lake Formation substrate: what a subscription and a direct Share write, whether the governed lake enters the catalog and from which account, who approves, what the catalog sends to generative AI, and how Production's outputs are published with no domain in Production. | not started as a plan; its first acts were taken by hand ([log](../../log/log-stage-06f-data-governance.md)): a database and a table in the portal, the table published and a data product published, measured to change no grant. Five decisions due; the first, how the lake enters the catalog, meets DataZone's documented lack of LF-TBAC support against this estate's TBAC shares | **None that block**: Stage 5a, 6a and the live project |
| [Stage 6g — Access to AWS by identity, and the VPN for the private network](stage-06g-access-by-identity.md) | [D39](../decisions/D39-access-by-identity.md) executed: `DenyControlPlaneOffVpn` deleted from the six persona permission sets, the lake's laptop branch turned into a principal branch on the drop-box, and every instrument, runbook and document that described the previous perimeter brought to the estate after the applies. | **in progress** — written and applied 2026-09-17: the deny deleted from the six persona sets and the lake's laptop branch turned into a principal branch, both re-planning `No changes` and read back (`INV-19`); `VP-7` inverted, `PX-5` retired. Decisions 1 and 2 taken and applied the same day: the proxy wears its instance's own address (`184.33.8.126` released) and `make up` refuses on the proxy alone. 1.4 and 2.4 measured from the laptop 2026-09-18 — `describe-log-groups` answers, `list-buckets` is now an implicit deny, the drop-box took the persona's `PutObject` by principal on either path. 1.4 is closed on both profiles; owed: 2.4's Athena read from a space | **None that block**; the hub up only for the monitored-profile halves of 1.4 and 2.4 |
| [Stage 6h — The Redshift connection in SageMaker Unified Studio](stage-06h-redshift-connection.md) | The first `sbx_*` database and the first connection: one SageMaker project given **write** on one sandbox database, and the three layers between the project and the table each read back on its own. | not started, written 2026-09-19. Its content is that **no single mechanism expresses "per database × project"**: a tag on the workgroup **and its namespace** admits the project to the compute, an IAM policy on the project role mints the session, and a Redshift `GRANT` decides the database — three systems, one symptom when any is missing. The credential is **IAM credentials**, not a password (decision 2), and `lineageSync` stays off because a scheduled query on a 1.44 USD/hour meter is a standing cost bought for nothing anybody asked for | **[5b](stage-05b-redshift-serverless.md) applied**, a live project from [6a](stage-06a-unified-studio.md)/[6d](stage-06d-unified-studio-remainder.md), and **[6c](stage-06c-networking-hub.md) pass 5** — the app ENIs and the workgroup in one VPC, which is what makes the JupyterLab path exist |
| [Stage 7 — GitLab, Runners and ECR](stage-07-gitlab-runners-ecr.md) | GitLab CE on EC2, runners, ECR + the pull-through cache, CodeArtifact, the internal names and TLS from the internal CA — all in `VPC-SharedServices`, all reaching the internet through the proxy. | not started. Its own "What the documentation changed" table carries the corrections: Let's Encrypt is **on by default** whenever `external_url` is HTTPS and retries every `reconfigure`; the CA root has a **fourth** surface (`/etc/gitlab/trusted-certs/`, GitLab itself); Omnibus takes the proxy **per component** and `no_proxy` must carry no port; the runner needs **three** separate proxy configurations (service, docker daemon, build container) and `NO_PROXY` accepts **suffixes only — no CIDR**; the pull-through cache's first pull **may need a route this estate does not have**, which makes it D38's first named NAT-contingency candidate; ECR **repository creation templates** exist; and a wildcard Pages domain needs **no secondary IP**. The buildbox is retired into the build runner; INT-09's clone moves to Sandbox; the D35 consumer map drops to **N Sandboxes**. **Pass 0 is applied** — step 5.a ran at 6a on 2026-08-21 | **[6c](stage-06c-networking-hub.md) passes 1-4** (the VPC under its new name, `awsds.internal`, the peering, the proxy) and **[6d](stage-06d-unified-studio-remainder.md)** for the INT-09 clone's project. `production/registry/` already exists; `production/pki/` does not. Decisions D8, D14, D15, D36, **D38** |
| [Stage 8 — CI/CD pipelines](stage-08-cicd-pipelines.md) | The three pipeline types and the promotion gate: the dev-env release chain (INT-17/INT-18), the `app-etl` build, the tag → Staging → Production promotion (INT-07/INT-08), the deploy credential layer, the gates, and this repository's own offline-gates pipeline. | not started. Nothing waits on a vend (6b makes Staging); the engineering apply moves to a new **`sandbox/app/app-etl/`** slice with a `layers.py` refusal that keeps CI out of Sandbox; `awsds-deploy-devenv-dev` is **not written**; `pip-audit` reaches OSV **through the proxy**, so a missing allow-list entry is a named 403; and the **D28 workflow lint is authored here** (step 5.6) with the four rules 6d supplies, beside a step recording that the SMUS CI/CD CLI is an **exporter, never a deployer** | Stage 7 (the build runner, the registries, the recorded edition answer), **6b** and **6c** |
| [Stage 9 — Deployment target platforms, producer path](stage-09-deployment-targets.md) | Staging and Production platforms, Model Registry, the producer path into the lake — the write share (INT-03), the drop-box pickup (INT-10), the debugging escape hatch. | not started. **3.5-3.7** move the off-VPC job deny from the six persona sets onto the job-execution **roles** as a permissions boundary (`awsds-job-exec-boundary`, read back with `get-role` because `list-roles` omits it, `DT-9`), and settle **serverless inference**, which takes no `VpcConfig` at all — recommended as an SCP deny rather than an exception. **4.3a** completes Staging's endpoint set with the subnets **pinned to the endpoints' AZ** (the `sagemaker.runtime` affinity D9 collides with). INT-04 is retired into INT-07 everywhere | Stage 5a, **Stage 8 pass 1**, and **6b/6c** — no vend anywhere |
| [Stage 10 — Workflow orchestration and promotion](stage-10-orchestration-promotion.md) | One orchestrator built (MWAA Serverless), end-to-end promotion, and the model chain through the registry. | not started. D7 amended to **MWAA Serverless only**, so design B is not built: **one design, two accounts** (Staging gets its own slice — a serverless workflow bills nothing at rest), applied **Staging first**; step **1.2** spells out AWS's documented **private-routing** shape and its constraints, including the two-AZ requirement as a **priced D9 exception** and the fact that the service takes **no proxy setting**, making the workers **D38's first named exception — one with no internet path at all**; step **4** is the fallback ladder, with provisioned MWAA replaced by a `DenyProvisionedMwaa` SCP and `OR-6` flipped to an **absence** check; authoring moves to **Sandbox** | Stages 8 and 9, **6d step 4** (the portability reading, pulled forward) |
| [Stage 11 — Data protection and DLP](stage-11-dlp.md) | Macie, CloudTrail data events, LF column/row/cell filters, GuardDuty's paid add-ons — data-specific detection on top of the perimeter, and the threat model with its accepted-risk ledger. | not started. The egress-control leg is **monitoring, not building** (6c builds the proxy; open question 23 is closed), and what arrives here is the **Squid access log** exported to Log Archive. Two Interactive accounts become one throughout — Macie's member set, the data-event trails and the exfiltration alarm's writer list. The proxy sees **destination and volume, never content**, so domain fronting is an accepted residual; and the DNS firewall's job changed from filtering the internet to closing the recursive resolver, which is a detection feed rather than a control | Stages 5a, 6, 9; **Stage 15 plus a month of billing** for its step 4 |
| [Stage 12 — Observability, governance and FinOps](stage-12-observability-finops.md) | Dashboards, alarms, log retention, cost attribution against the real bill, permission-set tightening, **backup and recoverability**, quota alarms. | not started. Scoped by D38: the NAT panels lose their resource, and three lines arrive — the **proxy's 403 rate** as a design signal, the **DNS Firewall's per-query charge** (whose retirement is a legitimate outcome of 4.4's reading), and **peering bytes**. Step 2.1 is where **D12's budget-notifies-nobody deferral is closed**, with a real spend curve | any stage that created resources — and **one real invoice** |
| [Stage 13 — Public-facing web tier (experiment)](stage-13-public-web-tier.md) | The public-facing experiment: web tier in front of a private backend — **and the only stage with public DNS** (D15 phase 2). | not started. The ALB lands in **`VPC-Networking`'s public tier** and becomes the **second enumerated listener** in `AWS_STATE.md`, which is what makes 6c step 1.5's gate testable for the first time; the backend sits in `VPC-Workloads` reached as **IP targets over the peering** (an address, never a path — Lesson 44); a teardown contract covering **public DNS** and a dangling-record check are added | **6c**, Stage 9, **and the domain name from the user** — the only blocking input left in the plan |
| [Stage 14 — Per-business-unit Sandbox vending](stage-14-sandbox-vending.md) | A business unit's `Sandbox` account from one name in a merge request (D35). | not started, **blocked on the account quota**. Its central question — where the VPN terminates — was **closed without N** by [D38](../decisions/D38-single-egress-hub.md): a designated hub, one of the three shapes this stage listed, taken early because the client-plane DNS break forced the topology first. The two shapes not taken keep their triggers (Transit Gateway is where the hub goes when a slot frees; per-unit endpoints are ruled out by the same argument as per-account NAT). What each vend still owes the topology: a CIDR from `10.16.0.0/13`, **two** peering pairs, one child zone with its reversed authorization, one proxy source block — all four generated from 6c's map | Stages 2, 3, 4, 5, 6. **The promotion chain is untouched**: only `Sandbox` multiplies |
| [Stage 15 — GuardDuty org-wide](stage-15-guardduty.md) | Threat detection over the whole organization: delegation to Audit, auto-enable `ALL`, **every optional protection plan switched off** (they arrive ON — its step 0), findings routed to a human for the first time. | not started. The `DenyGuardDutyTampering` collision on Audit's own detector is its decision 1, due before the first console act. Pre-instrumented by `./aws/guardduty.py` | **None that block** — the deferral past Stage 4's exposed host is a decision, not a dependency (the institutional-delta row argues the trade). **It gates rather than being gated**: Stage 11 step 4 needs this stage plus a month of billing; Stage 5a step 13.2's ingestion is empty until it runs. |
| [Stage 16 — The sandbox lake](stage-16-sandbox-lake.md) | A fourth Sandbox bucket, `awsds-sandbox-lake`: **permanent** artifacts, one prefix per SSO group, mounted into SMUS projects through the portal's **S3 connection** and vended to laptops through `s3-read-write` — a compensated **shadow lake**, accepted with its eyes open (its institutional-delta row argues the trade). | **DONE 2026-08-26 — created, applied, exercised, closed and logged in one day** ([log](../../log/log-stage-16-sandbox-lake.md)); the stage file's §"What ran on 2026-08-26" is the one record. The 26th slice applied (`12 added`, re-plan `No changes`); the account data CMK carries **no delegate-to-IAM statement**, so 2.2's key-policy statement was the only admission path. **Vended-only measured from every side**: 2.3's three independently-worded refusal layers, pass 5's unchanged `s3-read-write` (the KMS half closed by `head-object`), pass 4's both-doors CloudTrail reading — and the finding that **the SMUS JupyterLab image auto-vends "direct" S3 calls** (`aws_s3_access_grants_boto3_plugin`), so the in-image direct-refusal test is unrunnable and the laptop is that control's only home. **Pass 6 measured revocation on a sacrificial grant**: the vend door closes between +1 s and +19 s of the delete (a fresh bearer minted *inside* the window), issued credentials survive to their own expiry; `SL-4` was hardened by its first live anomaly (the old classifier took any reserved role for a tenant). **The Lake Formation admin-seat finding settled as `consumer-data-v0.5.0`** (one declared admin, `ignore_changes`, `DL-13` the defence; OQ 24 keeps the governance half). Left deliberately: §R's trust half (waits a real project death), verification (iv)'s object-level half (Stage 11's data events), two tenants with no laptop path until `identity/sso/` extends the vending reference | **None that block** — Stage 5a's consumer side (the account data CMK), Stage 6's measured create path (a real project to wire), `s3-read-write` (2026-08-24) and the SMUS-born Access Grants instance are all in place. One input is the user's: step 0.1 writes the requirement into `objectives.md`. **Sandbox only** — Development has no Access Grants instance until its first project. |

---

*Plan core: [GENERAL_PLAN.md](../../GENERAL_PLAN.md) · What was actually done by hand, per stage:
[docs/log/INDEX.md](../../log/INDEX.md) — same file slugs as here, prefixed `log-`*
