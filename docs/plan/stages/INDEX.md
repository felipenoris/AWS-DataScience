# Stages — index

Dependency order, not importance. Open one stage file plus the decisions its `Consumes` row lists;
that is the whole reading list for executing it.

| Stage | What it builds | Status | Prerequisites |
|---|---|---|---|
| [Stage 0 — Baseline](stage-00-baseline.md) | Management account by hand, local tooling, the documentation set. | **DONE** | none |
| [Stage 1a — Landing zone, accounts and OUs](stage-01a-landing-zone.md) | Control Tower, the accounts and OUs, root secured, budget — the slow, hard-to-undo half. | **DONE — no vend is owed since 2026-09-05: `Staging` is made by renaming `Development` (6b)**. *(Earlier: done except the `Staging` vend** ([log](../../log/log-stage-01a-landing-zone.md)) | none outstanding (D1 decided, every account e-mail registered) |
| [Stage 1b — Identity Center, permission sets, and the alarm above them](stage-01b-identity-and-controls.md) | Steps 1-6, 5.1 and 8: the alarm **first**, then delegation, users and groups, the administrator permission set, SSO profiles, retiring the direct assignments, AZ mapping, Access Analyzer. The other six permission sets are specified here and **written in code at Stage 2**. | **DONE** (2026-08-12, [log](../../log/log-stage-01b-identity-and-controls.md)) | Stage 1a complete, bar the deferred `Staging` vend |
| [Stage 1c — Preventive policies](stage-01c-preventive-policies.md) | Step 7 alone: SCPs, RCPs, tag and declarative policies, the Control Tower managed controls — **the only irreversible-from-inside stage of the three**. | **DONE** (2026-08-14) — ten documents, four policy types, battery 93/93 | **Stage 1b, complete.** What it actually consumes: the six SSO profiles and an administrator in the canary. Not its permission sets |
| [Stage 1d — Audit trail, Config scope, org-wide enablement](stage-01d-org-wide-enablement.md) | Steps 9, 10, 11 **and 12**: Object Lock in compliance mode, measuring and scoping AWS Config, org-wide RAM sharing + the Lake Formation cross-account version, and **the Region ceiling on `Security`**. **The four steps are independent of each other.** | **DONE 2026-08-15 — the landing zone closes here.** All four steps executed; every decision (3, 4, 8, 9, 10) taken. Object Lock at `COMPLIANCE`/90 days through `AWSControlTowerExecution`, which creates a **floor under the bucket's lifecycle** (INV-14, binding Stage 12 step 5); (iv)'s and (xiv)'s second halves stay provisional by construction | **Stage 1b.** *Not* Stage 1c for its steps — but 1c is done, and three of its findings changed this stage's roteiro |
| [Stage 2 — Terraform foundation](stage-02-terraform-foundation.md) | State buckets, **the six persona permission sets written from scratch**, the policy import, the hygiene checks, D11's `up`/`down`/`status`. **The module skeletons are no longer here** — step 7 moved to Stage 3 step 1.1a on 2026-08-16. | **DONE 2026-08-16** ([log](../../log/log-stage-02-terraform-foundation.md)) — one state bucket per Terraform-managed account, `identity/sso/` written and applied, `identity/org-policies/` **adopted** (ten documents + ten attachments, none created, second plan `No changes`), six offline checks, the lifecycle targets with their four refusals, and the Validation run against a real `[E]` slice. **All nine verifications answered**, (iii) last and from **Management**: the landing zone is `IN_SYNC` and the delegation intact — with two limits recorded rather than glossed, since its manifest has no concept of a resource policy and it has run exactly once (`INV-17`). Roteiro revised 2026-08-15 against the closed landing zone — the delegation moved to the **front** (six of ten documents sit on the *root*, so 5.0 could have deleted most of the slice; it did not) and the per-OU attachments are **authored, not discovered**, since a `for_each` would have reversed D37 on `Sandboxes` | **Stages 1a, 1b, 1c and 1d.** `Staging` is unvended, so its bootstrap slice and its assignments were skipped and are picked up at the vend. One Management-account action was needed and no earlier stage performs it: the **Organizations policy delegation** to the Identity account (step 5.1, INT-20 — proved). |
| [Stage 3 — Networking](stage-03-networking.md) | One VPC per account that has one (Sandbox — one per business unit, D35 — Development, Staging, Production), split `foundation/` + `egress/`. **Step 0 came first and is done**: the Account Factory VPCs every vend left behind are gone (`docs/AWS_STATE.md` §C). | **DONE (2026-08-16) — applied, measured and torn down.** Step 0 first: the Account Factory VPCs gone by StackSet instance deletion on Management, **nothing survived** (verification (vi)). Then three passes: `foundation/` `[P]` in Sandbox/Development/Production (31/30/32), the four zone associations and the two peerings in **one ordered apply on the accepting side**, and `vpc-egress-v0.1.0` with the three `egress/` `[E]` slices (16/15/14). **`./aws/networking.py` and `./aws/egress.py`: 0 FAILED.** **The D11 cycle ran twice**: `foundation/` outputs byte-identical and re-plans `No changes` every time, **39/39 `[E]` ids new**, gateway endpoints surviving — the pair INT-05 rests on. **Every Deliverable answered** by three `[E]` probe slices carrying **no IAM principal**: verification (iii) as a 200/403 pair against two equally public buckets, peering measured from Sandbox **and** Development against one host with two interfaces, flow logs ACCEPT+REJECT, private DNS across the account boundary — and **INT-09 exercised for the first time**. **Torn down: 59 resources, USD 0.0000/h.** Left, and both belong elsewhere: the `Staging` NXDOMAIN clause (no host to refuse until the vend) and verification (ii), which is Stage 6's | Stage 2. **`Staging` is unvended** — quota-increase support ticket open — so its slices and its two proofs apply at vend; nothing else waits. |
| [Stage 4 — VPN access](stage-04-vpn.md) | WireGuard, the only entry point; the control-plane deny that makes "only" reach the AWS APIs. The tunnel reaches GitLab over the Stage 3 peering. *(GuardDuty left this stage for [Stage 15](stage-15-guardduty.md) on 2026-08-18.)* | **DONE 2026-08-18 — closed by the GuardDuty split.** Passes 1-3 executed and measured (host, anchors, peers, tunnel, the control-plane deny on the six persona sets, both validations, the behavioural deliverables, the server-side MTU); pass 4 left the stage whole for [Stage 15](stage-15-guardduty.md), prepared — its findings travelled with it. Verifications i-viii answered or moved; the close-out log entry is the user's. 1.3's rank and `dormant()` body merged (2026-08-16, PR #9). **Revised and pre-instrumented 2026-08-16**: `aws/vpn.py` carries the mechanical readings; step 8 is written against the `identity/sso/` slice Stage 2 actually built. **Revised again the same day into the action-checklist format** (executor markers, action-first steps), with corrections its Status row lists: the `vpn/` rank sits between `foundation` and `egress` (and `slices.py`'s `dormant()` stub owes a body), the handshake log is generated on the host, verification (ii) is answered by the EC2 documentation, GuardDuty auto-enable `ALL` covers existing accounts, the full tunnel routes `::/0` too, and the AL2023 packages were measured present in the `us-west-2` repo. **Reviewed once more after Stage 3 closed** (post-teardown): the host key pair moves to the git-ignored `.tfvars` (decision 4 — an SSM-resolved AMI replaces a `[D]` instance too), the module gains `zone_index`, Validation 2 copies its "before" aside, the tunnel-pair deliverable reuses `production/probes/` via `make up` | Stage 3 — its `sandbox/foundation/`, the 9.3 allow-list (the WireGuard host is the first EC2 to exercise it) and the peering. D4 is decided: self-managed WireGuard. |
| [Stage 5 — Data foundation](stage-05-data-foundation.md) | Lake buckets, Glue catalog, Iceberg, Lake Formation registrations and the two read shares (the Production share waits for Stage 9); Security Hub on, with the first governed data. | **PASSES 0-4 DONE except pass 4's behavioural half (2026-08-18/19)** ([log](../../log/log-stage-05-data-foundation.md)) — the governed lake exists, its grants are made and it is shared: `58 to add` applied in two steps, then `9 added` (the governance manager's grants), then `4 added` (the two cross-account shares — four `LakeFormation-V4-*` RAM shares `ACTIVE` and held, **zero invitations, INT-11 closed**). **Three findings travel out of this stage** and are folded into the stages below: the `Create*DefaultPermissions` clearing is **not expressible in a plan** and acts at creation time, so every account that gains a `DataLakeSettings` applies it in two steps (Lesson 27, Recipe D); **every** cross-account grant carries the grant option, not just Production's; and a TBAC expression written on `classification` alone matches the **drop-box** (Lesson 29 — the `layer` gate is the control). **What remains is 4d** (every behavioural proof, all needing the tunnel), **4e** (4.3's SCP amendment, last) **and pass 6** (Security Hub). The data-lake-administrator prerequisite pass 3 discovered was delivered by 4a/4b — one `DataLakeSettings` per consumer account, applied under Recipe D. **Revised and pre-instrumented 2026-08-16**: `aws/datalake.py` carries the mechanical readings, `DL-5` already measuring the INT-11 parameters it defends. **Revised again 2026-08-17 after the data-governance review**: the LF-Tag ontology gains the `zone` dimension, consumer grants become classification-scoped LF-TBAC expressions (`restricted` by explicit grant only — Stage 11 narrows within them), the results-zone ceiling joins decision 6, the raw share to Sandbox is kept deliberately (institutional-delta row), and the missing quality gate is declared (3.8). **And once more later the same day: the NFS requirement was withdrawn (D24 withdrawn) — pass 5, steps 10-12, removed** | Stage 3 (the `[P]` gateway-endpoint IDs, INT-05). **Stage 4 for one named input**: the WireGuard EIP (bucket-policy branch). Stage 1d step 11. |
| [Stage 6a — Unified Studio (executed)](stage-06a-unified-studio.md) | **A record, not a plan.** What ran of the old Stage 6: `production/registry/`, the two `sagemaker/` prerequisite slices, the step 3 deny fragment, 1.6's SCP amendment, the domain `awsds-studio`, both member associations, 11 blueprint configurations and 22 grants per member, both project profiles, the create path measured end to end, `default-v0.1.0` pushed, 2.6's derived-zone removal, and the dated readings of passes 4-5. | **CLOSED as a record 2026-09-05** | — |
| [Stage 6b — `Development` becomes `Staging`](stage-06b-development-becomes-staging.md) | The account changes role: the SMUS surface is unwound **inside `Interactive`** (the `Workloads` OU denies `datazone:*` on arrival), the lake share and the read-write persona are revoked, the account is renamed and moved, and the tree migrates from `development/` to `staging/` on a new state bucket (Recipe E). | not started — **created 2026-09-05** | 6a. **Not** 6c |
| [Stage 6c — Networking: the single egress hub](stage-06c-networking-hub.md) | Three VPCs in Production (`Networking`, `SharedServices`, `Workloads`), five peerings, the `awsds.internal` zone family, two `[D]` hosts (WireGuard with the **transferred** Elastic IP, Squid with a new one), **zero NAT gateways** and no default route in any spoke — and the structural repair of the client-plane DNS shadowing. Writes **D38**. | not started — **created 2026-09-05** | Stages 3 and 4, 6a |
| [Stage 6d — Unified Studio: what 6a left owed](stage-06d-unified-studio-remainder.md) | The deny pair exercised, the house image made selectable and pulled across accounts, a working session measured **under the proxy**, the workflow surface measured (Stage 10's verification (i), pulled forward), the lifecycle proven, and ten verification rows closed. | not started — **created 2026-09-05** | **6c pass 5** |
| [Stage 7 — GitLab, Runners and ECR](stage-07-gitlab-runners-ecr.md) | GitLab CE on EC2, runners, ECR, CodeArtifact, internal names and TLS from the internal CA (D15 as revised — no public domain here). **Revised 2026-08-16** into the pass/verification format against the official GitLab and AWS documentation; pre-instrumented by `./aws/supplychain.py`. Notable corrections: the Proves row loses INT-08 (Stage 8's), ECR tag immutability and the `base` repository are written down, BuildKit rootless replaces the archived Kaniko, runner registration uses authentication tokens, and enhanced scanning is deferred to Stage 11 (basic carries the Stage 8 gate). | not started | Stages 3 (the Production VPC, the zones, the peerings) and 4 (the tunnel; the SG rule shape). **Pass 0 shrank to `production/registry/`'s 5.a half on 2026-08-21 (D36 §3 amended) and is APPLIED — inside Stage 6's pass 0**; `production/pki/` is back in THIS stage's pass 1, and step 2.6 rebuilds the `dev-env` image with the CA root. One deliverable (the INT-09 clone) needs Stage 6's `engineering` project. Decisions D8, D14, D15. |
| [Stage 8 — CI/CD pipelines](stage-08-cicd-pipelines.md) | The three pipeline types and the promotion gate: the dev-env release chain (INT-17/INT-18), the `app-etl` build, the Development → Staging → Production promotion (INT-07/INT-08), the deploy credential layer, the gates, and this repository's own offline-gates pipeline. | not started — **revised 2026-08-16 into the pass/verification format against the official GitLab and AWS documentation**; pre-instrumented by `./aws/cicd.py`. Notable corrections: the dev-env chain pushes **before** it scans (scan-on-push; one scan per image per 24 h), GitLab Dependency Scanning is **Ultimate** so the dependency gate is `pip-audit` (Lesson 12), a CE manual job blocks only under `rules:`, the deploy-role scope gains Staging's `egress/`, the registration fan-out is **N + 1 from the D35 map**, and the infrastructure pipeline runs offline gates with **no AWS credentials** | Stage 7 (runner, registries, the 3.3 edition answer, the 7.1 mirror answer) and Stage 6 step 5.1's recorded INT-17 mechanism. **The `Staging` vend gates pass 4 alone** — passes 0-3 and 5 run without it. |
| [Stage 9 — Deployment target platforms, producer path](stage-09-deployment-targets.md) | Staging and Production platforms, Model Registry, the producer path into the lake — the write share (INT-03), the drop-box pickup (INT-10), the debugging escape hatch. | not started — **revised 2026-08-16 into the pass/verification format against the official AWS documentation**; pre-instrumented by `./aws/deploytargets.py`. Notable corrections: the old step 7 dissolved into a reading (the deploy roles and INT-07's image half are Stage 8's; **ECR pulls need no KMS grant** — the service decrypts through its own grants), the LF grant became the documented two steps (account-level grant *with grant option*, local regrant to the job role), the Athena "per-principal result prefix" became one enforced results zone with the within-persona visibility stated as a limit, `DataScientistStagingAccess` was read as already built (no Athena, `DenyEveryWrite`), and the job-role name became a contract with Stage 5's drop-box statements (`awsds-prod-job-exec`). **Revised twice more 2026-08-19, against what Stage 5's passes measured**: the `DataLakeSettings` apply is Recipe D's two steps in `production/data/` *and* `staging/data/`, the `Parameters` hazard is symmetric (read each account's own map before authoring), the re-grant is a DESCRIBE-plus-target pair, and `production/data/`'s LF half became a `consumer-data` call (v0.2.0 — derived zone, enforced workgroup, account data CMK) with the outputs bucket written beside it | Stage 3 (`production/foundation/`), Stage 5 (the lake, the drop-box statements, `DL-5`'s discipline), **Stage 8 pass 1** (the principals step 3's resource policies name). **The `Staging` vend gates passes 4-5 alone** — passes 0-3 run without it. |
| [Stage 10 — Workflow orchestration and promotion](stage-10-orchestration-promotion.md) | Both orchestrators (D7) built and compared, end-to-end promotion — and the model chain through the registry. **Revised 2026-08-16 into the pass/verification format, against the official AWS documentation and the local service model**; pre-instrumented by `./aws/orchestration.py`. Notable corrections: the Studio's Workflows tool and alternative A are **one product with two-way sync**, so the comparison's A-side artifact is authored in the Studio; "logs only" was an overstatement — A has run/task APIs, a console page and (2026-06) **EventBridge events**, so both designs alarm through EventBridge rules; A runs Amazon-provider operators only, its **schedule lives in the YAML**, and its hard caps are written down (60-min task, 50 KB YAML, 50 versions); the IAM prefix is `airflow-serverless` and the first `CreateWorkflow` creates a **service-linked role** (Lesson 17); an omitted `NetworkConfiguration` silently runs tasks **outside the VPC** — the slice always sets it; B is Step Functions **Standard** by documented elimination, on the `/aws/vendedlogs/states` prefix; the model half gained the documented cross-account KMS requirement and a **batch-transform** serving recommendation; registration stays the pipeline's act, consuming Stage 9 3.2 unchanged | Stages 8 (the deploy layer — the slice is applied by the pipeline, which **is** INT-14's proof; the shared gates the D28 lint joins) and 9 (the job role, the registry, the definitions home). Stage 6's `engineering` project — its decision 5 deferred the Workflows surface to this stage. **The `Staging` vend gates pass 5's Staging leg and pass 6's full promotion alone.** The provisioned-fallback questions (step 4) apply **only if** INT-14's chain falls to its last rung. |
| [Stage 11 — Data protection and DLP](stage-11-dlp.md) | Macie, CloudTrail data events, LF column/row/cell filters, GuardDuty's paid add-ons — data-specific detection on top of the perimeter, and the threat model with its accepted-risk ledger. | not started — **revised 2026-08-17 into the action-checklist format against the official documentation and the Price List API**; pre-instrumented by `./aws/dlp.py`. Notable corrections: internal-access analysis measured at **USD 9.00/resource-month** (the analyzer became a create-read-delete instrument, and its KMS claim was narrowed — the derived CMK is a key-policy reading), **Macie's auto-enable covers new accounts only** (existing ones are added one by one — the inverse of GuardDuty's `ALL`), GuardDuty's two features named by API name with both prices measured and the `POLICIES.md` collision proceduralized, the trails became **data-event-only** with alarms on EventBridge `MatchedEvents` (no CloudWatch Logs ingestion), the first member-account trail fires `POLICIES.md`'s CloudTrail revision trigger (the tampering `Sid` is decided here), the Athena-inversion alarm became conditional on Stage 5 decision 4, and Stage 7 decision 2's ECR enhanced-scanning deferral got its owning step (4.6) | Stages 5, 6, 9; decision D6. **Stage 15's log is a named input** (the exercised decision-1 path is the step 4 unblock; its step 5 settled that no carve-out role exists), and step 4 needs about a month of GuardDuty billing behind that stage. |
| [Stage 12 — Observability, governance and FinOps](stage-12-observability-finops.md) | Dashboards, alarms, cost attribution against what the bill actually shows. | not started | any stage that created resources. |
| [Stage 13 — Public-facing web tier (experiment)](stage-13-public-web-tier.md) | The public-facing experiment: web tier in front of a private backend. **Also the only stage with public DNS** — domain registration, public hosted zone and public ACM live here and nowhere earlier (D15 phase 2). | not started | Stages 3, 9, **and the domain name from the user**. |
| [Stage 14 — Per-business-unit Sandbox vending](stage-14-sandbox-vending.md) | A business unit's `Sandbox` account from one name in a merge request (D35). | not started | Stages 2, 3, 4, 5, 6 — Stage 5 for the consumer side the `sandbox-unit` module composes. The first stage about **scale** rather than capability: it parameterises slices that already exist, and it is where the VPN topology question (one hub, a Transit Gateway, or per-unit endpoints) is finally answered — with N in hand. **The promotion chain is untouched**: only `Sandbox` multiplies. |
| [Stage 15 — GuardDuty org-wide](stage-15-guardduty.md) | Threat detection over the whole organization: delegation to Audit, auto-enable `ALL`, **every optional protection plan switched off** (they arrive ON — its step 0), findings routed to a human for the first time. | not started — created 2026-08-18 by splitting Stage 4's pass 4 out whole, already prepared: the `DenyGuardDutyTampering` collision on Audit's own detector is its decision 1, due before the first console act. Pre-instrumented by `./aws/guardduty.py` | **None that block** — the deferral past Stage 4's exposed host is a decision, not a dependency (the institutional-delta row argues the trade). **It gates rather than being gated**: Stage 11 step 4 needs this stage plus a month of billing; Stage 5 step 13.2's ingestion is empty until it runs. |
| [Stage 16 — The sandbox lake](stage-16-sandbox-lake.md) | A fourth Sandbox bucket, `awsds-sandbox-lake`: **permanent** artifacts, one prefix per SSO group, mounted into SMUS projects through the portal's **S3 connection** and vended to laptops through `s3-read-write` — a compensated **shadow lake**, accepted with its eyes open (its institutional-delta row argues the trade). | **DONE 2026-08-26 — created, applied, exercised, closed and logged in ONE day** ([log](../../log/log-stage-16-sandbox-lake.md)); the stage file's §"What ran on 2026-08-26" is the one record. The 26th slice applied (`12 added`, re-plan `No changes`); the account data CMK carries **no delegate-to-IAM statement**, so 2.2's key-policy statement was the only admission path. **Vended-only measured from every side**: 2.3's three independently-worded refusal layers, pass 5's unchanged `s3-read-write` (the KMS half closed by `head-object`), pass 4's both-doors CloudTrail reading — and the finding that **the SMUS JupyterLab image auto-vends "direct" S3 calls** (`aws_s3_access_grants_boto3_plugin`), so the in-image direct-refusal test is unrunnable and the laptop is that control's only home. **Pass 6 measured revocation on a sacrificial grant**: the vend door closes between +1 s and +19 s of the delete (a fresh bearer minted *inside* the window), issued credentials survive to their own expiry; `SL-4` was hardened by its first live anomaly (the old classifier took any reserved role for a tenant). **The Lake Formation admin-seat finding settled as `consumer-data-v0.5.0`** (one declared admin, `ignore_changes`, `DL-13` the defence; OQ 24 keeps the governance half). Left deliberately: §R's trust half (waits a real project death), verification (iv)'s object-level half (Stage 11's data events), two tenants with no laptop path until `identity/sso/` extends the vending reference | **None that block** — Stage 5's consumer side (the account data CMK), Stage 6's measured create path (a real project to wire), `s3-read-write` (2026-08-24) and the SMUS-born Access Grants instance are all in place. One input is the user's: step 0.1 writes the requirement into `objectives.md`. **Sandbox only** — Development has no Access Grants instance until its first project. |

---

*Plan core: [GENERAL_PLAN.md](../../GENERAL_PLAN.md) · What was actually done by hand, per stage:
[docs/log/INDEX.md](../../log/INDEX.md) — same file slugs as here, prefixed `log-`*
