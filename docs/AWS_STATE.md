# AWS_STATE.md — what to expect from this environment

**Read this next to a snapshot, never instead of one.** A snapshot says what AWS reports; this file says
what a snapshot is *expected* to report, and which differences are already accounted for. Without it, a
reading of `aws/output/` produces false alarms — and, worse, makes a real one indistinguishable from the
six that were already known.

Four files answer four different questions. Keeping them apart is what keeps any of them true:

| Question | Where |
|---|---|
| What should exist, and why | [`docs/GENERAL_PLAN.md`](GENERAL_PLAN.md), `docs/plan/`, [`docs/ORGANIZATION.md`](ORGANIZATION.md) |
| What was typed by hand, and when | [`docs/log/`](log/INDEX.md) |
| What AWS reports right now | `aws/output/` — regenerate it, see [`aws/INDEX.md`](../aws/INDEX.md) |
| **Whether the difference between those is expected** | **this file** |

Two rules keep this file from becoming a stale copy of the three above:

- **No identifiers.** Names only. Account ids, OU ids, ARNs, instance ids and email addresses live in the
  snapshot, which is regenerated on demand. A file that carries no identifier cannot be wrong about one.
- **No reasoning.** *Why* something is so belongs in [`docs/plan/decisions/`](plan/decisions/INDEX.md); *what was
  done* belongs in `docs/log/`. Here: only what is expected, and what a deviation from it means.

Everything below was measured from a script in [`aws/`](../aws/INDEX.md). **A bare section number refers to
`aws/output/list-identities.txt`** (first run 2026-08-11); a reference prefixed with a file name, such as
`AZs.txt` 3, refers to that snapshot instead (`aws/AZs.py`, 2026-08-12).

## A. Invariants — what a snapshot must show

| # | Expected | Section | A deviation means |
|---|---|---|---|
| **INV-01** | Exactly **one** Identity Store instance | 3.2 | The single-directory assumption the whole identity plane rests on is broken. The script stops here on purpose — reconcile before doing any identity work |
| **INV-02** | Every account is `ACTIVE`, and every account **but two** sits inside an OU. The two attached directly to the root are the **management account** — Control Tower leaves it there — and EXC-01 | 2.3, 2.4 | A *third* account at the root inherits no policy set at all. That is a hole in the ceiling, not a detail |
| **INV-03** | The OU tree is **two levels deep** — `Sandboxes` nested under `Interactive` | 2.3 | A one-level `for_each` over OUs sees nothing below depth 1, so every Sandbox account is invisible to it |
| **INV-04** | **Five** `sso-group-*` groups, each with **exactly one** member | 3.3, 3.5 | Memberships are directory state, changed by hand and by nobody else. Stage 1b step 8.3's alarm should have fired — if it did not, the alarm is the finding |
| **INV-05** | Six Control Tower groups are **empty**: `AWSAuditAccountAdmins`, `AWSLogArchiveAdmins`, `AWSLogArchiveViewers`, `AWSSecurityAuditors`, `AWSSecurityAuditPowerUsers`, `AWSServiceCatalogAdmins`. `AWSControlTowerAdmins` and `AWSAccountFactory` hold the Control Tower admin user, and nothing else does | 3.5 | Each of those six already **holds live assignments** — `AWSAuditAccountAdmins` is administrator on the Audit account. They grant nothing only because they are empty, so one added member is one granted access |
| **INV-06** | `InfrastructureAccess` carries the **five** project tags and a `PT4H` session | 4.2, 4.3 | A tag missing here is the tag convention failing at its first instance, which is where a tag policy would later fail silently |
| **INV-07** | Each account shows the assignment rows in **A.1** below, and no others | 5.1, 5.2 | A path exists that nobody wrote. Check A.1 first — most of those rows are Control Tower's, not this project's |
| **INV-08** | Every account returns the **same** AZ name→ID mapping, four zones, all `available` and `opt-in-not-required` | `AZs.txt` 3, 4 | **Not a finding, and not drift** — a vended account is assigned its own mapping and may legitimately differ. It is recorded because the plan anchors subnets on the **ID** precisely so a divergence costs nothing; if one appears, confirm no slice indexes AZs by list position, and leave it. The reverse — accounts agreeing while a slice indexes by position — is the state that looks fine and bills |
| **INV-10** | In **Audit**, `us-west-2`: exactly **one** IAM Access Analyzer, named `awsds-org-external-access`, type **`ORGANIZATION`**, `ACTIVE`, carrying the five project tags and **no archive rule** — and every finding of one shape: `ExternalAccess` on `AWSReservedSSO_*` roles, Identity Center's SAML trust reading as external to the analyzer by design (first authoritative reading 2026-08-15). A finding naming anything else is real (INV-16) | `audit-iam-analyser.txt` 2, 3, 5 — the one snapshot here that can only be produced from CloudShell inside Audit | Type `ACCOUNT` is the wrong zone of trust: it stays `ACTIVE`, reports on one near-empty account and raises nothing. A **second** analyzer of an unused-access type is the paid half arriving early (Stage 12) and bills per resource per month. An **archive rule** suppresses matching findings silently — the plan creates none |
| **INV-11** | **Revised 2026-08-13, twice, by step 7.7, and again 2026-08-14 by Stage 1d step 12 — which is what finally made the first sentence true of `Security` as well.** Every governed OU shows `FullAWSAccess` **plus two `aws-guardrails-*` SCPs** — its original Control Tower guardrail and one holding `CT.MULTISERVICE.PV.1` — **except `Sandboxes`, which shows `FullAWSAccess` and nothing else, by decision**: nothing is attached or enabled there unless the configuration differs from `Interactive` (**D37**), and the Region control it briefly carried was disabled, its document `p-h7lc62d0` ceasing to exist with it. **Which of the two documents holds what cannot be inferred and must be read**: the two root-user controls landed in the *original* guardrail on `Policy Test`, `Workloads` and `Interactive`, but in the *Region-control* document on `Identity` and `Data` — **and on `Security` the two enablements split across *both* documents at once** (2026-08-14): a **new** document for `CT.MULTISERVICE.PV.1` (`aws-guardrails-KAmzSQ`, `p-idgyiios`, one statement) while the two root-user controls went into the **pre-existing AWS guardrail** (`aws-guardrails-rFWRFL`, `p-2xyaqn66`), taking it from 11 statements to 13. **That is a third packing shape, measured — so the rule is not "one of two layouts", it is that the layout is unknowable without reading.** Control Tower packs per enablement, not per control — so counting policies is not counting controls, and a document's id says nothing about its contents. The sentence this row used to carry, kept because the *shape* still governs everything below it: every OU shows `FullAWSAccess` plus at least one `aws-guardrails-*` SCP. **Four OUs additionally show exactly one `awsds-org-scp-ou-*` document** (Stage 1c step 7.6): `Workloads`, `Data`, `Interactive` and `Identity`, one each and matching the OU name. **`Policy Test` and `Security` carry none of this project's**, and `Policy Test` carrying one means a battery is in progress or was interrupted. The organization **root** shows `FullAWSAccess` **plus this project's three SCPs — `awsds-org-scp-baseline`, `awsds-org-scp-perimeter` (7.5) and `awsds-org-scp-tag-enforcement` (7.8)** — and no Control Tower guardrail, which only ever goes to registered OUs. **Since 7.8 the root also carries one document of each remaining type**: `awsds-org-rcp-perimeter` (RCP), `awsds-org-tag-policy` (tag) and `awsds-org-declarative-ec2` (EC2 declarative). A `list-policies` that reports none of the last three is more likely a **filter defect than a detached policy** — `list-policies` takes exactly one `--filter`, and reading only `SERVICE_CONTROL_POLICY` reports the other three as absent both before and after they are attached. **Every node also shows `RCPFullAWSAccess`** — AWS attached it to the whole tree by itself when step 7.2 enabled the RCP type, exactly as `FullAWSAccess` mirrors the SCP type | `org-policy-baseline.txt` 3 | A guardrail missing from an OU that had one is Control Tower drift, not this project's doing. **`Sandboxes` acquiring any policy is now a finding, not information**: the rule since 2026-08-13 is that it carries none unless the configuration differs from `Interactive`, so a document there is either a deliberate divergence that belongs in writing or it is drift. **It needs none to be governed**, measured 2026-08-13: `sagemaker:CreateNotebookInstance` from `awsds-infra-sandbox-1` is denied by `Interactive`'s document, so the SCP half of inheritance is proven. A **second** `awsds-org-scp-ou-*` on one OU is a document that was copied rather than moved. The `Security` **guardrail** is the only one that started at 11 statements rather than 8 — the extra three are about the log-archive and audit buckets — and **since 2026-08-14 it reads 13**, the two root-user controls having landed there rather than in the Region document. **`Security` accounts therefore resolve to 26 statements in force, up from 23.** **`RCPFullAWSAccess` missing from a node is an RCP that would deny everything there**, since an RCP set with no allow is a closed door |
| **INV-12** | **Revised 2026-08-13 by step 7.7 and 2026-08-14 by Stage 1d step 12**, which added all three to `Security` as well — so the current shape is **every governed OU but `Sandboxes` carrying the Region ceiling and both root-user controls**. 7.7 added `CT.MULTISERVICE.PV.1` to all six governed OUs and the two root-user controls to five OUs — **every one of them with `ExemptAssumeRoot`, so each `GRRESTRICTROOTUSER` ANDs `Null: aws:AssumedRoot=true` with the root-ARN test; that condition missing from any OU carrying this control is an omission rather than a variant, and it closes `sts:AssumeRoot` into every account beneath it** (1a 6.7): **12 on `Policy Test`, `Workloads`, `Data`, `Interactive` and `Identity`; 12 on `Security` (a different 12 — the original 9 plus `CT.S3.PV.7`, `CT.S3.PV.8`, `CT.SNS.PV.1`); and *zero* on `Sandboxes`**, which briefly had one and is back to none by decision. **`Sandboxes` accepting `enable-control` is what closed verification (xi) — it *is* a registered target** — which is why carrying nothing there is now a choice rather than a limitation. **`Security` carries all three since 2026-08-14** (Stage 1d step 12, decision 10, closing open question 16): it was never in 7.7's target list, and the gap it left — `Log Archive` and `Audit` as the only governed accounts where a resource could be created in any Region — was decided rather than inherited. **It accepted `enable-control` despite being Control Tower's own foundational OU**, which was the step's one real unknown. Expect **15** controls there (its own 12 plus these three) — **derived, not measured**, since the enabled-controls list answers only from Management. **The two halves are verified by different instruments and neither substitutes for the other:** `ExemptAssumeRoot` and the deliberate *absence* of an access-key exemption are read out of the document by `./aws/org-policies.py` (`CHK-1`/`CHK-2`, both passing on `Security`), while the Region ceiling was probed **by hand in CloudShell** in each account — `ec2 create-key-pair --dry-run` denied in `us-east-1` naming `p-idgyiios`, `DryRunOperation` in `us-west-2`. **That hand-probe is not repeatable by the battery**: neither account holds a CLI profile, by design, so a `Security` regression here surfaces only in `CHK-1`/`CHK-2` and in section 4's table. The old reading of this row — 9 everywhere and an *empty list* on `Sandboxes` — `Security` names its controls in the `controlcatalog:::control/<opaque>` namespace, every other OU in `controltower:<region>::control/AWS-GR_*` | `org-policy-baseline.txt` 5 — **Management only**; a member account is told *"you must create a landing zone first"* | The `Security` extras are the control-half of its three extra SCP statements, so the two halves agreeing is the expected state and a divergence is Control Tower drift. **The empty `Sandboxes` answer is not yet evidence of registration**: nothing in that run errored, so the "unregistered targets error" discriminator was never exercised (Lesson 13). Stage 1c step 7.7's `enable-control` is what settles it. The two id namespaces are one service, not two — resolve either with `controlcatalog get-control`, whose `Aliases[0]` is the `AWS-GR_*` name |
| **INV-13** | **AWS Config records eight accounts and not the ninth.** The organization aggregator `aws-controltower-ConfigAggregatorForOrganization`, in **Audit**, lists exactly the eight governed accounts — **Management is absent, and that is correct**: it holds neither a configuration recorder nor a delivery channel, by Control Tower's design and by **decision 8**, which declined to build one (Stage 1d verification (xiii), 2026-08-14). Each account records **79-86** resources with the recorder Control Tower owns — `allSupported: true`, `includeGlobalResourceTypes: true`, `recordingFrequency: CONTINUOUS`, `recordingScope: PAID` — and roughly a third of that baseline is AWS service defaults that are recorded once and never change | — (no script section yet; `select-aggregate-resource-config` from Audit, or `select-resource-config` per profile) | **A governed account missing from the aggregator has a stopped or deleted recorder** — which `GRCONFIGENABLED` denies to everyone but `AWSControlTowerExecution`, so it is either landing-zone drift or that role was used by hand. **Management *appearing* is the opposite finding and is not drift**: it means something built a recorder there, which nothing in this project does, and it makes the `iam-root-access-key-check` of D16 nearly free — decision 8's own revision trigger. A count far above ~86 in an idle account is churn worth pricing before it becomes the Config bill (`docs/PRICING.md`) |
| **INV-14** | **The organization CloudTrail bucket is locked, and its lifecycle is now floored by that lock.** `aws-controltower-cloudtrail-logs-*` in Log Archive carries S3 Object Lock, `ObjectLockEnabled: Enabled`, default retention **`COMPLIANCE` / 90 days**, set 2026-08-15 by Stage 1d step 9 through `AWSControlTowerExecution` assumed from Management — the only principal `CTS3PV8` exempts. Versioning is `Enabled` (a precondition, not a step: `s3:PutBucketVersioning` is denied to everyone) and one lifecycle rule, `RetentionRule`, expires current *and* noncurrent versions at **365 days**. **The two numbers are coupled**: the lifecycle may never be shortened below 90 days, and the retention can never be shortened at all | — (`get-object-lock-configuration` and `get-bucket-lifecycle-configuration` from Log Archive; no script section, no CLI profile reaches that account) | **A lifecycle expiration below 90 days is the failure this invariant exists to prevent** — the landing zone's own deletions would start failing against locked versions, unfixably, and the trigger is a cost pass at Stage 12 step 5. **Object Lock reading absent means the setting was removed, which is not possible** — so it means the bucket is not the one the trail writes to; read `S3BucketName` off `aws-controltower-BaselineCloudTrail` rather than matching a name. **An object delivered today with no retention** means the default stopped applying, which is the only way this control fails quietly |
| **INV-09** | **Restated 2026-08-14 by Stage 1d step 11.1. Eight** service principals hold trusted access — `access-analyzer`, `cloudtrail`, `config`, `controltower`, `iam`, `member.org.stacksets.cloudformation`, **`ram`** and `sso` — and exactly **three** are delegated: `access-analyzer` and `config` to **Audit**, `sso` to **Identity**. **`ram` is enabled and not delegated**, and it was turned on through `ram:EnableSharingWithAwsOrganization` rather than through Organizations, which is what also created `AWSServiceRoleForResourceAccessManager` in Management | `org-trusted-access-services.txt` 1, 3 | A ninth principal is a service allowed to read the organization and create its own roles in every account, that no stage turned on. A *fourth* delegation is a member account administering a service org-wide. Six of the eight are the landing zone's; `access-analyzer` and `ram` are this project's. **`ram` present without `AWSServiceRoleForResourceAccessManager` is the one failure shape worth recognising here**: it means trusted access was enabled from the Organizations side, the list reads correct, and organization-wide sharing still does not work (INT-11) |
| **INV-15** | **The organization carries a resource-based delegation policy, and it names one account.** Since 2026-08-15 (Stage 2 step 5.1) `describe-resource-policy` answers **PRESENT** with **three** statements, whose sole principal is the **Identity** account — one navigation statement, one policy-lifecycle statement conditioned on the four policy types, one tagging statement. All nine `DEL-*` checks pass, **including the root-reach one**, and the grant is **exercised, not merely present**: an `organizations:UpdatePolicy` on a root-attached document succeeded from Identity on 2026-08-15 | `org-delegation.txt` 2, 3 | **ABSENT means `terraform-live/identity/org-policies/` cannot apply at all** — every write refused, the ten documents silently back to console-managed, and the report distinguishes that from a *denied read*, which is a different failure. **A principal that is not Identity, or a wildcard principal, is a grant nobody wrote**, and it reaches Control Tower's own guardrails as well as this project's. **Present-but-narrowed is the shape that hides**: a `Resource` list missing the policy-type ARN classes, or a policy-type condition on an operator without `IfExists`, denies every write on every document while the policy still reads as present — which is what `DEL-9` and `DEL-8`'s operator half exist to name. **The `Attach`/`Detach` half is exercised for the root and OU entries** (2026-08-15) — `AttachPolicy` on an already-attached pair returns `DuplicatePolicyAttachmentException`, and the same call from a principal with no delegation returns `AccessDeniedException`, which is what makes the first answer mean anything. **`account/…/*` is granted and unexercised** — nothing is attached to an account, so no inert call can reach it. This document is **hand-managed by decision**, so a change here is never Terraform drift |
| **INV-16** | **The organization analyzer's findings are the assignment table, restated by the SAML trust.** Every finding in `awsds-org-external-access` (first read 2026-08-15) is `ACTIVE` / `ExternalAccess` / `AWS::IAM::Role`, and every role is an `AWSReservedSSO_*` permission-set role: Identity Center roles trust the directory's SAML provider, which an ORGANIZATION zone of trust counts as external. The set therefore mirrors **A.1 exactly** — one finding per assignment-backed role per account, **35 on 2026-08-15**: 4 in each vended account and in `Policy Canary`, 5 in Management, 3 in Audit and in Log Archive | `audit-iam-analyser.txt` 4 — CloudShell inside Audit, like the rest of that snapshot | **A finding of any other shape is the analyzer doing its job** — a resource policy reaching a principal that is neither this organization nor its directory. The count moving with **no** A.1 change is the same signal in a quieter shape. A finding *vanishing* without an assignment being removed means an archive rule appeared, which INV-10 forbids |

| **INV-17** | **The landing zone is `IN_SYNC`, at the latest version, and it has run exactly once.** Read from Management on 2026-08-16 (Stage 2 verification (iii)): `status: ACTIVE`, deployed version **`4.0`** equal to `latestAvailableVersion`, `driftStatus: IN_SYNC`, `remediationTypes: INHERITANCE_DRIFT`, and a manifest of six keys — `accessManagement`, `backup` (disabled), `centralizedLogging` and `config` (both 365-day retention), `governedRegions: ["us-west-2"]`, `securityRoles`. **`list-landing-zone-operations` returns a single `CREATE`/`SUCCEEDED`** — no update, no reset, ever. **Two things the manifest is *not*:** it names no policy of any kind, not even the `aws-guardrails-*` SCPs Control Tower owns, so it bounds the landing zone's **configuration** and not what drift detection compares; and it has no concept of an organization **resource policy**, which is why `IN_SYNC` is *silence* about the 5.1 delegation rather than approval | `management-landing-zone-drift.txt` 2-4 — CloudShell inside Management, the account no CLI profile reaches | **`DRIFTED` is the finding**, and the resource policy is the *last* thing to suspect: an OU moved by hand, an account moved out of its OU, or a guardrail SCP detached from a governed OU all raise the same flag, and the console names the item. **`latestAvailableVersion` moving past the deployed one is the moment this invariant becomes interesting** — a landing-zone update is the re-evaluation nothing has forced yet, and it is the first occasion on which the delegation could be disturbed. **A second operation appearing is not a problem; it is the evidence that was missing** — re-read section 5 of the same report afterwards and confirm the resource policy is still `PRESENT` with its condition on two statements. **Empty output is not `IN_SYNC`**: both landing-zone calls succeed and return empty from a member account, which is why the report suppresses sections 2-4 and exits 2 outside Management |

### A.1 — the assignment shape, per class of account

An account is reached by more principals than the project put there. Counting only the project's two and
calling a Control Tower row "drift" is the mistake this table exists to prevent.

| Account class | Assignment rows expected |
|---|---|
| **Vended** — Data Governance, Development, Identity, Production, Sandbox Account 1 | `InfrastructureAccess` → `sso-group-infrastructure`; `AWSOrganizationsFullAccess` → `AWSControlTowerAdmins`; `AWSPowerUserAccess` → `AWSSecurityAuditPowerUsers`; `AWSReadOnlyAccess` → `AWSSecurityAuditors`. **No direct `USER` assignment** — the D32 Account Factory ones were removed in 1b step 5.1 (2026-08-12), and their return is what verification (vi) is watching for |
| **Policy Canary** | **`AWSAdministratorAccess` → the infrastructure user *directly*** (D32) and nothing else of this project's — no `InfrastructureAccess`, by design (D29). It is the **only** direct `USER` assignment left in the organization, and it is permanent: there is no group and no `awsds-infra-*` profile behind it, so removing it removes the only way in |
| **Audit** | `AWSAdministratorAccess` → `AWSAuditAccountAdmins` and → `AWSControlTowerAdmins`; `AWSPowerUserAccess` → `AWSSecurityAuditPowerUsers`; `AWSReadOnlyAccess` → `AWSSecurityAuditors` |
| **Log Archive** | `AWSAdministratorAccess` → `AWSControlTowerAdmins` and → `AWSLogArchiveAdmins`; `AWSPowerUserAccess` → `AWSSecurityAuditPowerUsers`; `AWSReadOnlyAccess` → `AWSLogArchiveViewers` and → `AWSSecurityAuditors` |
| **Management** | Control Tower's own only: `AWSAdministratorAccess`, `AWSPowerUserAccess`, `AWSReadOnlyAccess`, `AWSServiceCatalogAdminFullAccess` → `AWSServiceCatalogAdmins`, `AWSServiceCatalogEndUserAccess` → `AWSAccountFactory`. **No `InfrastructureAccess` and no direct user assignment** |

**Only three of those rows reach a principal that exists today**; the rest point at the empty groups of
INV-05. They are `sso-group-infrastructure` on the vended accounts and the one surviving direct D32
assignment on `Policy Canary` — both the infrastructure user — and **`AWSControlTowerAdmins` through
`AWSOrganizationsFullAccess`, which reaches every vended account**. The third one is Control Tower's, not
the plan's, and **its reach was measured on 2026-08-15** — in Stage 2, not in the 1c step that had been
carrying the question, and it came back as *two* answers. **In a plain vended account it is nearly inert**:
`describe-organization` and the account's own `describe-effective-policy` answer, and `list-roots`,
`list-accounts`, `list-policies`, `describe-policy`, `list-parents` and `describe-account` all return
`AccessDenied`. **In `Identity` it reaches the whole organization and its policy writes** — not because of
the permission set, but because Stage 2 step 5.1's delegation names the *account* (`INV-15`), so every
principal there holding `organizations:*` inherits it. What is left of open question 11 is a decision,
not a measurement.

## B. Known exceptions

| ID | What the snapshot shows | Disposition |
|---|---|---|
| **EXC-01** | An account named `Sandbox`, `SUSPENDED`, attached **directly to the organization root** — not to be confused with `Sandbox Account 1`, which is correctly under `Sandboxes` | Left over from an experiment that predates this project and has nothing to do with it (user, 2026-08-11). **Ignore it.** Do not propose adopting it, moving it into an OU, or deleting it, and do not read it as a violation of INV-02. It runs nothing, so the policy sets never reaching it costs nothing. *Not verified:* whether a closed account still occupies a slot in the organization's account quota — which matters only if the cap binds again |

## C. True now, and expected to change

Listed so that a future session does not "fix" something that a later stage is going to do properly.

| What the snapshot shows today | Changes at |
|---|---|
| ~~Only `SERVICE_CONTROL_POLICY` is `ENABLED` on the root~~ — **done: all four types read `ENABLED`** (7.2), **and since 7.8 all four are also *attached*** (2026-08-14): `awsds-org-rcp-perimeter` (RCP), `awsds-org-tag-policy` (tag), `awsds-org-declarative-ec2` (EC2 declarative) and a third SCP, `awsds-org-scp-tag-enforcement`, all on the **root**. What 7.2 left behind is `RCPFullAWSAccess` on every node, part of INV-11 | Nothing further — **1c is closed.** The RCP is the one document to re-read before adding any `sts:` action to it: its first version denied `AssumeRoleWithSAML` and locked every SSO user out of all six member accounts |
| `InfrastructureAccess` is the only permission set that is not Control Tower's, and it has **no permissions boundary** | Stage 2 step 5 writes the other six; **how their boundaries are delivered is Stage 2's decision 4** (each account's `foundation/`, an AWS-managed policy, or inline-only until Stage 3). One permission set was created by hand on purpose; six were only *specified* |
| Only `sso-group-infrastructure` holds any assignment — the other four `sso-group-*` groups reach no account | Same place: the group→account assignments are Terraform, not console |
| **Account-level S3 Block Public Access reads all four flags `true` in all nine accounts** (7.4 step 1) — the six with a profile are re-measurable here; **Management, Log Archive and Audit were set and console-verified by the user and this laptop cannot read them**, so `account-bpa.txt` will always say "6 of 6 *measured*" and section 4 is the rest of the sentence | **Done in Stage 1c**: 7.5 denies changing BPA with one carve-out, 7.8 attached the remaining three policy types. **At every vend, forever**: a new account lands with BPA unset and inherits the deny on arrival (D34/D35, Stage 14) |
| **The root carries this project's two SCPs and four OUs carry one each** — 7.5 and 7.6 are done and both were exercised (INV-11). **7.7 is done: `CT.MULTISERVICE.PV.1` and the two root-user controls on five OUs** — `Policy Test`, `Workloads`, `Data`, `Interactive`, `Identity` — **and Stage 1d step 12 added the same three to `Security` on 2026-08-14**, leaving **`Sandboxes` as the only OU carrying none, by rule (D37)**. **Every governed account now sits under a `us-west-2` ceiling** | **Nothing pending — open question 16 is closed by decision 10.** What the ceiling on `Security` *commits* is elsewhere: GuardDuty (Stage 15 since the 2026-08-18 split), Security Hub (Stage 5) and Macie (Stage 11) are **not** in the control's `NotAction` list, so each must be enabled in `us-west-2`, which is this design anyway. Everything else here is closed: 7.8 attached the RCP, the tag policy, the tag-forcing SCP and the declarative EC2 policy on 2026-08-14, and INV-11's root line is restated accordingly |
| **`awsds-org-scp-baseline` carries 8 statements, not 7** — `DenyImageAndSnapshotExport` was added 2026-08-13 (7.5a) — and `DenyUserCompute` carries **18 actions** in both `awsds-org-scp-ou-data` and `awsds-org-scp-ou-identity` (7.6a). All three read back from Organizations after upload and were re-probed | Nothing pending. **A future amendment repeats the same order**: upload, read back, re-probe — phase 4b for an OU document, phases 1-3 on the canary for a root one ([`docs/plan/runbooks/scp-battery.md`](plan/runbooks/scp-battery.md)) |
| **`Policy Canary` holds nothing** — no bucket, no ECR repository, no IAM user — and `Policy Test` carries no policy of this project's | It is emptied at the end of **every** battery ([`docs/plan/runbooks/scp-battery.md`](plan/runbooks/scp-battery.md)). A throwaway resource still there means a battery was interrupted; read the log before deleting it, because a battery in progress is the one time it is expected |
| There is no `Staging` account | The vend is held on the account cap — the quota-increase request sits in an **open AWS support ticket** (2026-08-15); `./aws/cloudshell/management-quotas.sh` from Management is what says it landed. See the Current position in [`CLAUDE.md`](../CLAUDE.md) |
| **Exactly three VPCs exist, all project-range, all Stage 3 pass 1 (applied 2026-08-16):** Sandbox `10.20.0.0/16`, Development `10.50.0.0/16`, Production `10.30.0.0/16` — each with 3 tiers × 2 AZs, S3+DynamoDB gateway endpoints on the org/allow-list policies (the INT-05 `[P]` anchors), a 30-day flow log, and the zones `sandbox.internal`, `prod.internal`, `pages.internal`. **Data Governance, `Policy Canary`, Identity, Log Archive and Audit hold no VPC** (D22, D29). The Account Factory vend artifacts are **gone** (step 0, same day): every stack instance of `AWSControlTowerBP-VPC-ACCOUNT-FACTORY-V1` deleted from Management, **nothing survived**, and Account Factory's network configuration creates **no VPC** | **The configuration half is proven at the `Staging` vend** — run `./aws/networking.py` at that vend and at every later one. A VPC in `172.31.0.0/16` anywhere means the Account Factory configuration was reverted; a project-range VPC in Data Governance or the canary is an `NT-1` FAIL. **Since pass 2 (2026-08-16) the instrument reads 0 FAILED** — two peerings (Sandbox↔Production, Development↔Production), four cross-account zone associations, 22 subnet-level peering routes — so **any red NT row is now a finding**, and a third peering or an association beyond the four is one too |
| **The metered network is `[E]` and is UP right now** — Stage 3 pass 3, applied through `make up` 2026-08-16: one NAT (design A, decision 4) and its Elastic IP per account, the private tier's `0.0.0.0/0` toward it in both AZs, and the interface endpoints of step 8.3 — **Sandbox 12, Development 11, Production 10**, single-AZ (`usw2-az1`) and private-DNS on, each carrying step 9's `aws:PrincipalOrgID` + `aws:PrincipalIsAWSService` document. `./aws/egress.py` reads **all checks passed** and **USD 0.480/h** | **This row describes a session, not a state.** Between sessions the correct reading is **zero endpoints, zero NATs, USD 0.00/h** (D11) — anything else is a forgotten `make down`, and by decision D12 no budget alert will say so. `./aws/egress.py` §6 is the only instrument that will. What survives a `make down` is `foundation/`: a *changed* gateway-endpoint id after a cycle is a finding, and so is an interface-endpoint id that did **not** change (INT-05, step 8.6) |
| **The VPN host exists and the anchors are consumed** — Stage 4 steps 2.3, 4.3 and 1.4, all applied 2026-08-17. The `[P]` half in `sandbox/foundation/`: the Elastic IP, **now associated**, the security group `awsds-sandbox-vpn` carrying **the estate's first and only world-open rule** (UDP/51820, `VP-3`), and the Secrets Manager container `awsds-sandbox-vpn-host-key` — **now holding one `AWSCURRENT` version, enrolled by hand and never by Terraform** (decision 4, third review). The `[D]` half in `sandbox/vpn/`: one `t4g.nano` running in `usw2-az1`, IMDSv2 required, `wg0` up on `10.90.0.1/24` admitting **two peers** — `mbp` at `10.90.0.2/32` and `raspi` at `10.90.0.3/32` (steps 4.1/4.2) — the log group `/awsds/sandbox/vpn` at 30 days, **already receiving named handshake lines**, and the health alarm. **`mbp` handshaked for the first time on 2026-08-17 at 12:15:17Z** and the tunnel was brought down again after the test; `raspi` has no client configuration and correctly reads `handshake=never`. `./aws/vpn.py` reads **0 FAILED, `VP-1` through `VP-6` and `VP-9` all passing**; `make status` reads **UP, 1 instance, USD 0.0042/h**, over the same **monthly** floor of ~USD 4.05 (EIP 3.65 + secret 0.40) plus 8 GB of gp3 | **This row now describes a `[D]` slice, so the expected reading depends on the session** — `make down` leaves it *stopped*, never destroyed, and the address and the secret do not move. **`VP-2` reading *allocated, no host yet* from here on is an orphan allocation**, not a stage in progress. **A world-open rule that is not this one is an `NT`/`VP-3` finding.** `RotationEnabled` reading true **ever** is `VP-9` FAIL and the keys runbook's one rule violated by machine. **A second `AWSCURRENT` version that nobody rotated deliberately (§3) means the secret was written by something that should not write it.** **Adding, removing or rotating any peer replaces the instance** by design (the roster rides the user data) — and the replacement is invisible to client configs, measured: the rebuild of 4.2 kept both the interface key and `52.89.212.1`. A host public key that ever *does* change without procedure C having run means the secret's value moved. **A handshake line reading `peer=unknown` is a peer the roster does not know about** — the `wg set` stopgap of the keys runbook §4, or worse |
| Audit holds one analyzer, external-access only (INV-10) | **Stage 12** adds the unused-access analyzer, which is the paid half. Until then a second analyzer there is a finding, not a head start |
| **Lake Formation in Data Governance IS governing, since 2026-08-18 (Stage 5 pass 1 applied)** — the row this replaces said the opposite and was true until that apply. Read back from `GetDataLakeSettings`: **`DataLakeAdmins`** holds exactly one principal, the account's `AWSReservedSSO_InfrastructureAccess_*` role (decision 5 — the governance manager is deliberately NOT an admin); **both `CreateDatabaseDefaultPermissions` and `CreateTableDefaultPermissions` read `[]`** — the `ALL`-to-`IAM_ALLOWED_PRINCIPALS` defaults are gone, and they were cleared **before the first database existed**, which is why the three databases (`raw`, `curated`, `dropbox`) carry no `IAMAllowedPrincipals` grant at all (measured per database); `Parameters` still reads **`CROSS_ACCOUNT_VERSION: 4`, `SET_CONTEXT: TRUE`** — INT-11 survived its first apply of this resource. The lake itself: five `awsds-data-*` buckets under one CMK (`alias/awsds-data-data` — named `-zn-lab` until the 2026-08-19 revision withdrew the `security-zone` dimension), `raw` and `curated` registered through `awsds-data-lf-registration`, the two LF-Tag keys (`classification`, `layer` — `security-zone` existed 2026-08-18/19 and was removed with the revision), one Iceberg table `curated.sample_trades`, `awsds-data-catalog-maintenance` with two unscheduled crawlers and a compaction optimizer | **Stage 5's 4d/4e and pass 6 still change this; passes 2, 3 and 4a-4c have landed.** Since **2026-08-19** the account also holds the governance manager's grants (pass 2 — nine; eight since the same-day revision revoked the `security-zone` ASSOCIATE) and **four cross-account grants producing four `LakeFormation-V4-*` RAM shares, all `ACTIVE`, held by both consumers with no invitation anywhere** (pass 3) — the org-sharing path working is what "no invitation" means, and a `PENDING` row is INT-11's fallback tax appearing. **The consumer side of that share read EMPTY between pass 3 and pass 4a, and that was correct, not a failure**: `Sandbox Account 1` and `Development` each held their two shares in RAM while `glue:GetDatabases` and `list-lf-tags` returned nothing there, because neither account had a data lake administrator (`DataLakeAdmins: []`) and AWS requires one before a shared resource is visible at all. **Both consumers became Lake Formation accounts at pass 4a/4b (2026-08-19)**: each holds its own `DataLakeSettings` — one administrator (`InfrastructureAccess`), `Parameters` carried explicitly at `CROSS_ACCOUNT_VERSION: 4` / `SET_CONTEXT: TRUE` (the INT-11 hazard is symmetric — both already read 4/TRUE in the before-reading), and both `Create*DefaultPermissions` cleared before the first resource link. *The empty-catalog reading above was measured 2026-08-19, before that apply.* What is still expected to be absent: Security Hub anywhere (pass 6, `DL-11`). **Present since pass 4b and expected**: two resource links and one enforced workgroup (`awsds-<env>-athena`) per consumer, plus `awsds-<env>-derived` under `alias/awsds-<env>-data` (`DL-7`, `DL-8`). **What must NOT change**: `Parameters` reading anything but 4/TRUE means something reset it (`DL-5`); an `IAMAllowedPrincipals` default reappearing means a `DataLakeSettings` apply dropped the emptying (`DL-6`) — and note the emptying is **not expressible in the plan**, so any future apply of that resource is read back, never assumed (the stage's 5.2 callout). A second admin is a principal nobody granted |
| The trusted-access list of INV-09 has eight principals and three delegations | **`ram` landed 2026-08-14** (Stage 1d step 11.1) and INV-09 is restated. It still grows by one at each of **Stage 15** (GuardDuty — Stage 4 until the 2026-08-18 split), **Stage 5** (Security Hub) and **Stage 11** (Macie). For those three, delegating *is* enabling, which is why they are not there yet — restate INV-09 as each one lands |
| **A persona's DIRECT S3 call from the tunnel is denied — *with an explicit deny in an identity-based policy* — and this is a known defect, not a finding to re-diagnose** (measured Stage 5 pass 4d, 2026-08-19, in **both** consumers from two different provisioned roles). `DenyControlPlaneOffVpn` pins `aws:SourceIp` to the VPN Elastic IP, but tunnel traffic to S3 leaves through the `[P]` gateway endpoint and arrives carrying the WireGuard host's **private** address plus a `vpcEndpointId` — measured in CloudTrail (`10.20.160.254` + the Sandbox S3 endpoint) against the same session's Glue call arriving as `52.89.212.1` with none. **So a person cannot list or download their own Athena result**, and the drop-box `PutObject` of D18/D25 is refused. What still works: anything service-mediated (`aws:ViaAWSService`), which is why Athena *writes* the result it cannot then be fetched | **Stage 5 pass 4d recorded it; the amendment is NOT applied.** Fix shape: a `StringNotEqualsIfExists aws:SourceVpce` condition over the **VPN homes'** `s3_gateway_endpoint_id` — **not** the consumers', which are never on the path (the single VPN home is also a consumer, so `consumer_vpce_ids` works by coincidence). `identity/sso/` already reads the `vpn_home` state, so no new output is needed. **Two things stay unmeasurable until it lands** and must not be reported as passing: the drop-box asymmetry (all three verbs fail for this one unrelated cause) and **D13's own mechanism**, whose implicit deny this explicit one masks. **Lesson 33** |
| **The two Glue crawlers have never run and CANNOT be run by any principal that exists** — `awsds-data-raw` and `awsds-data-dropbox` both read `Schedule: null`, `LastCrawl: null`, with zero triggers and zero workflows in the account. `DenyCatalogMaintenanceRunsExceptMaintenanceRole` admits only `awsds-data-catalog-maintenance` **or** a service principal, and that role's trust policy carries one statement (`GlueServiceOnly`, `Principal.Service = glue.amazonaws.com`) — it is an *execution* role, never an identity anyone assumes. Verified by reading, **Lesson 22**; the negative half was measured 2026-08-19, `InfrastructureAccess` in the `Data` OU refused *with an explicit deny in a service control policy* | **Neither document is at fault — the crawlers' `Schedule` is a decision nobody has taken**, and it is an open question (frequency, and whether the drop-box's cadence differs from `raw`'s). **Do not "fix" this by loosening the SCP or the trust policy**: the only invocation path the design permits is Glue's own scheduler, and adding a `Schedule` is what turns it on. **Read together with the row above**: D18/D25's ingestion is `PutObject → crawler catalogues`, both halves are broken, and the causes are **independent** — amending the VPN statement fixes half a path |

## Lake Formation grant register

**One row per applied grant triple `[principal, tag expression / resource, permissions]` — written in
the same sitting as the grant**, the discipline `POLICIES.md` keeps for policy statements.
[`docs/GOVERNANCE.md`](GOVERNANCE.md) defines the model and references this table; the stage log carries
each grant's story. A grant found in AWS with no row here is drift, in either direction.

*Thirteen rows covering **24 applied triples**, in four groups (25 until 2026-08-19: the `ASSOCIATE` on
`security-zone` was revoked when the user's revision withdrew the tag — the register's first removal,
noted on its row rather than deleted from it). Pass 1's four are **operational** —
same-account, named-resource by design, the machinery the catalog needs to maintain itself. Pass 2's
three rows are the first grants to a **human persona** and cover eight triples.
**No grant option exists on any pass-1 or pass-2 row, and no pass-2 row carries `SELECT` — pass 1's
single `SELECT` is the compaction grant on `curated.sample_trades` (decision 4), a service role, never a
persona; all verified by `list-permissions` against the API, not inferred from the code.** Pass 3's two rows are the first **cross-account** grants
and the first anywhere to carry the **grant option** — which for a cross-account grant is mandatory, not
discretionary: the share lands on the account and its own data lake administrator must pass it on
(`GOVERNANCE.md` §Grants). They are also the only rows whose principal is an **account** rather than a
role. **Pass 4's four rows are the RE-GRANTS** — the same expressions passed on inside each consumer
account to a local principal, which is the only way anything in a receiving account can use a share, plus
`DESCRIBE` on each resource link (a local database object, and a permission distinct from any permission
on the target). None of them carries the grant option: the reader is never the grantor (1b step 3.7).*

| Date | Principal | Expression / resource | Permissions | Why (log entry) |
|---|---|---|---|---|
| 2026-08-18 | `awsds-data-catalog-maintenance` | database `raw` | `CREATE_TABLE`, `DESCRIBE` | the raw crawler writes inferred tables (Stage 5 pass 1) |
| 2026-08-18 | `awsds-data-catalog-maintenance` | database `dropbox` | `CREATE_TABLE`, `DESCRIBE` | the drop-box crawler writes inferred tables (idem) |
| 2026-08-18 | `awsds-data-catalog-maintenance` | data location `awsds-data-raw` | `DATA_LOCATION_ACCESS` | a table pointing into the registered raw prefix (idem) |
| 2026-08-18 | `awsds-data-catalog-maintenance` | table `curated.sample_trades` | `SELECT`, `INSERT`, `ALTER`, `DESCRIBE` | Glue automatic compaction, decision 4 (idem) |
| 2026-08-19 | `GovernanceManagerAccess` (SSO role, resolved by pattern) | LF-Tag `classification` (4 values), `layer` (3) — **2 triples** (a third, on `security-zone`, was granted at pass 2 and **revoked 2026-08-19** when the revision withdrew the tag) | `ASSOCIATE` (implies `DESCRIBE` on the tag) | assigning tags to datasets, the persona's job per `GOVERNANCE.md` — decision 5's "specific grants instead of admin" (Stage 5 pass 2) |
| 2026-08-19 | idem | databases `raw`, `curated`, `dropbox` — **3 triples** | `DESCRIBE` | without it the persona sees an **empty catalog**: LF filters what `glue:GetDatabases` returns (idem) |
| 2026-08-19 | idem | `ALL_TABLES` wildcard in each of the three databases — **3 triples** | `DESCRIBE` | the tables it must tag, including ones the crawlers have not inferred yet (idem) |
| 2026-08-19 | **account** `Sandbox Account 1`, **account** `Development` — **2 triples** | LF-Tag expression on **databases**: `layer ∈ {raw, curated}` | `DESCRIBE` **with grant option** | the container the consumer's resource links point at; no classification gate, because `curated`'s database carries no classification and an unmatched database cannot be linked (Stage 5 pass 3) |
| 2026-08-19 | idem — **2 triples** | LF-Tag expression on **tables**: `layer ∈ {raw, curated}` **AND** `classification ∈ {public, internal}` | `SELECT`, `DESCRIBE` **with grant option** | the default consumer read of `GOVERNANCE.md` §Grants. The `layer` half keeps the drop-box out; `restricted` and `personal` are absent by enumeration (idem) |
| 2026-08-19 | `DataScientistAccess` in `Sandbox Account 1` **and** in `Development` — **2 triples** | resource link `raw` (a local database) | `DESCRIBE` | a link is a local object and needs its own permission: without it the persona sees no database at all, whatever it holds on the target (Stage 5 pass 4) |
| 2026-08-19 | idem — **2 triples** | resource link `curated` | `DESCRIBE` | idem |
| 2026-08-19 | idem — **2 triples** | LF-Tag expression on **databases**, `catalog_id` = Data Governance: `layer ∈ {raw, curated}` | `DESCRIBE` | the re-grant of the account-level share to a local principal — an administrator may pass on only what it received with the grant option (idem) |
| 2026-08-19 | idem — **2 triples** | LF-Tag expression on **tables**, `catalog_id` = Data Governance: `layer ∈ {raw, curated}` **AND** `classification ∈ {public, internal}` | `SELECT`, `DESCRIBE` | the read itself. Verified by `list-permissions` in each account: four rows for the persona and nothing else, **no grant option on any of them** (idem) |

**Not a row, but visible in `list-permissions` and expected — and true in three accounts since pass 4.**
Each consumer account's `InfrastructureAccess` holds `ALL, DESCRIBE, DROP` **with grant option** on the
two resource links it created: Lake Formation grants the creator of a catalog object, and no code here
asked for it. The sentence below is the same fact in Data Governance, and it now reads one account
further out.

The `InfrastructureAccess` role holds
`ALL` on everything it created — Lake Formation grants the creator, and that principal is the data lake
administrator by decision 5. It is not an applied triple and nothing revokes it.

## D. Keeping this file true

- **When a stage closes**, move whatever it changed out of section C — an entry there that has already
  happened is worse than no entry, because it tells a future session to expect the old world.
- **When a snapshot surprises you**, the surprise ends up here as an `INV-nn` or an `EXC-nn`, or it is a
  real finding and belongs in `docs/log/` and possibly in a decision. Deciding which is the whole job.
- **An entry that names an identifier, or explains why, is in the wrong file.** Move it and leave a name.
- Reference entries by **stable ID** — `INV-03`, `EXC-01` — never by row position.

---

*Live state: [`aws/INDEX.md`](../aws/INDEX.md) · Target state: [`docs/ORGANIZATION.md`](ORGANIZATION.md) ·
What was done by hand: [`docs/log/INDEX.md`](log/INDEX.md)*
